# HootoDay Project Notes

## プロジェクト概要

- プロジェクト名：HootoDay
- 完全な個人利用専用
- スケジューラーと健康記録を統合したアプリ
- 最初はブラウザ版として開発
- 将来はPWA化、iPhone対応、デスクトップアプリ化を検討
- 配布、一般公開、複数ユーザー対応、ストア公開の予定なし

## 主要機能

### 1. スケジューラー

- 月間カレンダー
- 日付ごとの予定
- 開始時刻、終了時刻、終日予定
- 予定カテゴリ
- 短い日記・メモ
- 日記やメモがある日のカレンダー表示

### 2. 健康記録

- 睡眠
- 体重
- 朝食、昼食、夕食、間食
- 運動
- 体調メモ
- 各項目の専用閲覧ページ
- 週、月、半年、年単位の集計
- TXTまたはMarkdown出力
- JSONバックアップと復元

## 初期予定カテゴリ

- 収録
- ライブ
- リハ
- 配信
- 歌枠
- 歯医者
- 整体
- 通院
- おでかけ
- 映
- その他

将来、カテゴリを追加、編集、並び替えできるようにします。「その他」では予定名を自由入力できるようにします。

## 健康記録の概要

### 睡眠

- 就寝時刻と起床時刻から睡眠時間を計算する
- 途中覚醒は複数登録可能とする
- 覚醒時刻だけの場合は、初期値として15分を睡眠時間から差し引く
- 覚醒開始時刻と終了時刻がある場合は、実際の覚醒時間を睡眠時間から差し引く

### 体重

- 最新値、目標値、標準体重、美容体重、期間平均、グラフを将来表示する

### 食事

- 朝食、昼食、夕食、間食の各欄に自由記述で記録する

### 運動

- 1日に複数回登録可能とする
- 運動種類、時間、心拍数、推定消費カロリー、メモを記録する

### 体調

- 全体、膝、腰、生理・周期、気になることを記録する

## 開発方針

- 一度に大規模実装をしない
- Phase単位で実装、確認、記録、commitする
- データを壊す可能性がある変更では、事前にバックアップを用意する
- 自分専用のため複数ユーザー機能は作らない
- 入力の手軽さと見やすさを優先する
- PCとiPhoneでは将来別UIにしてよい
- 通知とiPhoneウィジェットは後期Phaseへ回す
- 健康データの計算値は医療診断ではなく、本人用の概算記録とする

## 予定しているPhase

| Phase | 内容 |
| --- | --- |
| Phase 0 | 初期環境と仕様記録 |
| Phase 1 | アプリ全体の画面枠と月間カレンダーの見た目 |
| Phase 2 | 予定の登録、編集、削除 |
| Phase 3 | 日記・メモ |
| Phase 4 | 健康記録入力 |
| Phase 4A | 健康記録画面、日付別体重記録、健康プロフィール |
| Phase 4B | 体重集計、目標、標準体重、美容体重、グラフ |
| Phase 4C | 睡眠 |
| Phase 4C-2 | 睡眠まとめ、期間平均、平均就寝・起床時刻、睡眠時間グラフ |
| Health UI Phase | 健康プロフィールの設定への移動と健康カードの共通日付UI整理 |
| Phase 4D | 食事 |
| Phase 4E | 運動 |
| Phase 4F | 体調メモ |
| Phase 5 | 各健康項目の専用閲覧ページと集計 |
| Phase 6 | TXT、Markdown、JSONの出力・復元 |
| Phase 7 | PWAと端末間同期 |
| Phase 8 | 通知、iPhoneウィジェット、デスクトップアプリ化 |
| Special Phase | 即売会・在庫記録（実装時期は後で決定） |

## 将来追加機能：即売会・在庫記録

### 機能の目的

- 即売会イベントごとに、商品別の持込数と販売結果を記録する
- 単なる自由記述メモではなく、数量と金額を数値として保存・集計する
- 年2回程度の利用でも、過去イベントとの比較や累計を確認できるようにする
- 完全な個人利用専用機能とする

### イベント単位の記録項目

- イベントID
- イベント名
- 開催日
- 会場
- 全体メモ
- 商品別記録
- 作成日時
- 更新日時

### 商品別の基本項目

- 商品ID
- 商品名
- 持込数
- 売れた数
- サンプル配布数
- 残数
- 単価
- 売上
- 商品別メモ

### 自動計算と入力条件

- 残数 = 持込数 - 売れた数 - サンプル配布数
- 売上 = 売れた数 × 単価
- サンプル配布数は売上に含めず、在庫だけを減らす
- 計算結果が負数になる入力は保存しない

### 集計したい内容

- イベントごとの総持込数
- イベントごとの総販売数
- イベントごとの総サンプル配布数
- イベントごとの総残数
- イベントごとの総売上
- 商品ごとの累計販売数
- 商品ごとの累計サンプル配布数
- 商品ごとのイベント後残数
- 年ごとの販売数と売上
- 前回イベントとの比較
- 過去イベント一覧

### HootoDayとの連携案

- カレンダーへ即売会予定を登録する
- 即売会予定または選択日詳細から「即売会記録を開く」
- 日記・メモ欄には当日の感想を書き、数量と売上は即売会記録へ分離する
- 将来「記録を見る」からイベント別・商品別集計を開く
- 将来TXT、Markdown、CSVなどへ出力できるようにする

### 将来拡張の余地

初期実装の必須項目にはしないが、データ構造上は次の項目を追加できる余地を残す。

- 追加生産数
- 関係者への配布
- 破損・廃棄数
- 通販分
- 自宅に残した在庫
- 商品画像
- 原価と利益

### Special Phase：即売会・在庫記録

- 健康記録や出力機能とは混ぜず、独立したPhaseとして扱う
- 実装時期は後で決定する
- 初期実装はイベント、商品、持込数、販売数、サンプル配布数、残数、単価、売上を対象とする
- 高度な在庫移動、追加生産、原価・利益計算は後期拡張とする
- 既存の日記・予定・健康記録とは別データとして保存する

## 現在の技術構成

`package.json`に記載されている現在のバージョン指定は以下のとおりです。

### dependencies

- React：`^19.2.7`
- React DOM：`^19.2.7`

### devDependencies

- Node.js型定義（`@types/node`）：`^24.13.2`
- React型定義（`@types/react`）：`^19.2.17`
- React DOM型定義（`@types/react-dom`）：`^19.2.3`
- Vite Reactプラグイン（`@vitejs/plugin-react`）：`^6.0.3`
- Oxlint：`^1.71.0`
- TypeScript：`~6.0.2`
- Vite：`^8.1.1`

## 作業履歴

### 2026年7月17日

- Vite React TypeScript初期環境作成
- `npm.cmd install`成功
- Gitリポジトリ初期化
- `npm.cmd run build`成功
- 開発サーバー起動およびHTTP応答確認
- 初期commit前の状態を確認

### Phase 1（完了）

- アプリ基本レイアウト作成
- 月間カレンダー表示
- 前月、次月、今日の移動
- 日付選択
- 固定サンプル予定表示
- メモ存在表示
- 選択日詳細欄
- データ保存や予定・記録の編集機能は未実装
- 主なコンポーネント：`App`、`AppHeader`、`Sidebar`、`Calendar`、`DayDetails`
- 確認項目：PC・スマートフォン幅の表示、月移動、今日への移動、日付選択、サンプル予定・メモ・選択日詳細の連動、キーボード操作、横スクロール、コンソールエラー
- 実画面確認で、PC表示が大きく固定的に見えることを確認
- ウィンドウ幅に応じて全体が段階的に縮むようCSSを調整
- `transform`による全体縮小は使わず、`clamp()`、`minmax()`、可変幅で対応
- 状態は引き続き「実装済み・実機確認待ち」
- Phase 1のcommitは実機確認完了まで行わない
- 実画面確認で、カレンダーセル内の日付が縦中央寄りに見えることを確認
- 日付を各セルの左上固定へ変更
- 予定を日付の下へ縦に並べる構造へ調整
- メモ印はセル右上の配置を維持
- 状態は引き続き「実装済み・実機確認待ち」とし、commitはまだ行わない
- `@phosphor-icons/react`を導入
- サイドバーメニューの簡易記号をPhosphor Iconsへ変更
- 前月・次月ボタンをPhosphor Iconsへ変更
- 必要なアイコンだけを名前付きimport
- 予定カテゴリにはまだアイコンを導入していない
- HootoDayロゴは現状を維持
- 状態は引き続き「実装済み・実機確認待ち」とし、commitはまだ行わない
- 初回の緑・オレンジ配色が全体的にくすんで見えることを確認
- 茶色寄りだったオレンジを、明るく濁りの少ない色へ変更
- 画面背景を非常に淡い緑へ変更
- サイドバーなどの補助背景へアイボリーを移動
- カードとカレンダーは白を維持
- レイアウトと機能は変更していない
- 状態は引き続き「実装済み・実機確認待ち」とし、commitはまだ行わない
- 視認性と可愛さを両立するため、`M PLUS Rounded 1c`を採用
- 配色を紫中心から、緑・オレンジ・白・チャコール中心へ変更
- 強調と注意にはくすみ赤を使用
- レイアウトと機能は変更していない
- 状態は引き続き「実装済み・実機確認待ち」とし、commitはまだ行わない

#### Phase 1完了記録

- 実機確認日：2026年7月17日
- PC表示でサイドバー、カレンダー、選択日詳細欄の3列レイアウトを確認
- ウィンドウ幅に応じて全体が段階的に縮小することを確認
- 必要な画面幅で選択日詳細欄がカレンダー下へ移動するレスポンシブ表示を確認
- カレンダーセルの日付数字が左上へ配置されることを確認
- 予定、メモ印、今日、選択日の表示を確認
- `M PLUS Rounded 1c`を正式採用
- 薄緑、アイボリー、白、緑、オレンジを中心とした配色を正式採用
- `@phosphor-icons/react`を正式採用
- 前月、次月、今日への移動、日付選択、選択日詳細欄との連動を確認
- Phase 1では予定の登録、編集、削除、保存機能は未実装
- Phase 1を完了とし、次はPhase 2「予定の登録、編集、削除」へ進む

### Theme Phase 1.5（完了）

- ライト、ダーク、端末設定連動の3種類を追加
- localStorageキー：`hootoDay.theme`
- 保存値と初期値：`light`、`dark`、`system`（初期値は`system`）
- `data-theme`属性へ実際に適用する`light`または`dark`を設定
- `system`選択時のみ`prefers-color-scheme`の変更へ追従
- ライトテーマはPhase 1で正式採用した配色を維持
- ダークテーマは深緑黒、チャコール、緑、オレンジを中心に構成
- サイドバーの設定ボタンから開く簡易テーマ設定パネルを追加
- テーマ用アイコンにPhosphor Iconsを使用
- localStorageへ保存するのはテーマ設定のみで、予定や健康記録などのデータ保存は未実装
- 実装後は「実装済み・実機確認待ち」として実画面確認を実施
- commitはユーザーの実画面確認後に行う方針とした
- 実機確認で、設定ボタンを押しても`dialog`が開かない不具合を確認
- 原因はStrictMode下でEffectクリーンアップの`close()`が、ユーザー終了と同じ`onClose`処理へ流れていたこと
- ユーザー操作による終了と内部クリーンアップを分離して修正
- React StrictModeは維持
- dialog修正後も実機確認待ちとして確認を継続
- この時点ではcommitを行わない
- ダークテーマ初版は緑を主役色として構成
- 実機確認で、ダークテーマはオレンジ主体へ寄せたい要望が出た
- ダークテーマの主役色をオレンジへ変更
- 緑はロゴや医療・整体系カテゴリなどの補助色として維持
- ライトテーマの配色は変更していない
- 配色調整後も実機確認待ちとして確認を継続
- この時点ではcommitを行わない

#### Theme Phase 1.5完了記録

- 実機確認日：2026年7月17日
- 設定ボタンから`dialog`が開き、開いた直後に勝手に閉じないことを確認
- 閉じるボタン、Escapeキー、backdropクリックで閉じることを確認
- `dialog`内部のクリックでは閉じないことを確認
- ライト、ダーク、端末設定の切り替えを確認
- 再読み込み後も選択が維持されることを確認
- `system`設定を保存できることを確認
- React StrictModeを維持したまま`dialog`の不具合を修正
- ライトテーマはPhase 1で正式採用した緑主体の配色を維持
- ダークテーマはオレンジ主体の配色を正式採用
- ダークテーマの背景は暖色寄りチャコールを採用
- ダークテーマの緑はロゴやカテゴリなどの補助色として維持
- 設定ダイアログの選択状態と可読性を確認
- カレンダーの月移動など既存操作が維持されていることを確認
- 予定や健康記録の保存機能は未実装
- Theme Phase 1.5を完了とし、次はPhase 2「予定の登録、編集、削除」へ進む

### Phase 2A（完了）

- 予定の新規登録UI、編集UI、削除機能を追加
- 入力項目：日付、予定名、カテゴリ、終日、開始時刻、終了時刻、補足メモ
- 予定名、日付、開始時刻、終了時刻の入力検証を実装
- 予定の変更をカレンダーと選択日詳細へ即時反映
- 初期サンプル予定も編集・削除可能
- 予定データはReact stateのみで管理
- ブラウザ再読み込み後は初期サンプル予定へ戻る
- 予定のlocalStorage保存はPhase 2Bで実装予定
- 主な追加・変更：`EventEditorDialog`、`DayDetails`、`Calendar`、`useEvents`、予定型・予定ユーティリティ
- 実装後は「実装済み・実機確認待ち」として実画面確認を実施
- commitはユーザーの実画面確認後に行う方針とした
- 実機確認で、予定入力中にダイアログ外クリックで閉じる問題を確認
- 誤操作による入力消失を防ぐため、予定入力ダイアログのbackdropクリック終了を廃止
- 保存、キャンセル、閉じるボタン、Escapeキー、削除完了時だけ閉じる仕様へ変更
- ThemeSettingsのbackdropクリックによる終了仕様は維持
- backdrop仕様変更後も実機確認待ちとして確認を継続
- この時点ではcommitを行わない

#### Phase 2A完了記録

- 実機確認日：2026年7月17日
- 予定の新規登録を確認
- 通常予定、終日予定、終了時刻付き予定を確認
- 予定名、カテゴリ、日付、終日、開始時刻、終了時刻、補足メモを確認
- 予定名、日付、開始時刻、終了時刻の入力検証を確認
- 終了時刻が開始時刻以前の場合は保存しないことを確認
- カレンダーと選択日詳細への即時反映を確認
- 予定の編集と同一IDでの更新を確認
- 日付変更による予定移動を確認
- 対象予定だけが削除されることを確認
- 3件以上の予定で省略表示されることを確認
- 予定入力ダイアログはbackdropクリックでは閉じない仕様を正式採用
- 保存、キャンセル、閉じるボタン、Escapeキー、削除完了時だけ閉じる仕様を確認
- ThemeSettingsのbackdropクリックによる終了仕様は維持
- ライト・ダークテーマでの表示を確認
- Phase 2AではReact stateのみで予定を管理
- ブラウザ再読み込み後は初期サンプル予定へ戻る
- 予定のlocalStorage保存はPhase 2Bで実装する
- Phase 2Aを完了とし、次はPhase 2B「予定データの保存・復元」へ進む

### Phase 2B（完了）

- 予定データをlocalStorageへ保存
- 使用キー：`hootoDay.events`（テーマ設定の`hootoDay.theme`とは別管理）
- 保存形式：`{ version: 1, events: CalendarEvent[] }`
- 保存データのversion：`1`
- 将来の予定項目追加や形式変更を判別しやすくするため、配列直接保存ではなくversion付きラッパー形式を採用
- localStorageに保存データがない初回のみ初期サンプル予定を使用
- 保存済みデータがある場合は保存内容を優先
- 空配列を有効な保存状態として扱い、予定0件でも初期サンプルを復活させない
- 削除済み・編集済み予定を再読み込みで初期状態へ戻さない
- 不正JSON、version不正、eventsが配列でない場合は初期サンプル予定へ安全に戻す
- 配列内の一部予定だけが不正な場合は、不正項目だけを除外して有効な予定を復元する
- 元データが正しいversionの配列であれば、有効予定が0件になっても空配列として扱う
- localStorageの読み書きに失敗してもReact state上のアプリ操作を継続
- 予定の追加、編集、削除によるstate変更時に最新配列を保存
- Phase 2Aの表示・フォーム仕様は変更していない
- 実装後は「実装済み・実機確認待ち」として実画面確認を実施
- commitはユーザーの実画面確認後に行う方針とした

#### Phase 2B完了記録

- 実機確認日：2026年7月17日
- 新規登録した予定が再読み込み後も維持されることを確認
- 削除した予定が再読み込み後も復活しないことを確認
- 保存済みデータが初期サンプル予定より優先されることを確認
- localStorageキーは`hootoDay.events`
- version 1のラッパー形式を正式採用
- 空配列を有効な予定0件の状態として扱う
- 不正データでも白画面にせず安全に初期データへ戻す
- 配列内の一部だけが不正な場合は不正予定だけを除外する方針を採用
- localStorageの読み書きに失敗しても画面上の操作を継続
- テーマ設定の`hootoDay.theme`とは別管理
- Phase 2Aの表示や予定フォームの仕様は変更していない
- Phase 2Bを完了とし、次はPhase 2C「予定カテゴリのアイコン・短縮表示・時刻表示最適化」へ進む

### Phase 2C（完了）

- 実機確認日：2026年7月17日
- 全11カテゴリのPhosphor Iconsを実機で確認
- カレンダー内は14px、選択日詳細欄は20pxで判別可能なことを確認
- 560px以下ではカレンダー内アイコンを非表示
- 正式採用アイコン：収録→Microphone、ライブ→MicrophoneStage、リハ→Headphones、配信→Desktop、歌枠→MusicNotes、歯医者→Tooth、整体→PersonSimpleTaiChi、通院→FirstAidKit、おでかけ→Park、映→Ghost、その他→Cactus
- カレンダー用短縮カテゴリ：収録→収録、ライブ→ライ、リハ→リハ、配信→配信、歌枠→歌枠、歯医者→歯医、整体→整体、通院→通院、おでかけ→外出、映→映、その他→他
- 正式カテゴリ名と予定名が完全一致する場合だけ短縮表示し、自由入力した予定名は変更しない
- 時刻は分が`00`の場合だけ「20時」などの形式へ変換
- 分がある場合は`20:15`、`20:30`などの`HH:mm`表示を維持
- 終了時刻付き表示にも同じ時刻変換規則を適用
- 正式カテゴリ名、カテゴリ値、保存済みデータ、localStorageのversion 1保存形式は変更していない
- 全カテゴリを比較する一時確認パネルは実機確認完了後に削除
- Phase 2Cを完了とし、次はPhase 3「日記・メモ」へ進む

### Phase 3A（完了）

- 日付別の日記・メモ入力を追加し、1日につき1件を管理
- 本文は最大2000文字で、前後の空白を除去して保存
- localStorageキーは`hootoDay.dayMemos`
- `{ version: 1, memos: DayMemo[] }`形式で予定・テーマとは別に保存
- 新規保存、同一日付の上書き編集、確認付き削除に対応
- カレンダーのメモ印を保存済み実データと連動し、Phase 1の固定サンプルメモ表示を削除
- 選択日詳細欄に保存本文の概要を最大3行で表示
- 日記・メモdialogはbackdropクリックでは閉じず、保存、キャンセル、閉じる、Escape、削除完了時に終了
- 保存データ全体が不正な場合は空配列へ戻し、一部項目だけ不正な場合は不正項目だけを除外
- 同じ日付が複数ある場合は`updatedAt`が新しい項目を採用し、同時刻の場合は後の項目を採用
- localStorageの読み書き失敗時もReact state上の操作を可能な限り継続
- 実機確認に基づき、メモ存在表示をオレンジ色の丸からPhosphor Iconsの`PencilSimpleIcon`へ変更
- ペンシルは日付セル右上の配置を維持した表示専用アイコンで、クリック機能は追加していない
- 日記・メモ編集は引き続き選択日詳細欄の「記録を開く」から行う
- 実機確認日：2026年7月17日
- 新規保存、カレンダー右上のペンシル表示、日付・予定と重ならない配置を確認
- 選択日詳細の本文概要表示と、編集後の再読み込み維持を確認
- 削除後のペンシル消去と、再読み込み後も削除状態が維持されることを確認
- backdropクリックでは閉じず、入力途中の本文が維持されることを確認
- 保存成功、キャンセル、閉じる、Escape、削除完了の終了条件と、未保存入力の破棄を確認
- ライト・ダークテーマでの表示を確認
- localStorageキー`hootoDay.dayMemos`とversion 1形式を正式採用
- Phase 3Aでは日記一覧、検索、健康記録、TXT・Markdown出力は未実装
- Phase 3Aを完了とし、Phase 3BまたはPhase 4へ進む前に日記・メモの次工程を整理する

### Phase 4A（完了）

- Phase 4Aの範囲を、健康記録画面の共通土台、日付別体重記録、健康プロフィールとして整理
- 健康記録画面の共通土台を追加
- App内stateでカレンダーと健康記録を切り替え、選択中ナビゲーションの`aria-current`を連動
- カレンダーと健康記録で選択日を共有し、健康記録側の日付変更をカレンダーの表示月へ反映
- 前日、次日、今日、日付inputによる日付移動に対応し、ローカル日付基準で処理
- 日付別の`WeightRecord`を追加し、1日につき1件を管理
- localStorageキーは`hootoDay.weightRecords`
- `{ version: 1, records: WeightRecord[] }`形式で予定、日記、テーマとは別に保存
- 体重記録の新規保存、同一日付の上書き編集、確認付き削除に対応
- 体重は20kg以上300kg以下で検証し、保存時に小数第1位へ正規化
- 画面表示は今後の平均・グラフとの統一を考慮し、常に小数第1位まで表示
- 任意メモは最大200文字で、前後の空白を除去して保存
- 保存データ全体が不正な場合は空配列へ戻し、一部項目だけ不正な場合は不正項目だけを除外
- 同じ日付が複数ある場合は`updatedAt`が新しい項目を採用し、同時刻の場合は後の項目を採用
- localStorageの読み書き失敗時もReact state上の操作を可能な限り継続
- 睡眠、食事、運動、体調メモは未実装
- 最新体重、目標体重、標準体重、美容体重、期間平均、グラフは後続Phaseで実装予定
- 本人専用の`HealthProfile`型を追加し、身長、生年月日、計算用の性別、目標体重、更新日時を管理
- 健康プロフィールは1件のみとし、localStorageキー`hootoDay.healthProfile`へ`{ version: 1, profile: HealthProfile | null }`形式で保存
- `profile: null`を有効な未設定状態として扱い、初期サンプルは使用しない
- 年齢は保存せず、生年月日と現在のローカル日付から表示時に計算
- 全項目を任意入力としつつ、すべて未設定の状態は通常保存せず、保存済み内容を全消去する場合は削除操作を使用
- 身長は50.0～250.0cm、目標体重は20.0～300.0kgで検証し、数値は小数第1位へ正規化
- 生年月日は1900年1月1日から現在のローカル日付まで、計算用の性別は女性、男性、未設定に限定
- 健康プロフィールの新規保存、1件の上書き編集、確認付き削除に対応し、削除時も体重記録など既存データは維持
- 不正JSON、version不正、プロフィール不正は未設定へ安全に戻し、localStorage利用不可時もReact state上の操作を継続
- BMI、標準体重、美容体重、目標との差、基礎代謝、運動消費カロリー等の計算は未実装
- 既存の体重記録実装と保存形式は変更していない
- 狭いウィンドウ幅で健康記録カードが右側へはみ出す問題を実機確認
- 固定幅、`min-width`、grid・flex子要素の幅指定を見直し
- 健康記録ルートと各カードを親幅以内へ収めるレスポンシブ修正を実施
- 狭い画面では日付欄と健康記録カードを縦並びへ変更
- 健康プロフィールdialogと体重dialogもviewport内へ収まるよう幅とスクロール領域を確認・調整
- 実際のChromeでは`(max-width: 820px)`がtrueでも、PC用`.sidebar`と2列gridの指定が維持される問題を確認
- 原因は、独立した狭幅用ナビゲーションがなく、同じ`.sidebar`を上部ナビ風に変形するCSS構造だったこと
- 820px以下ではPC用`.sidebar`を非表示、独立した上部ナビゲーションを表示し、`.app-body`を明示的な1列gridへ変更
- Windows表示倍率やChromeズームは直接原因ではないことを確認
- 820px以下のナビゲーション切り替え修正後も、ウィンドウ幅によって共通レイアウトやカレンダーが固定キャンバス状にはみ出して見える問題を確認
- `.app-shell`、header、`.app-body`、sidebar、`.main-content`、カレンダーの幅責務を見直し
- viewport幅を持つ外側コンテナとして`.app-shell`を`width: 100%`、`max-width: 100%`へ変更し、1320px固定幅と中央寄せを廃止
- header、body、sidebar、main、カレンダー、健康記録へ親幅内に収まる幅と`min-width: 0`を連続して設定
- `body`の`overflow-x: hidden`で切れを隠さず、各コンテナの幅計算を修正して横超過の根本解消を図った
- 各ブレークポイントで横スクロールがないことを再確認する方針
- 状態は「完了」

#### Phase 4A完了記録

- 実機確認日：2026年7月17日
- 健康記録画面とカレンダーを相互に切り替えられることを確認
- 両画面での選択日共有と、前日、次日、今日への日付移動を確認
- 体重記録の新規保存、編集、対象日だけの削除、再読み込み後の維持を確認
- 体重の20.0～300.0kgの入力範囲と、小数第1位への正規化を確認
- 健康プロフィールの新規保存、編集、プロフィールだけの削除を確認
- 生年月日から表示時に計算する年齢が正しいことを確認
- 健康プロフィール削除後も体重記録が維持されることを確認
- ライト・ダークテーマで健康記録画面と各入力dialogを確認
- 体重記録は`hootoDay.weightRecords`、健康プロフィールは`hootoDay.healthProfile`の別localStorageキーで管理
- 狭幅時に健康記録カードが右側へはみ出す問題を修正
- 820px以下でPCサイドバー非表示、上部ナビゲーション表示、`.app-body`の1列化を確認
- `.app-shell`の固定最大幅と中央寄せを廃止
- header、body、sidebar、main、カレンダー、健康記録を含む共通レイアウトがviewport幅へ追従することを確認
- カレンダーと健康記録の両画面で横切れと横スクロールが解消したことを確認
- 睡眠、食事、運動、体調メモは未実装
- BMI、標準体重、美容体重、目標との差、平均体重、グラフ、基礎代謝、消費カロリー等は未実装
- Phase 4Aを完了とし、次はPhase 4B「体重集計・目標・標準体重・美容体重・グラフ」へ進む

### Phase 4B（完了）

- 健康記録内に「日付別記録」と「体重まとめ」の表示切り替えを追加
- 保存済み`WeightRecord[]`と`HealthProfile`から表示時に計算し、保存データ形式とlocalStorageキーは変更していない
- 最新体重は`updatedAt`ではなく記録日`date`が最も新しい記録を採用
- 最新記録日、前回記録との差、目標体重との差、標準体重との差、美容体重との差を表示
- BMIは`体重kg ÷ 身長m²`で計算し、小数第1位まで表示
- 標準体重は`身長m² × 22`で計算し、BMI 22を基準にした参考値として表示
- 美容体重（参考）は`身長m² × 20`で計算し、医学的な正式基準ではないBMI 20相当の参考値と明記
- 目標体重は健康プロフィールの値を使用し、未設定時はプロフィール設定への入口を表示
- 集計基準日は最新の体重記録日とし、記録がない場合は現在のローカル日付を使用
- 7日平均は基準日を含む過去7日間、30日平均は基準日を含む過去30日間を対象
- 6か月平均は基準日から6か月前、1年平均は基準日から1年前を開始日とし、月末とうるう日をローカル日付で安全に補正
- 各平均は記録のない日を0として含めず、単純平均、対象記録数、対象期間を表示
- グラフ期間は7日、30日、6か月、1年、全期間で切り替え、初期値は30日
- 追加ライブラリを使わず、親幅へ追従するSVG折れ線グラフを実装
- グラフへ全記録点、折れ線、縦軸、間引いた日付ラベル、最新点、最小・最大・最新値の数値概要を表示
- 目標体重設定時は「目標」と明記した水平補助線を表示
- 記録0件は空状態、1件や同一体重だけの記録でも0除算せず表示できる構造
- BMI、標準体重、美容体重と画面全体が医療診断ではないことを明記
- 状態は「完了」

#### Phase 4B完了記録

- 実機確認日：2026年7月17日
- 健康記録内で「日付別記録」と「体重まとめ」を切り替えられることを確認
- 最新体重、前回記録との差、目標体重との差を確認
- BMI、標準体重、美容体重（参考）の計算結果が既知の値と一致することを確認
- 美容体重はBMI 20相当の非医学的な参考値として正式採用
- 7日、30日、6か月、1年の平均体重、記録数、対象期間を確認
- 日付の異なる複数記録でSVG折れ線グラフを確認
- 7日、30日、6か月、1年、全期間のグラフ切り替えを確認
- 目標体重設定時の水平補助線を確認
- ライト・ダークテーマで概要カード、平均、グラフの表示を確認
- 保存済み体重記録と健康プロフィールを変更せず、計算結果は表示時に算出
- グラフを含め追加ライブラリを使用していない
- BMI、標準体重、美容体重が参考値であり、医療診断ではない旨の表示を確認
- Phase 4Bを完了とし、次はPhase 4C「睡眠記録」へ進む

### Phase 4C（完了）

- 起床した日を記録日とする、1日1件の日付別睡眠記録を追加
- 就寝時刻と起床時刻を必須とし、起床時刻が就寝時刻より早い場合は翌日として日またぎ計算
- 同一時刻、総就床時間30分未満、18時間超は保存不可
- 途中覚醒を複数登録でき、「時刻だけ」と「時間範囲」の2種類に対応
- 時刻だけの途中覚醒は初期15分、5～180分の範囲で差し引き時間を変更可能
- 時間範囲の途中覚醒は覚醒開始から再入眠までの実時間を差し引く
- 途中覚醒は睡眠区間内に限定し、同一時刻、範囲同士、時刻と範囲の重複を保存不可
- 総就床時間、途中覚醒合計、実睡眠時間を分単位で自動計算し、dialog内で即時プレビュー
- 実睡眠時間は`総就床時間 - 途中覚醒合計`とし、0分以下は保存不可
- localStorageキーは`hootoDay.sleepRecords`
- `{ version: 1, records: SleepRecord[] }`形式で体重、プロフィール、予定、日記、テーマとは別管理
- 計算値`totalInBedMinutes`、`awakeMinutes`、`sleepMinutes`を保存し、読み込み時に入力値から再計算して正常化
- 空配列を有効状態とし、初期サンプルは使用しない
- 不正JSON、version不正、records非配列は空配列、不正な記録だけを除外
- 同日重複は`updatedAt`が新しい記録を優先し、同時刻は後の記録を優先
- 睡眠記録の新規保存、同日上書き編集、確認付き削除、再読み込み復元に対応
- 睡眠dialogはbackdropクリックでは閉じず、保存、キャンセル、閉じる、Escape、削除完了時のみ終了
- 睡眠時間は本人入力による概算であり、医療機器による測定値ではないことを表示
- 睡眠の一覧、週間・月間平均、グラフ、評価は後続Phaseで実装予定
- 状態は「完了」

#### Phase 4C完了記録

- 実機確認日：2026年7月17日
- 00:00就寝、07:00起床で実睡眠時間7時間を確認
- 時刻だけの途中覚醒を02:00と05:00に各15分登録し、合計30分差し引いた実睡眠時間6時間30分を確認
- 02:00～03:30の時間範囲を1時間30分として差し引くことを確認
- 23:00就寝、07:00起床の日またぎ計算を確認
- 保存した睡眠記録が再読み込み後も維持されることを確認
- 睡眠記録削除後、再読み込みしても復活しないことを確認
- 睡眠記録は起床日を記録日とする方式を正式採用
- 途中覚醒は「時刻だけ」と「時間範囲」の2方式を正式採用
- 保存した計算値は読み込み時に就寝、起床、途中覚醒から再計算して正常化
- 睡眠時間は本人入力による概算値として扱う
- 睡眠の週間・月間平均、グラフ、評価は未実装
- Phase 4Cを完了とし、次はPhase 4D「食事記録」へ進む

### Phase 4C-2（完了）

- 健康記録画面へ「睡眠まとめ」タブを追加し、「日付別記録」「体重まとめ」と同じ画面内stateで切り替える
- 保存済みの`SleepRecord[]`を参照して表示時に集計し、`hootoDay.sleepRecords`のキー、version 1形式、保存内容は変更しない
- 集計基準日は最新の睡眠記録日とし、記録がない場合は現在のローカル日付を使用する
- 最新の実睡眠時間、記録日、就寝・起床時刻、総就床時間、途中覚醒回数・合計時間、前回記録との差を表示する
- 7日、30日、6か月、1年について、実睡眠時間、総就床時間、途中覚醒時間、途中覚醒回数、記録数、対象期間の平均を表示する
- 記録のない日は平均へ含めず、記録がない期間は`0時間`ではなく未記録表示とする
- 平均就寝・起床時刻は、24時間を円として扱う円周平均で計算する。深夜0時前後を連続した時刻として扱い、単純な時刻平均による昼へのずれを防ぐ
- SVGによる実睡眠時間の折れ線グラフを追加し、7日、30日、6か月、1年、全期間を切り替え可能とする。初期期間は30日
- グラフは全データ点を維持し、日付ラベルのみ必要に応じて間引く。最新点、平均、最短、最長、最新値、記録数を数値でも確認できる
- 記録0件、1件、同一睡眠時間だけの場合も安全に表示できる構造とする
- 選択期間の途中覚醒について、平均時間、最大時間、平均回数、最大回数を表示する
- メモ付き睡眠記録の総数と、日付が新しい最大3件の概要を表示する
- SVGへ`title`と説明用`aria-label`を付け、グラフだけに情報を依存しない
- 本人入力による概算記録であり、医療機器による測定、医療診断、睡眠状態の評価ではない旨を表示する
- 追加ライブラリは使用しない
- 状態は「完了」

#### Phase 4C-2完了記録

- 実機確認日：2026年7月17日
- 日付別記録、体重まとめ、睡眠まとめの切り替えを確認
- 複数の睡眠記録でSVG折れ線グラフを確認
- 7日、30日、6か月、1年、全期間のグラフ期間切り替えを確認
- 最新の実睡眠時間、記録日、就寝・起床時刻、総就床時間、途中覚醒、前回記録との差を確認
- 7日、30日、6か月、1年の期間平均と、未記録日を平均へ含めない動作を確認
- 平均就寝時刻と平均起床時刻を確認
- 23時台と0時台を含む平均就寝時刻が、正午付近ではなく自然な深夜時刻になることを確認
- 平均時刻の計算方法として、24時間を円として扱う円周平均を正式採用
- 平均・最大の途中覚醒時間と途中覚醒回数の表示を確認
- メモ付き記録数と最新3件までの概要表示を確認
- ライト・ダークテーマで睡眠まとめとグラフを確認
- `hootoDay.sleepRecords`、version 1形式、保存済みの睡眠記録は変更していない
- 睡眠時間と集計値は本人入力による概算であり、医療機器による測定や医療診断、睡眠状態の評価ではない
- 次はHealth UI Phaseとして、健康プロフィールを設定へ移動し、各健康カードから共通日付を変更できるUIへ整理する
- Health UI Phase完了後にPhase 4D「食事記録」へ進む

### Health UI Phase（完了）

- 実装時期はPhase 4C-2の直後とする
- 健康プロフィールを健康記録画面から設定へ移動する
- 設定ダイアログに健康プロフィール編集への入口を追加する
- 健康記録画面上部の大きな日付操作欄を縮小または撤去する
- 体重、睡眠、今後追加する食事・運動・体調の各カードへコンパクトな日付操作を配置する
- すべての健康カードは同じ共通選択日を操作し、カードごとに別の日付stateを持たない
- 体重まとめと睡眠まとめの集計基準日は従来どおり最新記録日とし、共通選択日の影響を受けない
- Health UI Phaseでは健康記録の保存形式や既存データを変更しない
- 健康プロフィールの大きなカードを日付別健康記録画面から削除し、設定ダイアログへ表示入口を移動
- 設定ダイアログを「テーマ」と「健康プロフィール」の2セクションへ整理
- 健康プロフィールの入力・保存・削除には既存の`HealthProfileDialog`を再利用
- ネイティブmodal同士の競合を避けるため、設定からプロフィール編集へ移る際は設定ダイアログを閉じてからプロフィールダイアログを開く
- 健康記録上部の大きな日付カードを、共通選択日と直接入力・前日・次日・今日を備えた小型ツールバーへ変更
- 共通日付操作コンポーネント`HealthDateNavigator`を追加し、上部・体重カード・睡眠カードで再利用
- 体重カードと睡眠カードの日付操作は、Appが保持する同じ`selectedDate`を変更し、カードごとの日付stateは作らない
- カード内の日付は現在年なら「7月17日」、現在年と異なる場合は「2027年1月1日」の形式で表示し、読み上げと`title`では常に年月日を示す
- 体重まとめ・睡眠まとめの集計基準日は従来どおり最新記録日であり、共通選択日の影響を受けない
- 将来の食事・運動・体調カードでも`HealthDateNavigator`を再利用する方針
- `hootoDay.healthProfile`、`hootoDay.weightRecords`、`hootoDay.sleepRecords`と各version 1形式は変更していない
- 状態は「完了」
- 完了後はPhase 4D「食事記録」へ進む

#### Health UI Phase完了記録

- 実機確認日：2026年7月17日
- 健康プロフィールが日付別健康記録画面から設定へ移動したことを確認
- 設定ダイアログの健康プロフィール概要と、設定済みプロフィールの編集入口を確認
- 既存の`HealthProfileDialog`による編集・保存・削除を確認
- 設定ダイアログを閉じてから`HealthProfileDialog`を開き、2つのネイティブmodalが同時に開かないことを確認
- 健康記録上部の日付操作が小型化されていることを確認
- 体重カードと睡眠カードのコンパクトな日付操作を確認
- 体重カードまたは睡眠カードから日付を変更すると、上部・体重・睡眠が同じ日付へ同時に切り替わることを確認
- カード別の日付stateを持たず、Appの`selectedDate`を共有する構造を正式採用
- カレンダーと健康記録の選択日共有を維持
- 体重まとめと睡眠まとめに共通日付ナビゲーションを表示せず、集計基準日は最新記録日のままであることを確認
- `hootoDay.healthProfile`、`hootoDay.weightRecords`、`hootoDay.sleepRecords`と各version 1形式は変更していない
- 将来の食事・運動・体調カードにも`HealthDateNavigator`を再利用する
- Health UI Phaseを完了とし、次はPhase 4D「食事記録」へ進む

### Phase 4D（完了）

- 共通選択日ごとに1日1件の食事記録を保存する日付別食事記録を追加
- `MealRecord`は日付、朝食、昼食、夕食、間食、更新日時を持つ
- 朝食、昼食、夕食、間食は自由記述のプレーンテキストとし、改行を維持する
- 各食事項目は任意、最大1000文字。前後の空白を除去し、4項目すべてが空欄または空白・改行だけの場合は保存しない
- localStorageキーは`hootoDay.mealRecords`
- 保存形式は`{ version: 1, records: MealRecord[] }`のversion付きラッパー形式
- 初期サンプルは使用せず、保存データがない場合と空配列は食事記録0件の有効な状態として扱う
- 食事記録の新規保存、同日上書き編集、対象日だけの確認付き削除に対応
- 食事カードへ`HealthDateNavigator`のコンパクト表示を追加し、上部・体重・睡眠と同じAppの`selectedDate`を操作する
- 食事カードでは空欄の食事項目を省略し、入力済み項目だけを各3行まで概要表示する。全文は入力ダイアログで確認する
- 保存本文は表示用に書き換えず、将来の期間指定TXT・Markdown出力で利用しやすいプレーンテキストのまま保持する
- 食事入力ダイアログは保存、キャンセル、閉じる、Escape、削除完了で閉じ、backdropクリックでは閉じない
- 読み込み時はunknownからラッパー、version、配列、日付、各文字列、文字数、更新日時を検証する
- JSON破損、version不正、records非配列は空配列とし、一部の不正記録だけを除外する
- 同日重複は`updatedAt`が新しい記録を優先し、同時刻の場合は配列で後にある記録を優先する
- localStorageの読み書きは`try/catch`で保護し、利用できない場合もReact state上の操作を継続する。食事本文はconsoleへ出力しない
- カロリー・栄養計算、食材解析、写真、時刻、食事評価、食事まとめページは未実装
- 状態は「完了」
- 次はPhase 4E「運動記録」へ進む

#### Phase 4D完了記録

- 実機確認日：2026年7月17日
- 朝食、昼食、夕食、間食の自由記述保存を確認
- 改行を含むプレーンテキスト本文の保存、再読み込み維持、カード表示を確認
- 食事カードでは入力済み項目だけを表示し、空欄項目を表示しない方針を正式採用
- 各食事項目の長文が最大3行程度で省略され、全文を編集ダイアログで確認できることを確認
- 保存済み記録の編集、対象日だけの削除、削除後の再読み込みで非復活を確認
- 食事カードから日付を変更した際に、上部・体重・睡眠・食事が同じ共通選択日へ切り替わることを確認
- 朝食、昼食、夕食、間食の4項目すべてが空欄または空白だけの場合は保存しない
- localStorageキー`hootoDay.mealRecords`とversion 1ラッパー形式を正式採用
- 将来の期間指定TXT・Markdown出力を前提とし、食事内容をHTML等へ変換せず改行を維持したプレーンテキストで保存する
- カロリー計算、栄養計算、食事評価、食事まとめページは未実装
- Phase 4Dを完了とし、次はPhase 4E「運動記録」へ進む

### Phase 4D-2（完了）

- よく使う食事内容を登録・選択できる食事定型メニューを追加
- `MealTemplate`はID、定型名、使用先、内容、`sortOrder`、作成日時、更新日時を持つ
- 使用先は朝食、昼食、夕食、間食、どこでも使用の5種類
- localStorageキーは`hootoDay.mealTemplates`、保存形式は`{ version: 1, templates: MealTemplate[] }`
- 食事記録の`hootoDay.mealRecords`とは別に保存し、既存の保存形式を変更しない
- 定型メニューの新規作成、同一IDの編集、確認付き削除、上下移動に対応
- 表示順は全定型共通の`sortOrder`で管理し、変更時に0から連番へ正常化して即時保存
- 食事入力欄には対象の食事種類と「どこでも使用」の定型だけを表示
- 選択した定型内容は既存本文を置き換えず、改行して末尾へ追記する
- 追記後に1000文字を超える場合は追加せず、エラーを表示する
- 定型選択だけでは食事記録を保存せず、食事入力の「保存」時だけ`MealRecord`へ反映
- 食事入力と定型管理は同じネイティブdialog内の画面切り替えとし、modalを重ねず入力途中の本文をReact stateに保持
- 読み込み時はunknownからversion、配列、各項目、日時を検証し、一部不正項目を除外、重複IDは新しい`updatedAt`を優先
- localStorageの読み書き失敗時もReact state上の操作を継続し、定型内容や食事本文をconsoleへ出力しない
- 使用回数、お気に入り、自動提案、自動並び替え、栄養・カロリー計算は未実装
- 実機確認日：2026年7月17日
- 定型メニューの新規登録と、再読み込み後も保存内容が維持されることを確認
- 保存済み定型メニューの編集内容が管理一覧と食事入力候補へ反映されることを確認
- 定型メニューの個別削除を確認
- 上下移動による並び替えと、管理一覧・食事入力候補への表示順反映を確認
- 朝食専用定型が朝食欄だけへ表示され、使用先が異なる専用定型は候補へ表示されないことを確認
- 「どこでも使用」の共通定型を対象となる全食事欄へ表示する方針を確認
- 既存本文を消さず、改行して末尾へ定型内容を追記できることを確認
- 定型管理画面へ移動して戻っても、未保存の食事本文が維持されることを確認
- 入力画面と管理画面を同じネイティブdialog内で切り替え、native dialogを同時に複数開かない構造を正式採用
- `MealRecord`と`MealTemplate`は別のlocalStorageキーとversion 1形式で管理する
- 使用回数、ランキング、自動提案、栄養計算は未実装
- Phase 4D-2を完了とする
- 次はPhase 4E「運動記録」へ進む

### Phase 4E（完了）

- 共通選択日ごとに1日複数件を保存できる日付別運動記録を追加
- `ExerciseSession`はID、日付、運動種類、その他の自由名、運動時間、平均心拍数、METs、計算使用体重、推定消費カロリー、メモ、作成日時、更新日時を持つ
- 初期運動種類はルームランナー、エアバイク、ビートセイバー、ストレッチ、その他
- 初期METsは順に3.5、5.0、5.0、2.3、3.0とし、保存データ外の共通設定表で一元管理する
- METsは運動強度に合わせて1.0～20.0の範囲で編集でき、小数第1位へ正常化する
- 運動時間は1～1440分の整数で保存し、表示時は「1時間30分」等へ変換する
- 平均心拍数は任意の30～250 bpmの整数で記録し、今回の推定カロリー計算には使用しない
- 新規入力時の計算使用体重は同日の体重、次に運動日以前の最新体重を使用し、未来日の体重は自動使用しない
- 計算使用体重は手動変更でき、編集時は保存済みの値を維持する
- 推定消費カロリーは`METs × 体重kg × 運動時間h`を整数kcalへ四捨五入する
- 保存時点のMETs、計算使用体重、推定消費カロリーをスナップショットとして保存し、体重記録の後日変更では自動再計算しない
- 体重が未設定でも運動記録を保存でき、その場合の推定消費カロリーは`null`とする
- 運動カードへ日次合計時間、計算済みセッションの合計推定消費カロリー、セッション数、個別一覧を表示する
- 新規、同一IDでの編集、確認付き個別削除に対応し、同日・他日の他セッションへ影響させない
- localStorageキーは`hootoDay.exerciseSessions`、保存形式は`{ version: 1, sessions: ExerciseSession[] }`
- 読み込み時はunknownからラッパーと各項目を検証し、不正セッションだけを除外、重複IDは新しい`updatedAt`を優先する
- 心拍数、年齢、性別、身長は今回の標準METs計算へ使用しない
- 推定値は本人入力による概算であり、医療機器や運動機器の測定値・医療診断ではない
- 運動種類管理、使用種類の追加・並び替え、期間集計、運動まとめ、グラフは未実装
- 実機確認日：2026年7月17日
- ルームランナー、エアバイク、ビートセイバー、ストレッチ、その他の5種類の運動登録を確認
- 「その他」の自由入力名が保存・表示されることを確認
- 同じ日へ複数セッションを登録し、個別に保持されることを確認
- 日次合計運動時間と、計算可能なセッションだけを対象とする合計推定消費カロリーを確認
- METs 5.0、体重60.0kg、運動時間30分で推定150kcalとなることを確認
- 運動日と同日の体重、同日がない場合は運動日以前の最新体重が自動選択されることを確認
- 運動日より未来の体重記録を自動使用しないことを確認
- 平均心拍数は記録専用とし、今回の推定カロリー計算へ使用しない
- 保存時点の体重、METs、推定消費カロリーをスナップショットとして保存する方針を正式採用
- 新規、同一IDでの編集、対象セッションだけの削除、再読み込み後の維持と非復活を確認
- 運動カードのHealthDateNavigatorと、上部・体重・睡眠・食事・カレンダーの共通選択日連動を確認
- 推定消費カロリーは本人入力による概算であり、医療機器や運動機器による測定値ではない
- 運動種類管理、期間集計、運動まとめ、グラフは未実装
- 運動まとめは後続の小Phaseとして追加可能とする
- Phase 4Eを完了とする
- 次はPhase 4F「体調メモ」へ進む

### Phase 4F（完了）

- 共通選択日ごとに1日1件を保存する日付別体調メモを追加
- `DailyConditionRecord`は日付、全体の体調、膝、腰、生理・周期メモ、気になること、自由メモ、更新日時を持つ
- 全体の体調は未設定、良い、普通、悪いの4状態
- 膝と腰は未設定、問題なし、少し気になる、痛い、強く痛いの5状態
- 状態の内部値と表示名は型安全な共通設定表で一元管理する
- 生理・周期メモと気になることは各最大500文字、自由メモは最大1000文字の改行可能なプレーンテキスト
- 文章は保存時に前後空白だけを除去し、本文内の改行を維持する
- すべての状態が未設定かつ文章欄が空白だけの場合は保存しない
- 新規、同日上書き編集、確認付きの対象日削除に対応
- 体調カードでは未設定状態と空欄文章を表示せず、文章は最大3行程度で省略する
- 体調カードへHealthDateNavigatorを追加し、体重、睡眠、食事、運動、カレンダーと同じ`selectedDate`を共有する
- localStorageキーは`hootoDay.conditionRecords`、保存形式は`{ version: 1, records: DailyConditionRecord[] }`
- 空配列を有効な0件状態として扱い、初期サンプルは使用しない
- 読み込み時はunknownからラッパーと各項目を検証し、不正記録だけを除外、同日重複は新しい`updatedAt`を優先する
- localStorageの読み書き失敗時もReact state上の操作を継続し、体調内容や生理・周期メモをconsoleへ出力しない
- 本人の振り返り用記録とし、自動診断、病名推定、医療緊急度判定は行わない
- 期間集計、体調グラフ、生理周期予測は未実装
- Phase 4の主要な日付別入力項目が一通り揃う
- 実機確認日：2026年7月17日
- 全体の体調、膝、腰の各状態を単独または組み合わせて保存できることを確認
- 生理・周期メモだけでも保存できることを確認
- 気になることと自由メモの保存、改行維持、カード概要表示を確認
- 全状態が未設定かつ全文章欄が空欄・空白だけの場合は保存できないことを確認
- エラー表示後に入力を修正して保存できることを確認
- 編集時に一部の状態を未設定へ戻し、一部の文章項目を空欄へ戻せることを確認
- 未設定または空欄へ戻した項目が体調カードから消えることを確認
- 新規、同日上書き編集、対象日だけの削除、再読み込み後の維持と非復活を確認
- 体調カードのHealthDateNavigatorと、上部・体重・睡眠・食事・運動・カレンダーの共通`selectedDate`連動を確認
- 体調記録は本人の振り返り用とし、自動診断、病名推定、医療緊急度判定を行わない
- Phase 4の主要な日付別入力項目が一通り完成したことを記録
- Phase 4Fを完了とする
- 次工程は健康記録全体の確認、運動まとめ、体調まとめ、または出力機能の優先順位を整理する

### Health Review Phase（完了）

- Phase 4で実装した健康プロフィール、体重、睡眠、食事、食事定型、運動、体調の入力・表示・保存を統合確認
- 日付別健康カードの正式順序を、体重、睡眠、食事、運動、体調メモの順に統一
- 日付別画面を圧迫していた未実装の「今後追加予定」カードを削除し、将来機能はPROJECT_NOTES.mdで管理する
- 各カードは左側に共通見出し、右側または狭幅時の下段に共通`HealthDateNavigator`、本文下部に主要操作を配置する
- 未記録時は「この日の○○記録はありません」と「○○を記録」ボタンを共通の空状態で表示し、0kg、0時間、0kcal等へ置き換えない
- 記録済みカードは日常的な概要だけを表示し、食事・運動・体調に加えて体重・睡眠メモも最大3行程度へ省略、全文は各dialogで確認する
- 1日1件の体重、睡眠、食事、体調は記録済み時に「編集」、1日複数件の運動は「運動を追加」と各セッションの「編集」を使用する
- 全健康カードはAppの同じ`selectedDate`を操作し、カード別の日付stateを持たない
- カード内日付は共通`HealthDateNavigator`で、現在年は「7月17日」、異なる年は「2027年1月1日」形式に統一
- 日付別画面では前日・次日・今日・日付直接選択を共有し、まとめ画面の集計基準日は従来どおり最新記録日とする
- 健康プロフィールは設定dialog内で管理し、日付別健康画面へ大きなプロフィールカードを戻さない
- 体重まとめのプロフィール編集入口と、設定dialogを閉じてからHealthProfileDialogを開く構造を維持
- 健康入力dialogは保存、キャンセル、右上の閉じる、Escape、削除完了で閉じ、backdropクリックでは閉じない安全方針を確認
- ThemeSettingsだけは従来どおりbackdropクリックで閉じる
- localStorageキーは`hootoDay.theme`、`hootoDay.events`、`hootoDay.dayMemos`、`hootoDay.healthProfile`、`hootoDay.weightRecords`、`hootoDay.sleepRecords`、`hootoDay.mealRecords`、`hootoDay.mealTemplates`、`hootoDay.exerciseSessions`、`hootoDay.conditionRecords`
- テーマ以外の保存データはversion 1で管理し、各キーのラッパー形式、空配列または`profile: null`の有効状態、unknownからの検証、try/catchを確認
- 各データは独立したlocalStorageキーとReact stateで管理され、1種類の保存・編集・削除が他種類を変更しない構造を確認
- 保存失敗時もReact state上の操作を継続し、日記本文、食事、運動、体調、生理・周期メモ等をconsoleへ出力しない
- 次のHealth Export Phaseで、日付ごとに日記・メモ、体重とメモ、睡眠と途中覚醒、食事、運動セッション、体調の全保存項目を取得可能であることを確認
- TXT・Markdown出力に必要な保存項目の不足は現時点で確認されなかったため、型と保存形式は変更しない
- 920px以下の1列カード、820px以下の上部ナビ、560px以下のフォーム・日付操作、320px以上のviewport追従を維持
- ライトは緑主体、ダークはオレンジ主体の既存CSS変数を使用し、状態表示は色だけでなく文言を併記する
- 見出し、button、label、aria-label、aria-pressed、aria-invalid、aria-describedby、role="alert"、aria-live、dialog見出し関連付けを確認
- 実機確認日：2026年7月17日
- 健康記録の正式な5カード順、各カードの見出し、日付操作、空状態、ボタン文言の統一を実機確認
- 体重・睡眠メモの3行省略と、不要な「今後追加予定」カードの削除を正式採用
- すべての健康カードが同じ`selectedDate`を共有し、健康プロフィールを設定で管理する構成を確認
- 健康入力dialogの終了条件、localStorageキーとversion 1形式、健康データ間の独立性を確認
- Health Export Phaseに必要な保存項目が揃っており、保存形式の変更が不要であることを確認
- ライト・ダークテーマ、レスポンシブ表示、アクセシビリティを確認し、Health Review Phaseを完了とする
- 次はHealth Export Phase「健康記録TXT・Markdown出力」へ進む

### Health Calendar Phase（完了）

- 既存の月間カレンダーへ、健康記録が1種類以上ある日を示す共通マークを追加
- 日記・メモと健康記録は日付セル右上の共通アイコン領域へ表示し、左に健康記録の`HeartbeatIcon`、右に日記・メモの`PencilSimpleIcon`を配置
- 健康記録または日記・メモの片方だけがある日は該当アイコンだけを表示し、右下の健康記録表示は使用しない
- 健康記録と日記・メモは色と形で区別する
- 健康記録の存在判定は、体重、睡眠、食事、運動、体調の既存stateを対象とする
- `healthSummary.ts`へ日付別健康概要、健康記録日の集合、運動セッション抽出、概要文生成を集約
- 選択日詳細へ「健康記録」セクションを追加し、保存されている項目だけを最大5行で表示
- 体重は小数第1位、睡眠は実睡眠時間と途中覚醒、食事は入力済み項目数、運動は件数・時間・計算可能な推定消費カロリー、体調は主要な設定済み状態を概要表示
- 健康記録がない日も空状態と「健康記録を開く」を表示し、同じ日付の日付別健康画面から新規入力できる
- 「健康記録を開く」で既存`selectedDate`を維持したまま健康記録の日付別画面へ移動する
- 日記・メモの操作文言を、曖昧な「記録を開く」から「日記・メモを開く」へ変更
- カレンダーと健康記録はAppが保持する同じ健康stateを参照し、保存・編集・削除を再読み込みなしで反映する
- 日付セルの`aria-label`へ「メモあり」「健康記録あり」を含め、健康概要は見出しとリストで読み上げ可能にした
- ライト・ダークテーマの既存CSS変数、共通レスポンシブ、カレンダー寸法を維持
- 保存形式、localStorageキー、versionは変更していない
- 健康記録専用カレンダーは実装せず、実際の使用後に必要であれば小型健康カレンダーを検討する
- 実機確認日：2026年7月17日
- カレンダー上で健康記録日の表示を確認
- 健康記録と日記・メモのアイコンを右上へ横並び表示し、左に`HeartbeatIcon`、右に`PencilSimpleIcon`を配置
- 健康だけ、日記だけ、両方ありの表示と、日付数字・予定・今日表示・選択枠へ重ならないことを確認
- 選択日詳細の健康概要と、「健康記録を開く」による同じ`selectedDate`の日付別健康画面への移動を確認
- 日記用ボタンは「日記・メモを開く」とし、健康記録の新規・編集・削除がカレンダーへ即時反映されることを確認
- localStorageキーとversionは変更していない
- 専用健康カレンダーは未実装とし、必要になった場合のみ将来検討する
- Health Calendar Phaseを完了とする
- 次工程はDaily Achievement Phaseとし、完了後にHealth Export Phaseへ進む

### Daily Achievement Phase（完了）

- 1日1件、最大120文字の「できたこと」を日記・メモとは別データとして保存
- `hootoDay.dailyAchievements`をversion 1ラッパー形式で管理し、新規・編集・削除と再読み込み後の維持に対応
- `hootoDay.monthlyAchievementSelections`をversion 1ラッパー形式で管理し、月ごとに1件を「いちばん頑張ったこと」として選択
- `MonthlyAchievementSelection`は本文を複製せず`selectedDate`で元の`DailyAchievement`を参照する
- 元本文を編集すると月のベスト表示も更新され、元記録を削除すると対応する月選択を解除する
- 一覧から月のベストを選択・変更・解除でき、王冠には正式exportの`CrownIcon`を使用
- 独立した月のベストカードを廃止し、「月間カレンダー」の固定見出しを表示年月へ変更
- 月選択と月のベストを同じコンパクトなヘッダー行へ配置し、左に前月・表示年月・次月、右に月のベスト操作を表示
- 未選択時だけ「今月のできたことを見る」を表示し、説明文はヘッダーへ表示しない
- 選択済み時は王冠、本文、選択日だけを1行表示し、補助的な「見る」ボタンやリンクは追加しない
- 選択済み表示全体を1つのbuttonとし、押すと対象月の一覧dialogを開いて変更・解除できる
- 画面最上部のAppHeaderを廃止し、重複していたHootoDayロゴ・名称と独立した「今日」ボタンを削除
- 「今日」操作をカレンダー年月操作へ移し、前月・表示年月・次月・今日・月のベスト操作を同じ領域へ統合
- AppHeader分の高さと余白を削除してメインコンテンツを上へ整理し、今日へ戻る既存動作は維持
- PC版はアイコン中心の細いサイドバーとし、通常時はラベルを隠してhover・focus時に右側へツールチップ表示
- PCナビは`aria-label`と`aria-current`を維持し、820px以下では従来の文字付きモバイルナビを使用
- サイドバー縮小分をメイン領域へ割り当て、カレンダーと選択日詳細の表示幅を拡張
- 月間カレンダーは月曜始まりを正式採用し、曜日順を月・火・水・木・金・土・日へ変更
- 月初オフセットを月曜基準へ変更し、土曜は青緑、日曜は赤の既存表示を維持
- 曜日行を日付グリッドのカードから分離し、月～日の各曜日を独立した小さな角丸ブロックとして表示
- 日付グリッドは曜日ブロックの下へ、セル背景と罫線を持つ単一グリッドとして表示
- 曜日行と日付グリッドを同じ親幅・gapなしの7列gridへ統一し、各曜日名の中心を対応する日付列の中心へ一致
- 日付グリッドを覆っていた白背景・padding・大きな角丸・影・外枠のカード要素を廃止
- ページ背景上へ曜日行と日付グリッドを直接配置し、日付セルの背景・罫線と最小限の外周だけを維持
- 二重カードに見えない軽い構成とし、曜日の視覚的な隙間は各列内の角丸ラベルで表現
- 当月外セルの背景をページ背景より一段濃くし、日付文字も当月より控えめな範囲で読みやすく調整
- 日付グリッド外周を専用テーマ変数で少し濃くし、レイアウトへ影響しない内向き2px outlineへ変更
- 内側のセル罫線は細いまま維持し、二重カードや強いshadowは追加しない
- 外周調整後も曜日と日付列の左右端・列幅・中心位置の一致を維持
- カレンダー全体の柔らかさを保ちながら、ページ背景・曜日・土日・外周へ適度なメリハリを追加
- ライトのページ背景を一段だけ緑味のある色へ調整し、当月平日の白いセルとの差を明確化
- 曜日ブロック専用の背景・枠・文字変数を追加し、ボタン風に強調しすぎない範囲で視認性を向上
- 土曜の淡い青緑と日曜の淡いピンクを一段だけ明確にし、当月外セルは控えめな配色を維持
- 日付グリッド外周は2pxのまま色だけを少し明確にし、内側罫線は濃くしない
- 選択日、今日、予定ラベルの既存の表示優先度は変更しない
- ライトテーマのメイン背景を一段深い低彩度セージグリーンへ変更し、白い当月平日セルとの明度差を強化
- 曜日ブロックはメイン背景より明るい階層を維持し、当月外セルは背景より控えめなグレーグリーンへ微調整
- 左サイドバーの薄いオレンジはアクセントとして維持し、メイン背景との色は入れ替えない
- 当月外、土曜、日曜、選択日、今日の配色方針とダークテーマの既存配色は維持
- 月移動には正式exportの`CaretCircleLeftIcon`と`CaretCircleRightIcon`を使用し、塗りつぶさない通常ウェイトで表示
- 予定、日記、健康記録、今日、選択日の各日付対応は変更しない
- DayDetailsの健康記録を最下部へ移し、初期状態と選択日変更時は閉じる折り畳み表示へ変更
- 閉じた状態でも「記録あり・種類数」または「記録なし」を表示し、開いた中に既存健康概要と「健康記録を開く」を表示
- 健康折り畳みはReact stateだけで管理して保存せず、`aria-expanded`と`aria-controls`を設定
- 狭幅では月選択と月のベストを自然に2行へ折り返す
- DayDetailsへ日記・メモとは独立した「今日のできたこと」を追加し、未記録時の新規入口と記録済み時の編集入口を表示
- カレンダーセルへ新しい存在アイコンは追加していない
- 日記、予定、健康記録、既存localStorageキーとは独立し、保存形式を変更していない
- 実機確認日：2026年7月17日
- 毎日1件のできたことを保存でき、新規・編集・削除と再読み込み後の維持を確認
- 月一覧の表示、月のベストの選択・変更・解除を確認
- 元本文の編集・削除が月のベストへ即時連動することを確認
- 選択済みの月のベストに王冠を表示し、未選択時だけ「今月のできたことを見る」を表示する構成を確認
- 選択済み表示全体から月一覧dialogを開けることを確認し、月のベストを年月操作と同じヘッダーへ正式統合
- AppHeaderを廃止し、「今日」ボタンを年月操作へ移動
- PCサイドバーをアイコン中心の細い表示へ変更し、820px以下のモバイルナビを維持
- 月曜始まりを正式採用し、曜日ブロックを日付グリッドから分離
- 曜日と日付列の位置が完全に一致することを確認し、カレンダー外側の大きなカードを廃止
- 月移動には`CaretCircleLeftIcon`と`CaretCircleRightIcon`を正式採用
- DayDetailsの健康記録を最下部の折り畳み表示へ変更
- ライトテーマ背景に低彩度セージグリーンを採用し、左サイドバーの薄いアイボリー・オレンジ系を維持
- ライト・ダーク・system、レスポンシブ表示、横スクロールがないことを確認
- Daily Achievement Phaseを完了とする
- 次工程はHealth Export Phase「健康記録TXT・Markdown出力」へ進む

### Health Export Phase（完了）

- 既存の「出力・バックアップ」を正式な画面として接続し、日付別記録のTXT・Markdown出力を実装
- 出力対象は日記・メモ、毎日のできたこと、体重、睡眠、食事、運動、体調、月のベスト
- 予定は今回の健康記録出力には含めない
- 期間は1日、基準日を含む過去7日、過去15日、基準日のカレンダー月、任意の期間指定に対応
- 期間指定は両端を含めて最大366日とし、開始日が終了日より後の場合や不正日付は生成しない
- 日付はローカル日付として扱い、年またぎとうるう年に対応
- 日付昇順で、実際に保存されている項目だけを出力し、記録のない日は省略
- 期間内に出力対象が1件もない場合は空状態を表示し、コピーとファイル保存を無効化
- 月のベストは期間に含まれる月を対象とし、DailyAchievementの参照先が有効な場合だけ月順に出力
- TXTはChatGPT等へ貼りやすい日本語ラベル付きプレーンテキストとして生成
- MarkdownはObsidianへ保存しやすい日付h2・記録種類h3・運動セッションh4の見出し構造で生成
- MarkdownをHTMLへ変換せず、生成される文字列をそのままプレビュー
- 生成処理はReactやDOMへ依存しない`healthExport.ts`の純粋関数を中心に分離
- クリップボードコピーと、UTF-8のBlobを使用したTXT・Markdownファイル保存に対応
- ファイル名は1日の場合`HootoDay_YYYY-MM-DD`、期間の場合`HootoDay_開始日_終了日`とする
- プレビューは読み取り専用、折り返し、最大高さ付きの縦スクロール表示
- 出力はAppが管理する既存の各配列をpropsで参照し、HookやlocalStorageの二重読み込みは行わない
- 保存済みデータは読み取り専用で扱い、既存localStorageキーとversion 1形式を変更しない
- 対象データの個別ON/OFF、予定出力、JSONバックアップ・復元は未実装
- 内容は端末内だけで生成し、外部送信、自動アップロード、AI要約、医療判断を行わない
- 健康・日記本文をconsoleへ出力しない
- ライト・ダーク、PCアイコンサイドバー、820px以下のモバイルナビ、320px以上のレスポンシブ表示に対応
- 実機確認日：2026年7月17日
- Markdownの1日出力で、保存済み項目だけが表示されることを確認
- 日記・メモや体調メモ等の改行が維持され、Markdownの見出し構造が崩れないことを確認
- プレビューとクリップボードへコピーした内容が一致し、日本語と改行が維持されることを確認
- Markdownファイル保存、正しいファイル名、UTF-8で文字化けしないこと、プレビューとの内容一致を確認
- TXTの過去7日出力、日付昇順、記録のない日の省略、保存済み項目だけの出力を確認
- 月のベストが該当月分だけ日付ブロックの末尾へ出力されることを確認
- 期間指定で開始日が終了日より後の場合にエラーとなり、修正後は正常なプレビューへ戻ることを確認
- エラー時と空状態ではコピー・ファイル保存を行わないことを確認
- 出力処理は読み取り専用で、既存の保存データ、localStorageキー、versionを変更しない
- 内容は端末内だけで生成し、外部通信、自動送信、自動アップロードを行わない
- Health Export Phaseを完了とする
- 次工程はJSON Backup Phase「JSONバックアップ・復元」へ進む

### JSON Backup Phase（完了）

- HootoDayのテーマ、予定、日記・メモ、健康プロフィール、体重、睡眠、食事、食事定型、運動、体調、毎日のできたこと、月のベスト選択を1つのJSONへバックアップ
- バックアップ全体は`app: "HootoDay"`、`formatVersion: 1`、ISO 8601形式の`createdAt`、全正式データを収める`data`で構成
- localStorageのラッパーを複製せず、Appが保持する最新の正規化済みstateから生成
- 手動バックアップのファイル名は`HootoDay_backup_YYYY-MM-DD_HH-mm-ss.json`、復元直前の自動退避は`HootoDay_before_restore_YYYY-MM-DD_HH-mm-ss.json`
- JSONはUTF-8、2スペースインデント、MIME typeは`application/json;charset=utf-8`とし、端末名やWindowsパス等を含めない
- 復元ファイルは10MBを上限とし、上限超過時は本文を読み込まず復元を無効化
- `unknown`からバックアップ全体を厳格に検証し、他アプリ、未対応version、必須キー欠落、不正な型・日付・enum・数値・文字数を拒否
- 一部だけ不正な場合も不正項目を除外せず、バックアップ全体を拒否して現在データを変更しない
- 1日1件・1月1件データの重複、ID重複、食事定型の並び順不整合、存在しない「できたこと」を参照する月のベスト等を拒否
- 復元前プレビューはファイル名、作成日時、テーマ、健康プロフィールの有無、各データ件数だけを表示し、本文や健康値は表示しない
- ファイル選択と検証だけではlocalStorageやReact stateを変更せず、検証成功時だけ「復元する」を有効化
- 最終確認はネイティブdialogを使用し、キャンセルとEscapeで閉じ、backdropクリックでは閉じない
- 復元実行前に現在stateから自動バックアップのダウンロードを開始し、生成または開始に失敗した場合は復元を中止
- 復元直前に全対象localStorageキーの元値とキー未存在状態をメモリへ退避
- 検証済みデータを既存の各version 1ラッパー形式へ変換して全キーへ保存し、途中失敗時は元値へロールバック
- ロールバック自体の失敗時は、自動保存された復元前バックアップの保管を促す明確なエラーを表示
- localStorageの全保存成功後だけ各Hookの一括置換関数でReact stateを更新し、ページ再読み込みなしでテーマ、カレンダー、健康記録、出力へ即時反映
- 復元成功後は確認dialog、選択ファイル、件数プレビューをクリアし、aria-liveで成功を通知
- バックアップと復元は端末内だけで処理し、外部通信、自動アップロード、本文や健康値のconsole出力を行わない
- 既存の12個のlocalStorageキーと各version 1形式は変更しない
- 実機確認日：2026年7月17日
- 「バックアップを保存」から手動JSONバックアップを保存でき、`HootoDay_backup_YYYY-MM-DD_HH-mm-ss.json`のファイル名を確認
- 保存JSONの`app: "HootoDay"`、`formatVersion: 1`、UTF-8の日本語表示を確認
- 正常なHootoDayバックアップの検証成功と、作成日時、テーマ、健康プロフィール有無、各データ件数のプレビューを確認
- ファイル選択だけでは復元されず、現在データが変化しないことを確認
- 最終確認dialogの内容と、キャンセル時に現在データが変化しないことを確認
- 復元直前に`HootoDay_before_restore_YYYY-MM-DD_HH-mm-ss.json`で現在データが自動退避されることを確認
- 復元成功通知と、復元内容が再読み込みなしでReact stateへ即時反映されることを確認
- ページ再読み込み後も復元内容が維持されることを確認
- 既存localStorageキーとversion 1形式を維持
- 復元は端末内で処理し、外部通信、本文や健康値のconsole出力を行わない
- JSON Backup Phaseを完了とする
- 次工程は運動まとめ・体調まとめ等の優先順位を整理する
