# HootoDay Project Notes

## プロジェクト概要

- プロジェクト名：HootoDay
- 完全な個人利用専用
- スケジューラーと健康記録を統合したアプリ
- 最初はブラウザ版として開発
- 将来はPWA化、iPhone対応、デスクトップアプリ化を検討
- 配布、一般公開、複数ユーザー対応、ストア公開の予定なし

## 主要機能

### 1. スケジューラー

- 月間カレンダー
- 日付ごとの予定
- 開始時刻、終了時刻、終日予定
- 予定カテゴリ
- 短い日記・メモ
- 日記やメモがある日のカレンダー表示

### 2. 健康記録

- 睡眠
- 体重
- 朝食、昼食、夕食、間食
- 運動
- 体調メモ
- 各項目の専用閲覧ページ
- 週、月、半年、年単位の集計
- TXTまたはMarkdown出力
- JSONバックアップと復元

## 初期予定カテゴリ

- 収録
- ライブ
- リハ
- 配信
- 歌枠
- 歯医者
- 整体
- 通院
- おでかけ
- 映
- その他

将来、カテゴリを追加、編集、並び替えできるようにします。「その他」では予定名を自由入力できるようにします。

## 健康記録の概要

### 睡眠

- 就寝時刻と起床時刻から睡眠時間を計算する
- 途中覚醒は複数登録可能とする
- 覚醒時刻だけの場合は、初期値として15分を睡眠時間から差し引く
- 覚醒開始時刻と終了時刻がある場合は、実際の覚醒時間を睡眠時間から差し引く

### 体重

- 最新値、目標値、標準体重、美容体重、期間平均、グラフを将来表示する

### 食事

- 朝食、昼食、夕食、間食の各欄に自由記述で記録する

### 運動

- 1日に複数回登録可能とする
- 運動種類、時間、心拍数、推定消費カロリー、メモを記録する

### 体調

- 全体、膝、腰、生理・周期、気になることを記録する

## 開発方針

- 一度に大規模実装をしない
- Phase単位で実装、確認、記録、commitする
- データを壊す可能性がある変更では、事前にバックアップを用意する
- 自分専用のため複数ユーザー機能は作らない
- 入力の手軽さと見やすさを優先する
- PCとiPhoneでは将来別UIにしてよい
- 通知とiPhoneウィジェットは後期Phaseへ回す
- 健康データの計算値は医療診断ではなく、本人用の概算記録とする

## 予定しているPhase

| Phase | 内容 |
| --- | --- |
| Phase 0 | 初期環境と仕様記録 |
| Phase 1 | アプリ全体の画面枠と月間カレンダーの見た目 |
| Phase 2 | 予定の登録、編集、削除 |
| Phase 3 | 日記・メモ |
| Phase 4 | 健康記録入力 |
| Phase 4A | 健康記録画面、日付別体重記録、健康プロフィール |
| Phase 4B | 体重集計、目標、標準体重、美容体重、グラフ |
| Phase 4C | 睡眠 |
| Phase 4C-2 | 睡眠まとめ、期間平均、平均就寝・起床時刻、睡眠時間グラフ |
| Health UI Phase | 健康プロフィールの設定への移動と健康カードの共通日付UI整理 |
| Phase 4D | 食事 |
| Phase 4E | 運動 |
| Phase 4F | 体調メモ |
| Phase 5 | 各健康項目の専用閲覧ページと集計 |
| Phase 6 | TXT、Markdown、JSONの出力・復元 |
| Phase 7 | PWAと端末間同期 |
| Phase 8 | 通知、iPhoneウィジェット、デスクトップアプリ化 |
| Special Phase | 即売会・在庫記録（実装時期は後で決定） |

## 将来追加機能：即売会・在庫記録

### 機能の目的

- 即売会イベントごとに、商品別の持込数と販売結果を記録する
- 単なる自由記述メモではなく、数量と金額を数値として保存・集計する
- 年2回程度の利用でも、過去イベントとの比較や累計を確認できるようにする
- 完全な個人利用専用機能とする

### イベント単位の記録項目

- イベントID
- イベント名
- 開催日
- 会場
- 全体メモ
- 商品別記録
- 作成日時
- 更新日時

### 商品別の基本項目

- 商品ID
- 商品名
- 持込数
- 売れた数
- サンプル配布数
- 残数
- 単価
- 売上
- 商品別メモ

### 自動計算と入力条件

- 残数 = 持込数 - 売れた数 - サンプル配布数
- 売上 = 売れた数 × 単価
- サンプル配布数は売上に含めず、在庫だけを減らす
- 計算結果が負数になる入力は保存しない

### 集計したい内容

- イベントごとの総持込数
- イベントごとの総販売数
- イベントごとの総サンプル配布数
- イベントごとの総残数
- イベントごとの総売上
- 商品ごとの累計販売数
- 商品ごとの累計サンプル配布数
- 商品ごとのイベント後残数
- 年ごとの販売数と売上
- 前回イベントとの比較
- 過去イベント一覧

### HootoDayとの連携案

- カレンダーへ即売会予定を登録する
- 即売会予定または選択日詳細から「即売会記録を開く」
- 日記・メモ欄には当日の感想を書き、数量と売上は即売会記録へ分離する
- 将来「記録を見る」からイベント別・商品別集計を開く
- 将来TXT、Markdown、CSVなどへ出力できるようにする

### 将来拡張の余地

初期実装の必須項目にはしないが、データ構造上は次の項目を追加できる余地を残す。

- 追加生産数
- 関係者への配布
- 破損・廃棄数
- 通販分
- 自宅に残した在庫
- 商品画像
- 原価と利益

### Special Phase：即売会・在庫記録

- 健康記録や出力機能とは混ぜず、独立したPhaseとして扱う
- 実装時期は後で決定する
- 初期実装はイベント、商品、持込数、販売数、サンプル配布数、残数、単価、売上を対象とする
- 高度な在庫移動、追加生産、原価・利益計算は後期拡張とする
- 既存の日記・予定・健康記録とは別データとして保存する

## 現在の技術構成

`package.json`に記載されている現在のバージョン指定は以下のとおりです。

### dependencies

- React：`^19.2.7`
- React DOM：`^19.2.7`

### devDependencies

- Node.js型定義（`@types/node`）：`^24.13.2`
- React型定義（`@types/react`）：`^19.2.17`
- React DOM型定義（`@types/react-dom`）：`^19.2.3`
- Vite Reactプラグイン（`@vitejs/plugin-react`）：`^6.0.3`
- Oxlint：`^1.71.0`
- TypeScript：`~6.0.2`
- Vite：`^8.1.1`

## 作業履歴

### 2026年7月17日

- Vite React TypeScript初期環境作成
- `npm.cmd install`成功
- Gitリポジトリ初期化
- `npm.cmd run build`成功
- 開発サーバー起動およびHTTP応答確認
- 初期commit前の状態を確認

### Phase 1（完了）

- アプリ基本レイアウト作成
- 月間カレンダー表示
- 前月、次月、今日の移動
- 日付選択
- 固定サンプル予定表示
- メモ存在表示
- 選択日詳細欄
- データ保存や予定・記録の編集機能は未実装
- 主なコンポーネント：`App`、`AppHeader`、`Sidebar`、`Calendar`、`DayDetails`
- 確認項目：PC・スマートフォン幅の表示、月移動、今日への移動、日付選択、サンプル予定・メモ・選択日詳細の連動、キーボード操作、横スクロール、コンソールエラー
- 実画面確認で、PC表示が大きく固定的に見えることを確認
- ウィンドウ幅に応じて全体が段階的に縮むようCSSを調整
- `transform`による全体縮小は使わず、`clamp()`、`minmax()`、可変幅で対応
- 状態は引き続き「実装済み・実機確認待ち」
- Phase 1のcommitは実機確認完了まで行わない
- 実画面確認で、カレンダーセル内の日付が縦中央寄りに見えることを確認
- 日付を各セルの左上固定へ変更
- 予定を日付の下へ縦に並べる構造へ調整
- メモ印はセル右上の配置を維持
- 状態は引き続き「実装済み・実機確認待ち」とし、commitはまだ行わない
- `@phosphor-icons/react`を導入
- サイドバーメニューの簡易記号をPhosphor Iconsへ変更
- 前月・次月ボタンをPhosphor Iconsへ変更
- 必要なアイコンだけを名前付きimport
- 予定カテゴリにはまだアイコンを導入していない
- HootoDayロゴは現状を維持
- 状態は引き続き「実装済み・実機確認待ち」とし、commitはまだ行わない
- 初回の緑・オレンジ配色が全体的にくすんで見えることを確認
- 茶色寄りだったオレンジを、明るく濁りの少ない色へ変更
- 画面背景を非常に淡い緑へ変更
- サイドバーなどの補助背景へアイボリーを移動
- カードとカレンダーは白を維持
- レイアウトと機能は変更していない
- 状態は引き続き「実装済み・実機確認待ち」とし、commitはまだ行わない
- 視認性と可愛さを両立するため、`M PLUS Rounded 1c`を採用
- 配色を紫中心から、緑・オレンジ・白・チャコール中心へ変更
- 強調と注意にはくすみ赤を使用
- レイアウトと機能は変更していない
- 状態は引き続き「実装済み・実機確認待ち」とし、commitはまだ行わない

#### Phase 1完了記録

- 実機確認日：2026年7月17日
- PC表示でサイドバー、カレンダー、選択日詳細欄の3列レイアウトを確認
- ウィンドウ幅に応じて全体が段階的に縮小することを確認
- 必要な画面幅で選択日詳細欄がカレンダー下へ移動するレスポンシブ表示を確認
- カレンダーセルの日付数字が左上へ配置されることを確認
- 予定、メモ印、今日、選択日の表示を確認
- `M PLUS Rounded 1c`を正式採用
- 薄緑、アイボリー、白、緑、オレンジを中心とした配色を正式採用
- `@phosphor-icons/react`を正式採用
- 前月、次月、今日への移動、日付選択、選択日詳細欄との連動を確認
- Phase 1では予定の登録、編集、削除、保存機能は未実装
- Phase 1を完了とし、次はPhase 2「予定の登録、編集、削除」へ進む

### Theme Phase 1.5（完了）

- ライト、ダーク、端末設定連動の3種類を追加
- localStorageキー：`hootoDay.theme`
- 保存値と初期値：`light`、`dark`、`system`（初期値は`system`）
- `data-theme`属性へ実際に適用する`light`または`dark`を設定
- `system`選択時のみ`prefers-color-scheme`の変更へ追従
- ライトテーマはPhase 1で正式採用した配色を維持
- ダークテーマは深緑黒、チャコール、緑、オレンジを中心に構成
- サイドバーの設定ボタンから開く簡易テーマ設定パネルを追加
- テーマ用アイコンにPhosphor Iconsを使用
- localStorageへ保存するのはテーマ設定のみで、予定や健康記録などのデータ保存は未実装
- 実装後は「実装済み・実機確認待ち」として実画面確認を実施
- commitはユーザーの実画面確認後に行う方針とした
- 実機確認で、設定ボタンを押しても`dialog`が開かない不具合を確認
- 原因はStrictMode下でEffectクリーンアップの`close()`が、ユーザー終了と同じ`onClose`処理へ流れていたこと
- ユーザー操作による終了と内部クリーンアップを分離して修正
- React StrictModeは維持
- dialog修正後も実機確認待ちとして確認を継続
- この時点ではcommitを行わない
- ダークテーマ初版は緑を主役色として構成
- 実機確認で、ダークテーマはオレンジ主体へ寄せたい要望が出た
- ダークテーマの主役色をオレンジへ変更
- 緑はロゴや医療・整体系カテゴリなどの補助色として維持
- ライトテーマの配色は変更していない
- 配色調整後も実機確認待ちとして確認を継続
- この時点ではcommitを行わない

#### Theme Phase 1.5完了記録

- 実機確認日：2026年7月17日
- 設定ボタンから`dialog`が開き、開いた直後に勝手に閉じないことを確認
- 閉じるボタン、Escapeキー、backdropクリックで閉じることを確認
- `dialog`内部のクリックでは閉じないことを確認
- ライト、ダーク、端末設定の切り替えを確認
- 再読み込み後も選択が維持されることを確認
- `system`設定を保存できることを確認
- React StrictModeを維持したまま`dialog`の不具合を修正
- ライトテーマはPhase 1で正式採用した緑主体の配色を維持
- ダークテーマはオレンジ主体の配色を正式採用
- ダークテーマの背景は暖色寄りチャコールを採用
- ダークテーマの緑はロゴやカテゴリなどの補助色として維持
- 設定ダイアログの選択状態と可読性を確認
- カレンダーの月移動など既存操作が維持されていることを確認
- 予定や健康記録の保存機能は未実装
- Theme Phase 1.5を完了とし、次はPhase 2「予定の登録、編集、削除」へ進む

### Phase 2A（完了）

- 予定の新規登録UI、編集UI、削除機能を追加
- 入力項目：日付、予定名、カテゴリ、終日、開始時刻、終了時刻、補足メモ
- 予定名、日付、開始時刻、終了時刻の入力検証を実装
- 予定の変更をカレンダーと選択日詳細へ即時反映
- 初期サンプル予定も編集・削除可能
- 予定データはReact stateのみで管理
- ブラウザ再読み込み後は初期サンプル予定へ戻る
- 予定のlocalStorage保存はPhase 2Bで実装予定
- 主な追加・変更：`EventEditorDialog`、`DayDetails`、`Calendar`、`useEvents`、予定型・予定ユーティリティ
- 実装後は「実装済み・実機確認待ち」として実画面確認を実施
- commitはユーザーの実画面確認後に行う方針とした
- 実機確認で、予定入力中にダイアログ外クリックで閉じる問題を確認
- 誤操作による入力消失を防ぐため、予定入力ダイアログのbackdropクリック終了を廃止
- 保存、キャンセル、閉じるボタン、Escapeキー、削除完了時だけ閉じる仕様へ変更
- ThemeSettingsのbackdropクリックによる終了仕様は維持
- backdrop仕様変更後も実機確認待ちとして確認を継続
- この時点ではcommitを行わない

#### Phase 2A完了記録

- 実機確認日：2026年7月17日
- 予定の新規登録を確認
- 通常予定、終日予定、終了時刻付き予定を確認
- 予定名、カテゴリ、日付、終日、開始時刻、終了時刻、補足メモを確認
- 予定名、日付、開始時刻、終了時刻の入力検証を確認
- 終了時刻が開始時刻以前の場合は保存しないことを確認
- カレンダーと選択日詳細への即時反映を確認
- 予定の編集と同一IDでの更新を確認
- 日付変更による予定移動を確認
- 対象予定だけが削除されることを確認
- 3件以上の予定で省略表示されることを確認
- 予定入力ダイアログはbackdropクリックでは閉じない仕様を正式採用
- 保存、キャンセル、閉じるボタン、Escapeキー、削除完了時だけ閉じる仕様を確認
- ThemeSettingsのbackdropクリックによる終了仕様は維持
- ライト・ダークテーマでの表示を確認
- Phase 2AではReact stateのみで予定を管理
- ブラウザ再読み込み後は初期サンプル予定へ戻る
- 予定のlocalStorage保存はPhase 2Bで実装する
- Phase 2Aを完了とし、次はPhase 2B「予定データの保存・復元」へ進む

### Phase 2B（完了）

- 予定データをlocalStorageへ保存
- 使用キー：`hootoDay.events`（テーマ設定の`hootoDay.theme`とは別管理）
- 保存形式：`{ version: 1, events: CalendarEvent[] }`
- 保存データのversion：`1`
- 将来の予定項目追加や形式変更を判別しやすくするため、配列直接保存ではなくversion付きラッパー形式を採用
- localStorageに保存データがない初回のみ初期サンプル予定を使用
- 保存済みデータがある場合は保存内容を優先
- 空配列を有効な保存状態として扱い、予定0件でも初期サンプルを復活させない
- 削除済み・編集済み予定を再読み込みで初期状態へ戻さない
- 不正JSON、version不正、eventsが配列でない場合は初期サンプル予定へ安全に戻す
- 配列内の一部予定だけが不正な場合は、不正項目だけを除外して有効な予定を復元する
- 元データが正しいversionの配列であれば、有効予定が0件になっても空配列として扱う
- localStorageの読み書きに失敗してもReact state上のアプリ操作を継続
- 予定の追加、編集、削除によるstate変更時に最新配列を保存
- Phase 2Aの表示・フォーム仕様は変更していない
- 実装後は「実装済み・実機確認待ち」として実画面確認を実施
- commitはユーザーの実画面確認後に行う方針とした

#### Phase 2B完了記録

- 実機確認日：2026年7月17日
- 新規登録した予定が再読み込み後も維持されることを確認
- 削除した予定が再読み込み後も復活しないことを確認
- 保存済みデータが初期サンプル予定より優先されることを確認
- localStorageキーは`hootoDay.events`
- version 1のラッパー形式を正式採用
- 空配列を有効な予定0件の状態として扱う
- 不正データでも白画面にせず安全に初期データへ戻す
- 配列内の一部だけが不正な場合は不正予定だけを除外する方針を採用
- localStorageの読み書きに失敗しても画面上の操作を継続
- テーマ設定の`hootoDay.theme`とは別管理
- Phase 2Aの表示や予定フォームの仕様は変更していない
- Phase 2Bを完了とし、次はPhase 2C「予定カテゴリのアイコン・短縮表示・時刻表示最適化」へ進む

### Phase 2C（完了）

- 実機確認日：2026年7月17日
- 全11カテゴリのPhosphor Iconsを実機で確認
- カレンダー内は14px、選択日詳細欄は20pxで判別可能なことを確認
- 560px以下ではカレンダー内アイコンを非表示
- 正式採用アイコン：収録→Microphone、ライブ→MicrophoneStage、リハ→Headphones、配信→Desktop、歌枠→MusicNotes、歯医者→Tooth、整体→PersonSimpleTaiChi、通院→FirstAidKit、おでかけ→Park、映→Ghost、その他→Cactus
- カレンダー用短縮カテゴリ：収録→収録、ライブ→ライ、リハ→リハ、配信→配信、歌枠→歌枠、歯医者→歯医、整体→整体、通院→通院、おでかけ→外出、映→映、その他→他
- 正式カテゴリ名と予定名が完全一致する場合だけ短縮表示し、自由入力した予定名は変更しない
- 時刻は分が`00`の場合だけ「20時」などの形式へ変換
- 分がある場合は`20:15`、`20:30`などの`HH:mm`表示を維持
- 終了時刻付き表示にも同じ時刻変換規則を適用
- 正式カテゴリ名、カテゴリ値、保存済みデータ、localStorageのversion 1保存形式は変更していない
- 全カテゴリを比較する一時確認パネルは実機確認完了後に削除
- Phase 2Cを完了とし、次はPhase 3「日記・メモ」へ進む

### Phase 3A（完了）

- 日付別の日記・メモ入力を追加し、1日につき1件を管理
- 本文は最大2000文字で、前後の空白を除去して保存
- localStorageキーは`hootoDay.dayMemos`
- `{ version: 1, memos: DayMemo[] }`形式で予定・テーマとは別に保存
- 新規保存、同一日付の上書き編集、確認付き削除に対応
- カレンダーのメモ印を保存済み実データと連動し、Phase 1の固定サンプルメモ表示を削除
- 選択日詳細欄に保存本文の概要を最大3行で表示
- 日記・メモdialogはbackdropクリックでは閉じず、保存、キャンセル、閉じる、Escape、削除完了時に終了
- 保存データ全体が不正な場合は空配列へ戻し、一部項目だけ不正な場合は不正項目だけを除外
- 同じ日付が複数ある場合は`updatedAt`が新しい項目を採用し、同時刻の場合は後の項目を採用
- localStorageの読み書き失敗時もReact state上の操作を可能な限り継続
- 実機確認に基づき、メモ存在表示をオレンジ色の丸からPhosphor Iconsの`PencilSimpleIcon`へ変更
- ペンシルは日付セル右上の配置を維持した表示専用アイコンで、クリック機能は追加していない
- 日記・メモ編集は引き続き選択日詳細欄の「記録を開く」から行う
- 実機確認日：2026年7月17日
- 新規保存、カレンダー右上のペンシル表示、日付・予定と重ならない配置を確認
- 選択日詳細の本文概要表示と、編集後の再読み込み維持を確認
- 削除後のペンシル消去と、再読み込み後も削除状態が維持されることを確認
- backdropクリックでは閉じず、入力途中の本文が維持されることを確認
- 保存成功、キャンセル、閉じる、Escape、削除完了の終了条件と、未保存入力の破棄を確認
- ライト・ダークテーマでの表示を確認
- localStorageキー`hootoDay.dayMemos`とversion 1形式を正式採用
- Phase 3Aでは日記一覧、検索、健康記録、TXT・Markdown出力は未実装
- Phase 3Aを完了とし、Phase 3BまたはPhase 4へ進む前に日記・メモの次工程を整理する

### Phase 4A（完了）

- Phase 4Aの範囲を、健康記録画面の共通土台、日付別体重記録、健康プロフィールとして整理
- 健康記録画面の共通土台を追加
- App内stateでカレンダーと健康記録を切り替え、選択中ナビゲーションの`aria-current`を連動
- カレンダーと健康記録で選択日を共有し、健康記録側の日付変更をカレンダーの表示月へ反映
- 前日、次日、今日、日付inputによる日付移動に対応し、ローカル日付基準で処理
- 日付別の`WeightRecord`を追加し、1日につき1件を管理
- localStorageキーは`hootoDay.weightRecords`
- `{ version: 1, records: WeightRecord[] }`形式で予定、日記、テーマとは別に保存
- 体重記録の新規保存、同一日付の上書き編集、確認付き削除に対応
- 体重は20kg以上300kg以下で検証し、保存時に小数第1位へ正規化
- 画面表示は今後の平均・グラフとの統一を考慮し、常に小数第1位まで表示
- 任意メモは最大200文字で、前後の空白を除去して保存
- 保存データ全体が不正な場合は空配列へ戻し、一部項目だけ不正な場合は不正項目だけを除外
- 同じ日付が複数ある場合は`updatedAt`が新しい項目を採用し、同時刻の場合は後の項目を採用
- localStorageの読み書き失敗時もReact state上の操作を可能な限り継続
- 睡眠、食事、運動、体調メモは未実装
- 最新体重、目標体重、標準体重、美容体重、期間平均、グラフは後続Phaseで実装予定
- 本人専用の`HealthProfile`型を追加し、身長、生年月日、計算用の性別、目標体重、更新日時を管理
- 健康プロフィールは1件のみとし、localStorageキー`hootoDay.healthProfile`へ`{ version: 1, profile: HealthProfile | null }`形式で保存
- `profile: null`を有効な未設定状態として扱い、初期サンプルは使用しない
- 年齢は保存せず、生年月日と現在のローカル日付から表示時に計算
- 全項目を任意入力としつつ、すべて未設定の状態は通常保存せず、保存済み内容を全消去する場合は削除操作を使用
- 身長は50.0～250.0cm、目標体重は20.0～300.0kgで検証し、数値は小数第1位へ正規化
- 生年月日は1900年1月1日から現在のローカル日付まで、計算用の性別は女性、男性、未設定に限定
- 健康プロフィールの新規保存、1件の上書き編集、確認付き削除に対応し、削除時も体重記録など既存データは維持
- 不正JSON、version不正、プロフィール不正は未設定へ安全に戻し、localStorage利用不可時もReact state上の操作を継続
- BMI、標準体重、美容体重、目標との差、基礎代謝、運動消費カロリー等の計算は未実装
- 既存の体重記録実装と保存形式は変更していない
- 狭いウィンドウ幅で健康記録カードが右側へはみ出す問題を実機確認
- 固定幅、`min-width`、grid・flex子要素の幅指定を見直し
- 健康記録ルートと各カードを親幅以内へ収めるレスポンシブ修正を実施
- 狭い画面では日付欄と健康記録カードを縦並びへ変更
- 健康プロフィールdialogと体重dialogもviewport内へ収まるよう幅とスクロール領域を確認・調整
- 実際のChromeでは`(max-width: 820px)`がtrueでも、PC用`.sidebar`と2列gridの指定が維持される問題を確認
- 原因は、独立した狭幅用ナビゲーションがなく、同じ`.sidebar`を上部ナビ風に変形するCSS構造だったこと
- 820px以下ではPC用`.sidebar`を非表示、独立した上部ナビゲーションを表示し、`.app-body`を明示的な1列gridへ変更
- Windows表示倍率やChromeズームは直接原因ではないことを確認
- 820px以下のナビゲーション切り替え修正後も、ウィンドウ幅によって共通レイアウトやカレンダーが固定キャンバス状にはみ出して見える問題を確認
- `.app-shell`、header、`.app-body`、sidebar、`.main-content`、カレンダーの幅責務を見直し
- viewport幅を持つ外側コンテナとして`.app-shell`を`width: 100%`、`max-width: 100%`へ変更し、1320px固定幅と中央寄せを廃止
- header、body、sidebar、main、カレンダー、健康記録へ親幅内に収まる幅と`min-width: 0`を連続して設定
- `body`の`overflow-x: hidden`で切れを隠さず、各コンテナの幅計算を修正して横超過の根本解消を図った
- 各ブレークポイントで横スクロールがないことを再確認する方針
- 状態は「完了」

#### Phase 4A完了記録

- 実機確認日：2026年7月17日
- 健康記録画面とカレンダーを相互に切り替えられることを確認
- 両画面での選択日共有と、前日、次日、今日への日付移動を確認
- 体重記録の新規保存、編集、対象日だけの削除、再読み込み後の維持を確認
- 体重の20.0～300.0kgの入力範囲と、小数第1位への正規化を確認
- 健康プロフィールの新規保存、編集、プロフィールだけの削除を確認
- 生年月日から表示時に計算する年齢が正しいことを確認
- 健康プロフィール削除後も体重記録が維持されることを確認
- ライト・ダークテーマで健康記録画面と各入力dialogを確認
- 体重記録は`hootoDay.weightRecords`、健康プロフィールは`hootoDay.healthProfile`の別localStorageキーで管理
- 狭幅時に健康記録カードが右側へはみ出す問題を修正
- 820px以下でPCサイドバー非表示、上部ナビゲーション表示、`.app-body`の1列化を確認
- `.app-shell`の固定最大幅と中央寄せを廃止
- header、body、sidebar、main、カレンダー、健康記録を含む共通レイアウトがviewport幅へ追従することを確認
- カレンダーと健康記録の両画面で横切れと横スクロールが解消したことを確認
- 睡眠、食事、運動、体調メモは未実装
- BMI、標準体重、美容体重、目標との差、平均体重、グラフ、基礎代謝、消費カロリー等は未実装
- Phase 4Aを完了とし、次はPhase 4B「体重集計・目標・標準体重・美容体重・グラフ」へ進む

### Phase 4B（完了）

- 健康記録内に「日付別記録」と「体重まとめ」の表示切り替えを追加
- 保存済み`WeightRecord[]`と`HealthProfile`から表示時に計算し、保存データ形式とlocalStorageキーは変更していない
- 最新体重は`updatedAt`ではなく記録日`date`が最も新しい記録を採用
- 最新記録日、前回記録との差、目標体重との差、標準体重との差、美容体重との差を表示
- BMIは`体重kg ÷ 身長m²`で計算し、小数第1位まで表示
- 標準体重は`身長m² × 22`で計算し、BMI 22を基準にした参考値として表示
- 美容体重（参考）は`身長m² × 20`で計算し、医学的な正式基準ではないBMI 20相当の参考値と明記
- 目標体重は健康プロフィールの値を使用し、未設定時はプロフィール設定への入口を表示
- 集計基準日は最新の体重記録日とし、記録がない場合は現在のローカル日付を使用
- 7日平均は基準日を含む過去7日間、30日平均は基準日を含む過去30日間を対象
- 6か月平均は基準日から6か月前、1年平均は基準日から1年前を開始日とし、月末とうるう日をローカル日付で安全に補正
- 各平均は記録のない日を0として含めず、単純平均、対象記録数、対象期間を表示
- グラフ期間は7日、30日、6か月、1年、全期間で切り替え、初期値は30日
- 追加ライブラリを使わず、親幅へ追従するSVG折れ線グラフを実装
- グラフへ全記録点、折れ線、縦軸、間引いた日付ラベル、最新点、最小・最大・最新値の数値概要を表示
- 目標体重設定時は「目標」と明記した水平補助線を表示
- 記録0件は空状態、1件や同一体重だけの記録でも0除算せず表示できる構造
- BMI、標準体重、美容体重と画面全体が医療診断ではないことを明記
- 状態は「完了」

#### Phase 4B完了記録

- 実機確認日：2026年7月17日
- 健康記録内で「日付別記録」と「体重まとめ」を切り替えられることを確認
- 最新体重、前回記録との差、目標体重との差を確認
- BMI、標準体重、美容体重（参考）の計算結果が既知の値と一致することを確認
- 美容体重はBMI 20相当の非医学的な参考値として正式採用
- 7日、30日、6か月、1年の平均体重、記録数、対象期間を確認
- 日付の異なる複数記録でSVG折れ線グラフを確認
- 7日、30日、6か月、1年、全期間のグラフ切り替えを確認
- 目標体重設定時の水平補助線を確認
- ライト・ダークテーマで概要カード、平均、グラフの表示を確認
- 保存済み体重記録と健康プロフィールを変更せず、計算結果は表示時に算出
- グラフを含め追加ライブラリを使用していない
- BMI、標準体重、美容体重が参考値であり、医療診断ではない旨の表示を確認
- Phase 4Bを完了とし、次はPhase 4C「睡眠記録」へ進む

### Phase 4C（完了）

- 起床した日を記録日とする、1日1件の日付別睡眠記録を追加
- 就寝時刻と起床時刻を必須とし、起床時刻が就寝時刻より早い場合は翌日として日またぎ計算
- 同一時刻、総就床時間30分未満、18時間超は保存不可
- 途中覚醒を複数登録でき、「時刻だけ」と「時間範囲」の2種類に対応
- 時刻だけの途中覚醒は初期15分、5～180分の範囲で差し引き時間を変更可能
- 時間範囲の途中覚醒は覚醒開始から再入眠までの実時間を差し引く
- 途中覚醒は睡眠区間内に限定し、同一時刻、範囲同士、時刻と範囲の重複を保存不可
- 総就床時間、途中覚醒合計、実睡眠時間を分単位で自動計算し、dialog内で即時プレビュー
- 実睡眠時間は`総就床時間 - 途中覚醒合計`とし、0分以下は保存不可
- localStorageキーは`hootoDay.sleepRecords`
- `{ version: 1, records: SleepRecord[] }`形式で体重、プロフィール、予定、日記、テーマとは別管理
- 計算値`totalInBedMinutes`、`awakeMinutes`、`sleepMinutes`を保存し、読み込み時に入力値から再計算して正常化
- 空配列を有効状態とし、初期サンプルは使用しない
- 不正JSON、version不正、records非配列は空配列、不正な記録だけを除外
- 同日重複は`updatedAt`が新しい記録を優先し、同時刻は後の記録を優先
- 睡眠記録の新規保存、同日上書き編集、確認付き削除、再読み込み復元に対応
- 睡眠dialogはbackdropクリックでは閉じず、保存、キャンセル、閉じる、Escape、削除完了時のみ終了
- 睡眠時間は本人入力による概算であり、医療機器による測定値ではないことを表示
- 睡眠の一覧、週間・月間平均、グラフ、評価は後続Phaseで実装予定
- 状態は「完了」

#### Phase 4C完了記録

- 実機確認日：2026年7月17日
- 00:00就寝、07:00起床で実睡眠時間7時間を確認
- 時刻だけの途中覚醒を02:00と05:00に各15分登録し、合計30分差し引いた実睡眠時間6時間30分を確認
- 02:00～03:30の時間範囲を1時間30分として差し引くことを確認
- 23:00就寝、07:00起床の日またぎ計算を確認
- 保存した睡眠記録が再読み込み後も維持されることを確認
- 睡眠記録削除後、再読み込みしても復活しないことを確認
- 睡眠記録は起床日を記録日とする方式を正式採用
- 途中覚醒は「時刻だけ」と「時間範囲」の2方式を正式採用
- 保存した計算値は読み込み時に就寝、起床、途中覚醒から再計算して正常化
- 睡眠時間は本人入力による概算値として扱う
- 睡眠の週間・月間平均、グラフ、評価は未実装
- Phase 4Cを完了とし、次はPhase 4D「食事記録」へ進む

### Phase 4C-2（完了）

- 健康記録画面へ「睡眠まとめ」タブを追加し、「日付別記録」「体重まとめ」と同じ画面内stateで切り替える
- 保存済みの`SleepRecord[]`を参照して表示時に集計し、`hootoDay.sleepRecords`のキー、version 1形式、保存内容は変更しない
- 集計基準日は最新の睡眠記録日とし、記録がない場合は現在のローカル日付を使用する
- 最新の実睡眠時間、記録日、就寝・起床時刻、総就床時間、途中覚醒回数・合計時間、前回記録との差を表示する
- 7日、30日、6か月、1年について、実睡眠時間、総就床時間、途中覚醒時間、途中覚醒回数、記録数、対象期間の平均を表示する
- 記録のない日は平均へ含めず、記録がない期間は`0時間`ではなく未記録表示とする
- 平均就寝・起床時刻は、24時間を円として扱う円周平均で計算する。深夜0時前後を連続した時刻として扱い、単純な時刻平均による昼へのずれを防ぐ
- SVGによる実睡眠時間の折れ線グラフを追加し、7日、30日、6か月、1年、全期間を切り替え可能とする。初期期間は30日
- グラフは全データ点を維持し、日付ラベルのみ必要に応じて間引く。最新点、平均、最短、最長、最新値、記録数を数値でも確認できる
- 記録0件、1件、同一睡眠時間だけの場合も安全に表示できる構造とする
- 選択期間の途中覚醒について、平均時間、最大時間、平均回数、最大回数を表示する
- メモ付き睡眠記録の総数と、日付が新しい最大3件の概要を表示する
- SVGへ`title`と説明用`aria-label`を付け、グラフだけに情報を依存しない
- 本人入力による概算記録であり、医療機器による測定、医療診断、睡眠状態の評価ではない旨を表示する
- 追加ライブラリは使用しない
- 状態は「完了」

#### Phase 4C-2完了記録

- 実機確認日：2026年7月17日
- 日付別記録、体重まとめ、睡眠まとめの切り替えを確認
- 複数の睡眠記録でSVG折れ線グラフを確認
- 7日、30日、6か月、1年、全期間のグラフ期間切り替えを確認
- 最新の実睡眠時間、記録日、就寝・起床時刻、総就床時間、途中覚醒、前回記録との差を確認
- 7日、30日、6か月、1年の期間平均と、未記録日を平均へ含めない動作を確認
- 平均就寝時刻と平均起床時刻を確認
- 23時台と0時台を含む平均就寝時刻が、正午付近ではなく自然な深夜時刻になることを確認
- 平均時刻の計算方法として、24時間を円として扱う円周平均を正式採用
- 平均・最大の途中覚醒時間と途中覚醒回数の表示を確認
- メモ付き記録数と最新3件までの概要表示を確認
- ライト・ダークテーマで睡眠まとめとグラフを確認
- `hootoDay.sleepRecords`、version 1形式、保存済みの睡眠記録は変更していない
- 睡眠時間と集計値は本人入力による概算であり、医療機器による測定や医療診断、睡眠状態の評価ではない
- 次はHealth UI Phaseとして、健康プロフィールを設定へ移動し、各健康カードから共通日付を変更できるUIへ整理する
- Health UI Phase完了後にPhase 4D「食事記録」へ進む

### Health UI Phase（完了）

- 実装時期はPhase 4C-2の直後とする
- 健康プロフィールを健康記録画面から設定へ移動する
- 設定ダイアログに健康プロフィール編集への入口を追加する
- 健康記録画面上部の大きな日付操作欄を縮小または撤去する
- 体重、睡眠、今後追加する食事・運動・体調の各カードへコンパクトな日付操作を配置する
- すべての健康カードは同じ共通選択日を操作し、カードごとに別の日付stateを持たない
- 体重まとめと睡眠まとめの集計基準日は従来どおり最新記録日とし、共通選択日の影響を受けない
- Health UI Phaseでは健康記録の保存形式や既存データを変更しない
- 健康プロフィールの大きなカードを日付別健康記録画面から削除し、設定ダイアログへ表示入口を移動
- 設定ダイアログを「テーマ」と「健康プロフィール」の2セクションへ整理
- 健康プロフィールの入力・保存・削除には既存の`HealthProfileDialog`を再利用
- ネイティブmodal同士の競合を避けるため、設定からプロフィール編集へ移る際は設定ダイアログを閉じてからプロフィールダイアログを開く
- 健康記録上部の大きな日付カードを、共通選択日と直接入力・前日・次日・今日を備えた小型ツールバーへ変更
- 共通日付操作コンポーネント`HealthDateNavigator`を追加し、上部・体重カード・睡眠カードで再利用
- 体重カードと睡眠カードの日付操作は、Appが保持する同じ`selectedDate`を変更し、カードごとの日付stateは作らない
- カード内の日付は現在年なら「7月17日」、現在年と異なる場合は「2027年1月1日」の形式で表示し、読み上げと`title`では常に年月日を示す
- 体重まとめ・睡眠まとめの集計基準日は従来どおり最新記録日であり、共通選択日の影響を受けない
- 将来の食事・運動・体調カードでも`HealthDateNavigator`を再利用する方針
- `hootoDay.healthProfile`、`hootoDay.weightRecords`、`hootoDay.sleepRecords`と各version 1形式は変更していない
- 状態は「完了」
- 完了後はPhase 4D「食事記録」へ進む

#### Health UI Phase完了記録

- 実機確認日：2026年7月17日
- 健康プロフィールが日付別健康記録画面から設定へ移動したことを確認
- 設定ダイアログの健康プロフィール概要と、設定済みプロフィールの編集入口を確認
- 既存の`HealthProfileDialog`による編集・保存・削除を確認
- 設定ダイアログを閉じてから`HealthProfileDialog`を開き、2つのネイティブmodalが同時に開かないことを確認
- 健康記録上部の日付操作が小型化されていることを確認
- 体重カードと睡眠カードのコンパクトな日付操作を確認
- 体重カードまたは睡眠カードから日付を変更すると、上部・体重・睡眠が同じ日付へ同時に切り替わることを確認
- カード別の日付stateを持たず、Appの`selectedDate`を共有する構造を正式採用
- カレンダーと健康記録の選択日共有を維持
- 体重まとめと睡眠まとめに共通日付ナビゲーションを表示せず、集計基準日は最新記録日のままであることを確認
- `hootoDay.healthProfile`、`hootoDay.weightRecords`、`hootoDay.sleepRecords`と各version 1形式は変更していない
- 将来の食事・運動・体調カードにも`HealthDateNavigator`を再利用する
- Health UI Phaseを完了とし、次はPhase 4D「食事記録」へ進む

### Phase 4D（完了）

- 共通選択日ごとに1日1件の食事記録を保存する日付別食事記録を追加
- `MealRecord`は日付、朝食、昼食、夕食、間食、更新日時を持つ
- 朝食、昼食、夕食、間食は自由記述のプレーンテキストとし、改行を維持する
- 各食事項目は任意、最大1000文字。前後の空白を除去し、4項目すべてが空欄または空白・改行だけの場合は保存しない
- localStorageキーは`hootoDay.mealRecords`
- 保存形式は`{ version: 1, records: MealRecord[] }`のversion付きラッパー形式
- 初期サンプルは使用せず、保存データがない場合と空配列は食事記録0件の有効な状態として扱う
- 食事記録の新規保存、同日上書き編集、対象日だけの確認付き削除に対応
- 食事カードへ`HealthDateNavigator`のコンパクト表示を追加し、上部・体重・睡眠と同じAppの`selectedDate`を操作する
- 食事カードでは空欄の食事項目を省略し、入力済み項目だけを各3行まで概要表示する。全文は入力ダイアログで確認する
- 保存本文は表示用に書き換えず、将来の期間指定TXT・Markdown出力で利用しやすいプレーンテキストのまま保持する
- 食事入力ダイアログは保存、キャンセル、閉じる、Escape、削除完了で閉じ、backdropクリックでは閉じない
- 読み込み時はunknownからラッパー、version、配列、日付、各文字列、文字数、更新日時を検証する
- JSON破損、version不正、records非配列は空配列とし、一部の不正記録だけを除外する
- 同日重複は`updatedAt`が新しい記録を優先し、同時刻の場合は配列で後にある記録を優先する
- localStorageの読み書きは`try/catch`で保護し、利用できない場合もReact state上の操作を継続する。食事本文はconsoleへ出力しない
- カロリー・栄養計算、食材解析、写真、時刻、食事評価、食事まとめページは未実装
- 状態は「完了」
- 次はPhase 4E「運動記録」へ進む

#### Phase 4D完了記録

- 実機確認日：2026年7月17日
- 朝食、昼食、夕食、間食の自由記述保存を確認
- 改行を含むプレーンテキスト本文の保存、再読み込み維持、カード表示を確認
- 食事カードでは入力済み項目だけを表示し、空欄項目を表示しない方針を正式採用
- 各食事項目の長文が最大3行程度で省略され、全文を編集ダイアログで確認できることを確認
- 保存済み記録の編集、対象日だけの削除、削除後の再読み込みで非復活を確認
- 食事カードから日付を変更した際に、上部・体重・睡眠・食事が同じ共通選択日へ切り替わることを確認
- 朝食、昼食、夕食、間食の4項目すべてが空欄または空白だけの場合は保存しない
- localStorageキー`hootoDay.mealRecords`とversion 1ラッパー形式を正式採用
- 将来の期間指定TXT・Markdown出力を前提とし、食事内容をHTML等へ変換せず改行を維持したプレーンテキストで保存する
- カロリー計算、栄養計算、食事評価、食事まとめページは未実装
- Phase 4Dを完了とし、次はPhase 4E「運動記録」へ進む

### Phase 4D-2（完了）

- よく使う食事内容を登録・選択できる食事定型メニューを追加
- `MealTemplate`はID、定型名、使用先、内容、`sortOrder`、作成日時、更新日時を持つ
- 使用先は朝食、昼食、夕食、間食、どこでも使用の5種類
- localStorageキーは`hootoDay.mealTemplates`、保存形式は`{ version: 1, templates: MealTemplate[] }`
- 食事記録の`hootoDay.mealRecords`とは別に保存し、既存の保存形式を変更しない
- 定型メニューの新規作成、同一IDの編集、確認付き削除、上下移動に対応
- 表示順は全定型共通の`sortOrder`で管理し、変更時に0から連番へ正常化して即時保存
- 食事入力欄には対象の食事種類と「どこでも使用」の定型だけを表示
- 選択した定型内容は既存本文を置き換えず、改行して末尾へ追記する
- 追記後に1000文字を超える場合は追加せず、エラーを表示する
- 定型選択だけでは食事記録を保存せず、食事入力の「保存」時だけ`MealRecord`へ反映
- 食事入力と定型管理は同じネイティブdialog内の画面切り替えとし、modalを重ねず入力途中の本文をReact stateに保持
- 読み込み時はunknownからversion、配列、各項目、日時を検証し、一部不正項目を除外、重複IDは新しい`updatedAt`を優先
- localStorageの読み書き失敗時もReact state上の操作を継続し、定型内容や食事本文をconsoleへ出力しない
- 使用回数、お気に入り、自動提案、自動並び替え、栄養・カロリー計算は未実装
- 実機確認日：2026年7月17日
- 定型メニューの新規登録と、再読み込み後も保存内容が維持されることを確認
- 保存済み定型メニューの編集内容が管理一覧と食事入力候補へ反映されることを確認
- 定型メニューの個別削除を確認
- 上下移動による並び替えと、管理一覧・食事入力候補への表示順反映を確認
- 朝食専用定型が朝食欄だけへ表示され、使用先が異なる専用定型は候補へ表示されないことを確認
- 「どこでも使用」の共通定型を対象となる全食事欄へ表示する方針を確認
- 既存本文を消さず、改行して末尾へ定型内容を追記できることを確認
- 定型管理画面へ移動して戻っても、未保存の食事本文が維持されることを確認
- 入力画面と管理画面を同じネイティブdialog内で切り替え、native dialogを同時に複数開かない構造を正式採用
- `MealRecord`と`MealTemplate`は別のlocalStorageキーとversion 1形式で管理する
- 使用回数、ランキング、自動提案、栄養計算は未実装
- Phase 4D-2を完了とする
- 次はPhase 4E「運動記録」へ進む

### Phase 4E（完了）

- 共通選択日ごとに1日複数件を保存できる日付別運動記録を追加
- `ExerciseSession`はID、日付、運動種類、その他の自由名、運動時間、平均心拍数、METs、計算使用体重、推定消費カロリー、メモ、作成日時、更新日時を持つ
- 初期運動種類はルームランナー、エアバイク、ビートセイバー、ストレッチ、その他
- 初期METsは順に3.5、5.0、5.0、2.3、3.0とし、保存データ外の共通設定表で一元管理する
- METsは運動強度に合わせて1.0～20.0の範囲で編集でき、小数第1位へ正常化する
- 運動時間は1～1440分の整数で保存し、表示時は「1時間30分」等へ変換する
- 平均心拍数は任意の30～250 bpmの整数で記録し、今回の推定カロリー計算には使用しない
- 新規入力時の計算使用体重は同日の体重、次に運動日以前の最新体重を使用し、未来日の体重は自動使用しない
- 計算使用体重は手動変更でき、編集時は保存済みの値を維持する
- 推定消費カロリーは`METs × 体重kg × 運動時間h`を整数kcalへ四捨五入する
- 保存時点のMETs、計算使用体重、推定消費カロリーをスナップショットとして保存し、体重記録の後日変更では自動再計算しない
- 体重が未設定でも運動記録を保存でき、その場合の推定消費カロリーは`null`とする
- 運動カードへ日次合計時間、計算済みセッションの合計推定消費カロリー、セッション数、個別一覧を表示する
- 新規、同一IDでの編集、確認付き個別削除に対応し、同日・他日の他セッションへ影響させない
- localStorageキーは`hootoDay.exerciseSessions`、保存形式は`{ version: 1, sessions: ExerciseSession[] }`
- 読み込み時はunknownからラッパーと各項目を検証し、不正セッションだけを除外、重複IDは新しい`updatedAt`を優先する
- 心拍数、年齢、性別、身長は今回の標準METs計算へ使用しない
- 推定値は本人入力による概算であり、医療機器や運動機器の測定値・医療診断ではない
- 運動種類管理、使用種類の追加・並び替え、期間集計、運動まとめ、グラフは未実装
- 実機確認日：2026年7月17日
- ルームランナー、エアバイク、ビートセイバー、ストレッチ、その他の5種類の運動登録を確認
- 「その他」の自由入力名が保存・表示されることを確認
- 同じ日へ複数セッションを登録し、個別に保持されることを確認
- 日次合計運動時間と、計算可能なセッションだけを対象とする合計推定消費カロリーを確認
- METs 5.0、体重60.0kg、運動時間30分で推定150kcalとなることを確認
- 運動日と同日の体重、同日がない場合は運動日以前の最新体重が自動選択されることを確認
- 運動日より未来の体重記録を自動使用しないことを確認
- 平均心拍数は記録専用とし、今回の推定カロリー計算へ使用しない
- 保存時点の体重、METs、推定消費カロリーをスナップショットとして保存する方針を正式採用
- 新規、同一IDでの編集、対象セッションだけの削除、再読み込み後の維持と非復活を確認
- 運動カードのHealthDateNavigatorと、上部・体重・睡眠・食事・カレンダーの共通選択日連動を確認
- 推定消費カロリーは本人入力による概算であり、医療機器や運動機器による測定値ではない
- 運動種類管理、期間集計、運動まとめ、グラフは未実装
- 運動まとめは後続の小Phaseとして追加可能とする
- Phase 4Eを完了とする
- 次はPhase 4F「体調メモ」へ進む

### Phase 4F（完了）

- 共通選択日ごとに1日1件を保存する日付別体調メモを追加
- `DailyConditionRecord`は日付、全体の体調、膝、腰、生理・周期メモ、気になること、自由メモ、更新日時を持つ
- 全体の体調は未設定、良い、普通、悪いの4状態
- 膝と腰は未設定、問題なし、少し気になる、痛い、強く痛いの5状態
- 状態の内部値と表示名は型安全な共通設定表で一元管理する
- 生理・周期メモと気になることは各最大500文字、自由メモは最大1000文字の改行可能なプレーンテキスト
- 文章は保存時に前後空白だけを除去し、本文内の改行を維持する
- すべての状態が未設定かつ文章欄が空白だけの場合は保存しない
- 新規、同日上書き編集、確認付きの対象日削除に対応
- 体調カードでは未設定状態と空欄文章を表示せず、文章は最大3行程度で省略する
- 体調カードへHealthDateNavigatorを追加し、体重、睡眠、食事、運動、カレンダーと同じ`selectedDate`を共有する
- localStorageキーは`hootoDay.conditionRecords`、保存形式は`{ version: 1, records: DailyConditionRecord[] }`
- 空配列を有効な0件状態として扱い、初期サンプルは使用しない
- 読み込み時はunknownからラッパーと各項目を検証し、不正記録だけを除外、同日重複は新しい`updatedAt`を優先する
- localStorageの読み書き失敗時もReact state上の操作を継続し、体調内容や生理・周期メモをconsoleへ出力しない
- 本人の振り返り用記録とし、自動診断、病名推定、医療緊急度判定は行わない
- 期間集計、体調グラフ、生理周期予測は未実装
- Phase 4の主要な日付別入力項目が一通り揃う
- 実機確認日：2026年7月17日
- 全体の体調、膝、腰の各状態を単独または組み合わせて保存できることを確認
- 生理・周期メモだけでも保存できることを確認
- 気になることと自由メモの保存、改行維持、カード概要表示を確認
- 全状態が未設定かつ全文章欄が空欄・空白だけの場合は保存できないことを確認
- エラー表示後に入力を修正して保存できることを確認
- 編集時に一部の状態を未設定へ戻し、一部の文章項目を空欄へ戻せることを確認
- 未設定または空欄へ戻した項目が体調カードから消えることを確認
- 新規、同日上書き編集、対象日だけの削除、再読み込み後の維持と非復活を確認
- 体調カードのHealthDateNavigatorと、上部・体重・睡眠・食事・運動・カレンダーの共通`selectedDate`連動を確認
- 体調記録は本人の振り返り用とし、自動診断、病名推定、医療緊急度判定を行わない
- Phase 4の主要な日付別入力項目が一通り完成したことを記録
- Phase 4Fを完了とする
- 次工程は健康記録全体の確認、運動まとめ、体調まとめ、または出力機能の優先順位を整理する

### Health Review Phase（完了）

- Phase 4で実装した健康プロフィール、体重、睡眠、食事、食事定型、運動、体調の入力・表示・保存を統合確認
- 日付別健康カードの正式順序を、体重、睡眠、食事、運動、体調メモの順に統一
- 日付別画面を圧迫していた未実装の「今後追加予定」カードを削除し、将来機能はPROJECT_NOTES.mdで管理する
- 各カードは左側に共通見出し、右側または狭幅時の下段に共通`HealthDateNavigator`、本文下部に主要操作を配置する
- 未記録時は「この日の○○記録はありません」と「○○を記録」ボタンを共通の空状態で表示し、0kg、0時間、0kcal等へ置き換えない
- 記録済みカードは日常的な概要だけを表示し、食事・運動・体調に加えて体重・睡眠メモも最大3行程度へ省略、全文は各dialogで確認する
- 1日1件の体重、睡眠、食事、体調は記録済み時に「編集」、1日複数件の運動は「運動を追加」と各セッションの「編集」を使用する
- 全健康カードはAppの同じ`selectedDate`を操作し、カード別の日付stateを持たない
- カード内日付は共通`HealthDateNavigator`で、現在年は「7月17日」、異なる年は「2027年1月1日」形式に統一
- 日付別画面では前日・次日・今日・日付直接選択を共有し、まとめ画面の集計基準日は従来どおり最新記録日とする
- 健康プロフィールは設定dialog内で管理し、日付別健康画面へ大きなプロフィールカードを戻さない
- 体重まとめのプロフィール編集入口と、設定dialogを閉じてからHealthProfileDialogを開く構造を維持
- 健康入力dialogは保存、キャンセル、右上の閉じる、Escape、削除完了で閉じ、backdropクリックでは閉じない安全方針を確認
- ThemeSettingsだけは従来どおりbackdropクリックで閉じる
- localStorageキーは`hootoDay.theme`、`hootoDay.events`、`hootoDay.dayMemos`、`hootoDay.healthProfile`、`hootoDay.weightRecords`、`hootoDay.sleepRecords`、`hootoDay.mealRecords`、`hootoDay.mealTemplates`、`hootoDay.exerciseSessions`、`hootoDay.conditionRecords`
- テーマ以外の保存データはversion 1で管理し、各キーのラッパー形式、空配列または`profile: null`の有効状態、unknownからの検証、try/catchを確認
- 各データは独立したlocalStorageキーとReact stateで管理され、1種類の保存・編集・削除が他種類を変更しない構造を確認
- 保存失敗時もReact state上の操作を継続し、日記本文、食事、運動、体調、生理・周期メモ等をconsoleへ出力しない
- 次のHealth Export Phaseで、日付ごとに日記・メモ、体重とメモ、睡眠と途中覚醒、食事、運動セッション、体調の全保存項目を取得可能であることを確認
- TXT・Markdown出力に必要な保存項目の不足は現時点で確認されなかったため、型と保存形式は変更しない
- 920px以下の1列カード、820px以下の上部ナビ、560px以下のフォーム・日付操作、320px以上のviewport追従を維持
- ライトは緑主体、ダークはオレンジ主体の既存CSS変数を使用し、状態表示は色だけでなく文言を併記する
- 見出し、button、label、aria-label、aria-pressed、aria-invalid、aria-describedby、role="alert"、aria-live、dialog見出し関連付けを確認
- 実機確認日：2026年7月17日
- 健康記録の正式な5カード順、各カードの見出し、日付操作、空状態、ボタン文言の統一を実機確認
- 体重・睡眠メモの3行省略と、不要な「今後追加予定」カードの削除を正式採用
- すべての健康カードが同じ`selectedDate`を共有し、健康プロフィールを設定で管理する構成を確認
- 健康入力dialogの終了条件、localStorageキーとversion 1形式、健康データ間の独立性を確認
- Health Export Phaseに必要な保存項目が揃っており、保存形式の変更が不要であることを確認
- ライト・ダークテーマ、レスポンシブ表示、アクセシビリティを確認し、Health Review Phaseを完了とする
- 次はHealth Export Phase「健康記録TXT・Markdown出力」へ進む

### Health Calendar Phase（完了）

- 既存の月間カレンダーへ、健康記録が1種類以上ある日を示す共通マークを追加
- 日記・メモと健康記録は日付セル右上の共通アイコン領域へ表示し、左に健康記録の`HeartbeatIcon`、右に日記・メモの`PencilSimpleIcon`を配置
- 健康記録または日記・メモの片方だけがある日は該当アイコンだけを表示し、右下の健康記録表示は使用しない
- 健康記録と日記・メモは色と形で区別する
- 健康記録の存在判定は、体重、睡眠、食事、運動、体調の既存stateを対象とする
- `healthSummary.ts`へ日付別健康概要、健康記録日の集合、運動セッション抽出、概要文生成を集約
- 選択日詳細へ「健康記録」セクションを追加し、保存されている項目だけを最大5行で表示
- 体重は小数第1位、睡眠は実睡眠時間と途中覚醒、食事は入力済み項目数、運動は件数・時間・計算可能な推定消費カロリー、体調は主要な設定済み状態を概要表示
- 健康記録がない日も空状態と「健康記録を開く」を表示し、同じ日付の日付別健康画面から新規入力できる
- 「健康記録を開く」で既存`selectedDate`を維持したまま健康記録の日付別画面へ移動する
- 日記・メモの操作文言を、曖昧な「記録を開く」から「日記・メモを開く」へ変更
- カレンダーと健康記録はAppが保持する同じ健康stateを参照し、保存・編集・削除を再読み込みなしで反映する
- 日付セルの`aria-label`へ「メモあり」「健康記録あり」を含め、健康概要は見出しとリストで読み上げ可能にした
- ライト・ダークテーマの既存CSS変数、共通レスポンシブ、カレンダー寸法を維持
- 保存形式、localStorageキー、versionは変更していない
- 健康記録専用カレンダーは実装せず、実際の使用後に必要であれば小型健康カレンダーを検討する
- 実機確認日：2026年7月17日
- カレンダー上で健康記録日の表示を確認
- 健康記録と日記・メモのアイコンを右上へ横並び表示し、左に`HeartbeatIcon`、右に`PencilSimpleIcon`を配置
- 健康だけ、日記だけ、両方ありの表示と、日付数字・予定・今日表示・選択枠へ重ならないことを確認
- 選択日詳細の健康概要と、「健康記録を開く」による同じ`selectedDate`の日付別健康画面への移動を確認
- 日記用ボタンは「日記・メモを開く」とし、健康記録の新規・編集・削除がカレンダーへ即時反映されることを確認
- localStorageキーとversionは変更していない
- 専用健康カレンダーは未実装とし、必要になった場合のみ将来検討する
- Health Calendar Phaseを完了とする
- 次工程はDaily Achievement Phaseとし、完了後にHealth Export Phaseへ進む

### Daily Achievement Phase（完了）

- 1日1件、最大120文字の「できたこと」を日記・メモとは別データとして保存
- `hootoDay.dailyAchievements`をversion 1ラッパー形式で管理し、新規・編集・削除と再読み込み後の維持に対応
- `hootoDay.monthlyAchievementSelections`をversion 1ラッパー形式で管理し、月ごとに1件を「いちばん頑張ったこと」として選択
- `MonthlyAchievementSelection`は本文を複製せず`selectedDate`で元の`DailyAchievement`を参照する
- 元本文を編集すると月のベスト表示も更新され、元記録を削除すると対応する月選択を解除する
- 一覧から月のベストを選択・変更・解除でき、王冠には正式exportの`CrownIcon`を使用
- 独立した月のベストカードを廃止し、「月間カレンダー」の固定見出しを表示年月へ変更
- 月選択と月のベストを同じコンパクトなヘッダー行へ配置し、左に前月・表示年月・次月、右に月のベスト操作を表示
- 未選択時だけ「今月のできたことを見る」を表示し、説明文はヘッダーへ表示しない
- 選択済み時は王冠、本文、選択日だけを1行表示し、補助的な「見る」ボタンやリンクは追加しない
- 選択済み表示全体を1つのbuttonとし、押すと対象月の一覧dialogを開いて変更・解除できる
- 画面最上部のAppHeaderを廃止し、重複していたHootoDayロゴ・名称と独立した「今日」ボタンを削除
- 「今日」操作をカレンダー年月操作へ移し、前月・表示年月・次月・今日・月のベスト操作を同じ領域へ統合
- AppHeader分の高さと余白を削除してメインコンテンツを上へ整理し、今日へ戻る既存動作は維持
- PC版はアイコン中心の細いサイドバーとし、通常時はラベルを隠してhover・focus時に右側へツールチップ表示
- PCナビは`aria-label`と`aria-current`を維持し、820px以下では従来の文字付きモバイルナビを使用
- サイドバー縮小分をメイン領域へ割り当て、カレンダーと選択日詳細の表示幅を拡張
- 月間カレンダーは月曜始まりを正式採用し、曜日順を月・火・水・木・金・土・日へ変更
- 月初オフセットを月曜基準へ変更し、土曜は青緑、日曜は赤の既存表示を維持
- 曜日行を日付グリッドのカードから分離し、月～日の各曜日を独立した小さな角丸ブロックとして表示
- 日付グリッドは曜日ブロックの下へ、セル背景と罫線を持つ単一グリッドとして表示
- 曜日行と日付グリッドを同じ親幅・gapなしの7列gridへ統一し、各曜日名の中心を対応する日付列の中心へ一致
- 日付グリッドを覆っていた白背景・padding・大きな角丸・影・外枠のカード要素を廃止
- ページ背景上へ曜日行と日付グリッドを直接配置し、日付セルの背景・罫線と最小限の外周だけを維持
- 二重カードに見えない軽い構成とし、曜日の視覚的な隙間は各列内の角丸ラベルで表現
- 当月外セルの背景をページ背景より一段濃くし、日付文字も当月より控えめな範囲で読みやすく調整
- 日付グリッド外周を専用テーマ変数で少し濃くし、レイアウトへ影響しない内向き2px outlineへ変更
- 内側のセル罫線は細いまま維持し、二重カードや強いshadowは追加しない
- 外周調整後も曜日と日付列の左右端・列幅・中心位置の一致を維持
- カレンダー全体の柔らかさを保ちながら、ページ背景・曜日・土日・外周へ適度なメリハリを追加
- ライトのページ背景を一段だけ緑味のある色へ調整し、当月平日の白いセルとの差を明確化
- 曜日ブロック専用の背景・枠・文字変数を追加し、ボタン風に強調しすぎない範囲で視認性を向上
- 土曜の淡い青緑と日曜の淡いピンクを一段だけ明確にし、当月外セルは控えめな配色を維持
- 日付グリッド外周は2pxのまま色だけを少し明確にし、内側罫線は濃くしない
- 選択日、今日、予定ラベルの既存の表示優先度は変更しない
- ライトテーマのメイン背景を一段深い低彩度セージグリーンへ変更し、白い当月平日セルとの明度差を強化
- 曜日ブロックはメイン背景より明るい階層を維持し、当月外セルは背景より控えめなグレーグリーンへ微調整
- 左サイドバーの薄いオレンジはアクセントとして維持し、メイン背景との色は入れ替えない
- 当月外、土曜、日曜、選択日、今日の配色方針とダークテーマの既存配色は維持
- 月移動には正式exportの`CaretCircleLeftIcon`と`CaretCircleRightIcon`を使用し、塗りつぶさない通常ウェイトで表示
- 予定、日記、健康記録、今日、選択日の各日付対応は変更しない
- DayDetailsの健康記録を最下部へ移し、初期状態と選択日変更時は閉じる折り畳み表示へ変更
- 閉じた状態でも「記録あり・種類数」または「記録なし」を表示し、開いた中に既存健康概要と「健康記録を開く」を表示
- 健康折り畳みはReact stateだけで管理して保存せず、`aria-expanded`と`aria-controls`を設定
- 狭幅では月選択と月のベストを自然に2行へ折り返す
- DayDetailsへ日記・メモとは独立した「今日のできたこと」を追加し、未記録時の新規入口と記録済み時の編集入口を表示
- カレンダーセルへ新しい存在アイコンは追加していない
- 日記、予定、健康記録、既存localStorageキーとは独立し、保存形式を変更していない
- 実機確認日：2026年7月17日
- 毎日1件のできたことを保存でき、新規・編集・削除と再読み込み後の維持を確認
- 月一覧の表示、月のベストの選択・変更・解除を確認
- 元本文の編集・削除が月のベストへ即時連動することを確認
- 選択済みの月のベストに王冠を表示し、未選択時だけ「今月のできたことを見る」を表示する構成を確認
- 選択済み表示全体から月一覧dialogを開けることを確認し、月のベストを年月操作と同じヘッダーへ正式統合
- AppHeaderを廃止し、「今日」ボタンを年月操作へ移動
- PCサイドバーをアイコン中心の細い表示へ変更し、820px以下のモバイルナビを維持
- 月曜始まりを正式採用し、曜日ブロックを日付グリッドから分離
- 曜日と日付列の位置が完全に一致することを確認し、カレンダー外側の大きなカードを廃止
- 月移動には`CaretCircleLeftIcon`と`CaretCircleRightIcon`を正式採用
- DayDetailsの健康記録を最下部の折り畳み表示へ変更
- ライトテーマ背景に低彩度セージグリーンを採用し、左サイドバーの薄いアイボリー・オレンジ系を維持
- ライト・ダーク・system、レスポンシブ表示、横スクロールがないことを確認
- Daily Achievement Phaseを完了とする
- 次工程はHealth Export Phase「健康記録TXT・Markdown出力」へ進む

### Health Export Phase（完了）

- 既存の「出力・バックアップ」を正式な画面として接続し、日付別記録のTXT・Markdown出力を実装
- 出力対象は日記・メモ、毎日のできたこと、体重、睡眠、食事、運動、体調、月のベスト
- 予定は今回の健康記録出力には含めない
- 期間は1日、基準日を含む過去7日、過去15日、基準日のカレンダー月、任意の期間指定に対応
- 期間指定は両端を含めて最大366日とし、開始日が終了日より後の場合や不正日付は生成しない
- 日付はローカル日付として扱い、年またぎとうるう年に対応
- 日付昇順で、実際に保存されている項目だけを出力し、記録のない日は省略
- 期間内に出力対象が1件もない場合は空状態を表示し、コピーとファイル保存を無効化
- 月のベストは期間に含まれる月を対象とし、DailyAchievementの参照先が有効な場合だけ月順に出力
- TXTはChatGPT等へ貼りやすい日本語ラベル付きプレーンテキストとして生成
- MarkdownはObsidianへ保存しやすい日付h2・記録種類h3・運動セッションh4の見出し構造で生成
- MarkdownをHTMLへ変換せず、生成される文字列をそのままプレビュー
- 生成処理はReactやDOMへ依存しない`healthExport.ts`の純粋関数を中心に分離
- クリップボードコピーと、UTF-8のBlobを使用したTXT・Markdownファイル保存に対応
- ファイル名は1日の場合`HootoDay_YYYY-MM-DD`、期間の場合`HootoDay_開始日_終了日`とする
- プレビューは読み取り専用、折り返し、最大高さ付きの縦スクロール表示
- 出力はAppが管理する既存の各配列をpropsで参照し、HookやlocalStorageの二重読み込みは行わない
- 保存済みデータは読み取り専用で扱い、既存localStorageキーとversion 1形式を変更しない
- 対象データの個別ON/OFF、予定出力、JSONバックアップ・復元は未実装
- 内容は端末内だけで生成し、外部送信、自動アップロード、AI要約、医療判断を行わない
- 健康・日記本文をconsoleへ出力しない
- ライト・ダーク、PCアイコンサイドバー、820px以下のモバイルナビ、320px以上のレスポンシブ表示に対応
- 実機確認日：2026年7月17日
- Markdownの1日出力で、保存済み項目だけが表示されることを確認
- 日記・メモや体調メモ等の改行が維持され、Markdownの見出し構造が崩れないことを確認
- プレビューとクリップボードへコピーした内容が一致し、日本語と改行が維持されることを確認
- Markdownファイル保存、正しいファイル名、UTF-8で文字化けしないこと、プレビューとの内容一致を確認
- TXTの過去7日出力、日付昇順、記録のない日の省略、保存済み項目だけの出力を確認
- 月のベストが該当月分だけ日付ブロックの末尾へ出力されることを確認
- 期間指定で開始日が終了日より後の場合にエラーとなり、修正後は正常なプレビューへ戻ることを確認
- エラー時と空状態ではコピー・ファイル保存を行わないことを確認
- 出力処理は読み取り専用で、既存の保存データ、localStorageキー、versionを変更しない
- 内容は端末内だけで生成し、外部通信、自動送信、自動アップロードを行わない
- Health Export Phaseを完了とする
- 次工程はJSON Backup Phase「JSONバックアップ・復元」へ進む

### JSON Backup Phase（完了）

- HootoDayのテーマ、予定、日記・メモ、健康プロフィール、体重、睡眠、食事、食事定型、運動、体調、毎日のできたこと、月のベスト選択を1つのJSONへバックアップ
- バックアップ全体は`app: "HootoDay"`、`formatVersion: 1`、ISO 8601形式の`createdAt`、全正式データを収める`data`で構成
- localStorageのラッパーを複製せず、Appが保持する最新の正規化済みstateから生成
- 手動バックアップのファイル名は`HootoDay_backup_YYYY-MM-DD_HH-mm-ss.json`、復元直前の自動退避は`HootoDay_before_restore_YYYY-MM-DD_HH-mm-ss.json`
- JSONはUTF-8、2スペースインデント、MIME typeは`application/json;charset=utf-8`とし、端末名やWindowsパス等を含めない
- 復元ファイルは10MBを上限とし、上限超過時は本文を読み込まず復元を無効化
- `unknown`からバックアップ全体を厳格に検証し、他アプリ、未対応version、必須キー欠落、不正な型・日付・enum・数値・文字数を拒否
- 一部だけ不正な場合も不正項目を除外せず、バックアップ全体を拒否して現在データを変更しない
- 1日1件・1月1件データの重複、ID重複、食事定型の並び順不整合、存在しない「できたこと」を参照する月のベスト等を拒否
- 復元前プレビューはファイル名、作成日時、テーマ、健康プロフィールの有無、各データ件数だけを表示し、本文や健康値は表示しない
- ファイル選択と検証だけではlocalStorageやReact stateを変更せず、検証成功時だけ「復元する」を有効化
- 最終確認はネイティブdialogを使用し、キャンセルとEscapeで閉じ、backdropクリックでは閉じない
- 復元実行前に現在stateから自動バックアップのダウンロードを開始し、生成または開始に失敗した場合は復元を中止
- 復元直前に全対象localStorageキーの元値とキー未存在状態をメモリへ退避
- 検証済みデータを既存の各version 1ラッパー形式へ変換して全キーへ保存し、途中失敗時は元値へロールバック
- ロールバック自体の失敗時は、自動保存された復元前バックアップの保管を促す明確なエラーを表示
- localStorageの全保存成功後だけ各Hookの一括置換関数でReact stateを更新し、ページ再読み込みなしでテーマ、カレンダー、健康記録、出力へ即時反映
- 復元成功後は確認dialog、選択ファイル、件数プレビューをクリアし、aria-liveで成功を通知
- バックアップと復元は端末内だけで処理し、外部通信、自動アップロード、本文や健康値のconsole出力を行わない
- 既存の12個のlocalStorageキーと各version 1形式は変更しない
- 実機確認日：2026年7月17日
- 「バックアップを保存」から手動JSONバックアップを保存でき、`HootoDay_backup_YYYY-MM-DD_HH-mm-ss.json`のファイル名を確認
- 保存JSONの`app: "HootoDay"`、`formatVersion: 1`、UTF-8の日本語表示を確認
- 正常なHootoDayバックアップの検証成功と、作成日時、テーマ、健康プロフィール有無、各データ件数のプレビューを確認
- ファイル選択だけでは復元されず、現在データが変化しないことを確認
- 最終確認dialogの内容と、キャンセル時に現在データが変化しないことを確認
- 復元直前に`HootoDay_before_restore_YYYY-MM-DD_HH-mm-ss.json`で現在データが自動退避されることを確認
- 復元成功通知と、復元内容が再読み込みなしでReact stateへ即時反映されることを確認
- ページ再読み込み後も復元内容が維持されることを確認
- 既存localStorageキーとversion 1形式を維持
- 復元は端末内で処理し、外部通信、本文や健康値のconsole出力を行わない
- JSON Backup Phaseを完了とする
- 次工程は運動まとめ・体調まとめ等の優先順位を整理する

### Exercise Summary Phase（完了）

- 健康記録画面へ、保存済み`exerciseSessions`を読み取り専用で集計する「運動まとめ」を追加
- 日付別記録、体重まとめ、睡眠まとめに続く4番目の表示切り替えとして配置
- 集計期間は、基準日を含む過去7日、基準日のカレンダー月、基準月を含む過去6か月、基準日のカレンダー年
- まとめ専用の基準日は最新の運動記録日を初期値とし、0件の場合は現在のローカル日付を使用
- 基準日は日付inputから変更でき、共通`selectedDate`とlocalStorageには保存しない
- 合計回数、運動日数、合計時間、計算可能な記録の推定消費カロリー合計を表示
- 1回あたりの平均運動時間と、期間日数から週換算した1週間あたり平均運動日数を表示
- 1週間と1か月は日別、半年と1年は月別に合計運動時間を集計し、追加ライブラリなしのSVG棒グラフで表示
- 推移には0分の日・月も含め、グラフに加えて運動があった日・月の数値一覧を表示
- 種類別に回数、合計時間、計算可能な推定消費カロリー、全運動時間に占める割合を集計
- 種類別は合計時間、回数、既存運動種類定義順で安定して並べ、記録のない種類は表示しない
- 期間内の記録を日付降順、同日内は既存の作成順でグループ表示し、種類、時間、心拍数、METs、推定消費カロリー、メモ概要を確認可能
- 記録がない期間は0値カードを並べず、「この期間の運動記録はありません」と日付別記録への入口を表示
- 集計処理は`exerciseSummary.ts`の純粋関数へ分離し、Appが保持する既存stateを使用してlocalStorageを再読込しない
- 新規入力、編集、削除、JSON復元後の`exerciseSessions`変更を再読み込みなしで集計へ反映
- 医療評価、達成判定、運動不足判定、目標比較は行わず、保存済み推定値を本人の振り返り用として端末内で集計
- 既存の`hootoDay.exerciseSessions`キー、version 1形式、`ExerciseSession`型は変更しない
- 実機確認日：2026年7月17日
- 1か月集計の合計回数、運動日数、合計時間、推定消費カロリーが実データと一致することを確認
- 種類別の回数、合計時間、全運動時間に占める割合と、時間の多い順の並びを確認
- 記録のない運動種類が種類別集計へ表示されないことを確認
- 1か月の日別グラフと、半年・1年の月別グラフを確認
- 運動記録の編集後、概要、種類別、グラフ、一覧が再読み込みなしで同時に更新されることを確認
- 運動まとめは保存済み`exerciseSessions`を表示時に集計する読み取り専用画面として正式採用
- `hootoDay.exerciseSessions`、version 1形式、`ExerciseSession`型は変更していない
- 医療評価、達成判定、運動不足判定、AI評価を行わない
- Exercise Summary Phaseを完了とする
- 次工程はCondition Summary Phase「体調まとめ」

### Condition Summary Phase（完了）
- Appが管理する既存の`conditionRecords`を読み取り専用で集計する「体調まとめ」画面を追加
- 健康記録の表示切り替えを、日付別記録、体重まとめ、睡眠まとめ、運動まとめ、体調まとめの正式な5画面構成へ拡張
- 集計期間は、基準日を含む過去7日、基準日のカレンダー月、基準月を含む過去6か月、基準日のカレンダー年に対応
- まとめ専用の基準日は最新の体調記録日を初期値とし、記録0件では現在のローカル日付を使用。共通`selectedDate`やlocalStorageへ保存しない
- 記録日数、全体の体調を設定した日数、膝を記録した日数、腰を記録した日数を概要カードへ表示
- 全体の体調は良い、普通、悪い、膝と腰は問題なし、少し気になる、痛い、強く痛いの件数と割合を、既存`conditionOptions.ts`の定義順で集計
- 未設定は状態別集計から除外し、割合の分母は各状態項目を設定済みの記録数とする。自動評価やスコア化は行わない
- 全体、膝、腰を切り替えて、1週間・1か月は日別、半年・1年は月別の状態件数を表示する推移グラフを追加
- グラフは色だけに依存せず、日付または月、状態名、件数を文字一覧とaria-labelでも確認可能
- 生理・周期メモ、気になること、自由メモは空白以外の本文がある日数だけを集計し、本文は概要カードへ表示しない
- 期間内の体調記録を日付降順で一覧表示し、設定済み状態と入力済み文章だけを表示。長文は最大3行程度に省略し、まとめ画面から直接編集しない
- 記録0件では「この期間の体調記録はありません」と日付別記録への入口だけを表示
- 本人の記録の振り返り用とし、医療判断、診断、病名推定、緊急度判定、生理周期予測を行わない
- `hootoDay.conditionRecords`、version 1形式、`DailyConditionRecord`型、既存の保存データは変更していない
- ライト・ダーク・system、320px以上のレスポンシブ、キーボード操作と読み上げ情報に対応
- 実機確認日：2026年7月17日
- 1か月の記録日数、全体の体調記録日数、膝記録日数、腰記録日数が実データと一致することを確認
- 状態別件数と割合、未設定が集計へ含まれないこと、記録のない状態が表示されないことを確認
- 全体・膝・腰の推移切り替えと、グラフ・文字一覧の連動を確認
- 1か月の日別推移、半年・1年の月別推移を確認
- 体調記録一覧の日付降順、長文の3行省略、未設定と空欄の非表示を確認
- 新規保存、編集、削除、JSON復元後の変更がApp管理stateから即時反映される読み取り専用集計画面であることを確認
- `hootoDay.conditionRecords`、version 1形式、`DailyConditionRecord`型を変更していないことを確認
- 医療診断、病名推定、緊急度判定、生理周期予測を行わず、本文や健康内容をconsoleへ出力しない方針を確認
- Condition Summary Phaseを完了とする
- 次工程はHootoDay全体の次優先機能を整理する

### v1 Review Phase（完了）
- Condition Summary Phaseまでに実装した全画面・全保存機能を、個人利用版v1相当の基準点として統合確認
- Appの正式画面はカレンダー、健康記録、記録を見る、出力・バックアップ、設定。PCアイコンサイドバーと820px以下のモバイルナビで同じ表示名・アイコン・順序を使用
- 未接続だった「記録を見る」を、h1と短い準備中案内だけの読み取り専用画面へ接続。既存データの表示・編集や大きな仮カードは追加していない
- 記録を見る画面の横断一覧・検索・集計は未実装であり、将来工程として維持
- カレンダーは月曜始まり、曜日ブロック、日付グリッド、月移動、今日、月のベスト、予定・日記・健康マーク、selectedDate、DayDetailsの既存仕様を維持
- DayDetailsは選択日、予定、日記・メモ、今日のできたこと、操作、健康記録折り畳みの順序を維持。日記操作を新規時「日記・メモを書く」、保存済み時「日記・メモを編集」へ明確化
- DayDetailsから健康画面へ移動する操作を「この日の健康記録を見る」とし、同じselectedDateで日付別健康記録へ移動する
- 予定、日記・メモ、毎日のできたこと、月のベストの新規・編集・削除・保存・表示連動と各version 1形式を監査
- 日付別健康記録は体重、睡眠、食事、運動、体調メモの正式順と、単一selectedDate・共通HealthDateNavigatorを維持
- 健康まとめは日付別記録、体重まとめ、睡眠まとめ、運動まとめ、体調まとめの順序、読み取り専用集計、期間操作、空状態、グラフ・文字情報を維持
- TXT・Markdown出力とJSONバックアップ・復元はセクションを分離し、プレビュー、コピー、ファイル保存、検証、復元前バックアップ、ロールバック、即時反映の既存構造を維持
- 設定はテーマと健康プロフィールの2セクションを維持し、設定dialogを閉じてからHealthProfileDialogを開くことでmodal競合を防止
- 入力dialogは保存、キャンセル、閉じる、Escape、削除完了で終了し、backdropクリックでは閉じない。ThemeSettingsだけは既存例外としてbackdropクリック終了を維持
- 空状態は「この日の○○記録はありません」「この期間の○○記録はありません」を基本とし、未記録を0値として誤表示しない
- カレンダー、健康記録、記録を見る、出力・バックアップの各画面に1つのh1を持たせ、dialogは関連付けたh2を使用
- 正式localStorageキー12件のスペル、version 1、ラッパー形式、不正データ検証、空配列・null、各Hookの一括置換、JSON復元対象を監査
- 正式キーは`hootoDay.theme`、`hootoDay.events`、`hootoDay.dayMemos`、`hootoDay.healthProfile`、`hootoDay.weightRecords`、`hootoDay.sleepRecords`、`hootoDay.mealRecords`、`hootoDay.mealTemplates`、`hootoDay.exerciseSessions`、`hootoDay.conditionRecords`、`hootoDay.dailyAchievements`、`hootoDay.monthlyAchievementSelections`
- PCサイドバー、モバイルナビ、カレンダー、DayDetails、健康カード・タブ、グラフ、一覧、出力、バックアップ、設定、dialogのレスポンシブ構造を監査。横超過を`overflow-x: hidden`だけで隠す構造へ戻していない
- ライトはセージグリーン・アイボリー・白・緑・オレンジ、ダークは暖色寄りダーク・オレンジ主体の既存テーマ変数を維持
- button、label、aria-current、aria-pressed、aria-expanded、aria-controls、aria-invalid、aria-describedby、aria-live、role="alert"、dialog見出し、focus-visible、グラフの文字情報を監査
- App内の各データHookは1回だけ呼び出し、画面コンポーネントからlocalStorageを直接再読込しない。重い集計はuseMemoまたは純粋関数へ分離し、元配列を破壊しない
- アプリの記録本文・健康内容を外部送信またはconsole出力する処理、自動アップロード、AI評価、医療診断はない
- 既存のM PLUS Rounded 1cはGoogle Fontsから取得するため、フォントファイル取得の外部通信のみ存在する。予定・日記・健康等の保存データは送信しない
- TXT・Markdown出力、JSON保存・復元はユーザーの明示操作時だけ実行し、復元プレビューでは本文ではなく件数を表示
- 個人利用版v1相当の条件である主要画面、カレンダー・予定・日記・健康の連動、毎日のできたこと・月ベスト、健康入力と各まとめ、TXT・Markdown出力、JSONバックアップ・復元、PC・モバイル・テーマ、version管理が揃っている
- v1相当は配布版・一般公開版を意味せず、個人利用版の安定した基準点とする
- 未実装は、記録を見る画面の本実装、即売会・在庫記録、PWA、Supabase等の同期、通知、CSV・PDF出力
- 最狭幅で月ベスト領域が縦長になる問題を修正。821～920px向けの横方向の`flex-basis: 320px`が、820px以下の縦並びでは高さ方向へ作用していたため、狭幅側でflex伸長と固定basisを解除
- 820px以下では年月操作と月ベストを上下配置し、月ベストは内容に応じた自然な高さで表示
- 選択済み月ベストは王冠・本文・日付のみを最大2行程度でコンパクトに表示し、未選択時は「今月のできたことを見る」のみを表示
- 巨大な空白カードや固定高さを追加せず、月操作、曜日行、日付グリッドの自然な間隔を維持
- 実機確認日：2026年7月17日
- 記録を見る準備中画面、日記・メモの新規・編集文言、「この日の健康記録を見る」を実機確認
- カレンダーで選択した同じ`selectedDate`を維持して日付別健康記録へ移動し、各健康カードへ連動することを確認
- ライト、ダーク、systemの各テーマと、PC・狭幅の表示を確認。確認後のテーマはsystem
- 320px付近でも月ベストが巨大な空白にならず、王冠・本文・日付が最大2行程度に収まり、表示全体から月一覧dialogを開けることを確認
- 狭幅不具合は、821～920px用の`flex: 1 1 320px`が縦並び時に高さ方向へ作用したことが原因。820px以下で`flex: 0 0 auto`として正式修正
- 主要画面、正式localStorageキー、version 1形式、外部送信を行わないプライバシー方針を最終監査
- 重大な既知不具合はなく、個人利用版v1相当の基準点として正式採用
- v1 Review Phaseを完了とし、次工程は未実装機能の優先順位を整理する

### Records Browser Phase（完了）
- 「記録を見る」の準備中画面を、保存済み文章記録を横断して閲覧できる読み取り専用画面へ置き換え
- 検索対象は、日記・メモ、毎日のできたこと、体重メモ、睡眠メモ、生理・周期メモ、気になること、体調メモ
- 予定、食事本文、食事定型、運動記録、体重・睡眠等の数値、月のベスト単体、健康プロフィールは初期対象外
- Appで管理中の各stateから共通`RecordBrowserItem`を生成し、localStorageの直接再読込やHookの二重呼び出しを行わない
- 本文と種類ラベルを対象に、前後空白を除いた部分一致検索を実施。英字は大文字・小文字を区別せず、検索語と検索履歴は保存しない
- 種類フィルターは、すべて、日記・メモ、今日のできたこと、体重メモ、睡眠メモ、体調関連を用意
- 期間フィルターは、すべて、過去7日、過去30日、過去半年、期間指定を用意。過去半年は5か月前の月初から今日までを正式仕様とする
- 期間指定は開始日・終了日の両端を含み、未入力、不正日付、開始日が終了日より後の場合は結果0件と誤表示せずエラーを表示
- 新しい順・古い順を選択可能。同日内は、今日のできたこと、日記・メモ、体重メモ、睡眠メモ、生理・周期メモ、気になること、体調メモの順
- 検索結果は日付ごとにグループ化し、種類ラベルと改行を維持したプレーンテキスト本文を表示
- 長文は初期状態で最大5行程度に省略し、「全文を見る」「閉じる」で切り替える。開閉状態は保存しない
- 日記・メモと毎日のできたことから同日のカレンダーへ、健康メモから同日の日付別健康記録へ移動可能
- 同日付移動では共通`selectedDate`と`displayMonth`を更新し、健康画面はまとめではなく日付別記録を表示
- 保存記録0件と検索条件による0件を区別し、検索条件リセットは画面stateだけを初期化
- 外部通信、AI要約、医療評価、本文のconsole出力、保存データの書き換えを行わない
- 既存localStorageキーとversion 1形式は変更しない
- 実機確認で各記録カードが大きく一覧が間延びして見えたため、検索結果の表示密度を調整
- 921px以上では、種類ラベル・本文・移動操作の3領域によるコンパクトな行表示を正式採用
- 短文記録は固定高さを持たず、日付グループ内を細い区切り線と小さな余白で整理
- 長文だけ「全文を見る」で必要な高さへ拡張し、既存の省略・開閉機能を維持
- 820px以下では種類、本文、全文操作、移動操作を1列のコンパクト表示へ切り替える
- 移動ボタンの表示文言を「日記・メモを見る」「カレンダーを見る」「健康記録を見る」へ統一して短縮し、aria-labelには対象日付と目的を明記
- 検索、フィルター、並び順、同日付移動、全文表示、空状態、保存形式は変更しない
- 実機確認日：2026年7月18日
- 7種類の文章記録が横断表示され、空欄本文が除外されることを確認
- 日付別グループ、PC版の種類・本文・移動操作の3領域表示、同日内の細い区切り線を確認
- 短文記録へ大きな空白が発生せず、長文の全文表示・収納が動作することを確認
- 移動ボタン文言を統一し、aria-labelへ対象日付と移動目的を含めることを確認
- 820px以下の縦並びと、320px付近で文字切れ・横スクロールがないことを確認
- 検索、種類・期間フィルター、並び順、条件リセットを維持することを確認
- 同日のカレンダーと日付別健康記録へ移動できることを確認
- 読み取り専用とし、既存localStorageキー・version 1形式・保存データを変更しないことを確認
- 外部通信、AI要約、本文のconsole出力を行わないことを確認
- `RecordsPlaceholder`を本実装へ置き換え、未使用コードを残していないことを確認
- Records Browser Phaseを完了とし、次工程は即売会・在庫記録等の優先順位を整理する

### Inventory Foundation Phase（完了）
- 商品マスターを追加し、商品IDでイベントをまたいだ共通管理を行う
- 商品名・通常価格・カテゴリ・初期在庫・販売状態・メモと、BOOTH表示名・価格・URL・販売枠を管理
- 過去のイベント・BOOTH販売には、選択時点の商品名と価格をsnapshotとして保存
- 共通実在庫は初期在庫と在庫増減履歴から計算し、現在庫そのものは保存しない
- 入荷・追加製造、イベント販売、サンプル配布、BOOTH販売、キャンセル、返品、その他増減を履歴化
- イベント持込数とBOOTH販売枠だけでは実在庫を減らさず、実販売・配布時だけ減算
- 負の在庫になる販売・減少調整は保存前に拒否し、不足数を画面へ表示
- 商品名・カテゴリ検索、販売終了商品の表示切替、商品IDによる選択を実装
- イベント販売は既存予定IDへ紐付け、持込残数と商品別売上を表示
- BOOTH注文は未発送・発送済み・キャンセルを手動入力し、購入者名・住所・メールは保存しない
- BOOTHキャンセルは在庫を減らさず、再有効化時は在庫不足を再検証
- `hootoDay.products`、`hootoDay.inventoryMovements`、`hootoDay.eventSalesRecords`、`hootoDay.boothSalesRecords`をversion 1形式で追加
- JSONバックアップ全体をformatVersion 2へ更新し、新規4データ、自動退避、復元、ロールバックへ追加。旧formatVersion 1は新規データを空配列として復元可能
- 健康記録のTXT・Markdown出力には販売・在庫データを混在させない。在庫・イベント・BOOTHレポートは未実装
- 端末内の手動管理とし、BOOTH自動通信、税務判断、送料・手数料・利益計算は行わない
- PC・モバイルナビへ「販売・在庫」を追加し、在庫一覧、商品管理、イベント販売、BOOTH販売、在庫履歴の5画面を用意
- commitはユーザーの実機確認後に行う

### Inventory Foundation Phase 追加調整（完了）
- `Product.firstSaleEventId`を追加し、初売りイベントを任意のカレンダー予定IDで参照する構造にした
- 初売り設定だけでは在庫履歴、持込数、販売数、売上を作成せず、実在庫にも影響しない
- 商品登録・編集では「設定しない」「登録済みの予定から選ぶ」「新しいイベント予定を作る」の3方式を用意した
- 商品登録中に作る予定はイベント名・開催日を必須、開始時刻・メモを任意とし、カテゴリは「即売会」固定とした
- 新規作成した即売会予定は通常の`hootoDay.events`へ保存し、商品には予定名ではなくIDを保存する
- カレンダー先行と商品先行の両フローを正式採用し、商品編集時も初売り予定の変更・解除・新規作成を可能にした
- 商品一覧へ設定済みの初売り予定を表示し、不明な参照は警告表示して再設定可能にした
- イベント販売入力から即売会予定を新規作成し、作成した予定をそのまま選択できる入口を追加した
- 選択中イベントを初売りに設定した商品を販売候補として表示する。販売記録の自動作成や在庫の自動減算は行わない
- 即売会カテゴリを予定型、保存検証、カレンダー、予定入力、DayDetails、JSONバックアップへ追加した
- 即売会のDayDetailsには販売記録の商品数、販売数、サンプル数、売上と販売・在庫画面への入口を表示する
- 初売り参照だけがある予定の削除時は解除商品数を確認し、了承後に参照を`null`へ戻す。販売記録がある予定は削除を拒否する
- 初売りイベント変更・解除時も過去のイベント販売記録とsnapshotは変更しない
- `hootoDay.products`と`hootoDay.events`はlocalStorage version 1を維持し、旧商品は`firstSaleEventId: null`として読み込む
- JSONバックアップformatVersion 2では`firstSaleEventId`が`data.events`内のIDを参照することを検証し、不正参照を拒否する
- 外部通信、BOOTH API接続、自動送信は行わず、商品名・イベント名・売上をconsoleへ出力しない
- Inventory Foundation Phaseの実機確認完了に伴い、この追加調整も完了とする

### Inventory Foundation Phase 画面統合・準備中販売（完了）
- 「在庫一覧」と「商品管理」を「在庫・商品」へ統合し、販売・在庫内を「在庫・商品」「イベント販売」「BOOTH販売」「在庫履歴」の4タブに整理した
- `EventSalesRecord.status`へ`planned`と`completed`を追加した
- plannedでは販売数・サンプル数を`null`で保存し、nullは未入力、0は実績0件として区別する
- 持込数・当日単価だけで準備中保存でき、plannedでは在庫履歴を作らず売上・残数を未確定として表示する
- completed確定時だけ販売数を`eventSale`、サンプル数を`eventSample`として在庫へ反映する
- completed編集時は自分自身の既存在庫減少分を戻して不足検証し、対応履歴を置換して二重計上を防ぐ
- completedからplannedへ戻すと販売数・サンプル数をnullへ戻し、対応履歴を解除して在庫を戻す
- planned削除は在庫へ影響せず、completed削除は対応履歴を削除して在庫を戻す
- plannedの持込予定が現在庫を超えても保存可能とし、completed確定時だけ在庫不足を拒否する
- イベント画面とDayDetailsでは準備情報と確定実績を分離し、plannedを販売0件・売上0円として集計しない
- `hootoDay.eventSalesRecords`はversion 1を維持し、旧statusなし記録はcompletedとして読み込む
- JSONバックアップformatVersion 2でplanned/completedとInventoryMovementの数量・紐付け整合性を検証する
- 次工程としてFull Data Reset Phaseを記録する。テーマ以外の予定、日記、健康、実績、商品、在庫、販売データを対象とし、実行前JSON自動バックアップ、「初期化」の文字入力確認、途中失敗時ロールバックを行う
- イベント販売画面の「新しい即売会予定を作る」を折り畳み表示へ変更し、初期状態は閉じて必要な場合だけ入力欄を展開する
- 折り畳みを閉じてもイベント名・開催日・開始時刻・メモの入力途中内容はReact stateで維持し、開閉状態は保存しない
- 新規予定の作成成功後は入力内容を初期化して折り畳みを閉じ、作成失敗時は入力内容と開いた状態を維持する
- 既存予定の選択欄は常時表示し、新規作成した予定を自動選択する既存動作を維持する
- 予定作成の確定ボタンを折り畳み入力ブロックの末尾へ統合し、「この内容で即売会予定を作成」の主要操作として表示する
- 折り畳み外側にあった独立した作成ボタンは表示・操作対象から外し、入力から予定作成までを同じブロック内で完結させる
- 実機確認で予定作成ボタンが親のイベント販売formをsubmitしていた不具合を確認し、`type="button"`から予定作成専用ハンドラーだけを呼ぶ構造へ修正した
- 新規予定の必須不足エラーは折り畳み内の該当欄直下へ表示し、親のイベント販売保存検証と分離した
- イベント販売保存のエラーは商品・予定・持込数・単価・販売数・サンプル数等の対応欄付近へ表示し、最初の不正欄へ自動スクロール・フォーカスする
- 入力修正時は対応する項目エラーだけを解除し、上部は「入力内容を確認してください。」の短い概要表示とする
- 実機確認日：2026年7月18日
- 商品登録、初期在庫、BOOTH設定、初売りイベントの新規作成とカレンダー反映を確認した
- 初売り設定だけでは在庫が変わらず、在庫一覧と商品管理を「在庫・商品」へ統合した4タブ構成を確認した
- 準備中保存では在庫非反映、販売数・サンプル数のnullと実績0件を区別することを確認した
- 実績確定時の在庫減少・売上計算、編集時の差分反映、確定取り消しと削除時の在庫復元を確認した
- 新規即売会予定欄の折り畳み、入力ブロック内の`type="button"`作成操作、親form submit防止、自動選択・初期化・収納を確認した
- 新規予定と販売記録の個別エラー表示、最初の不正欄への自動スクロール・フォーカス、入力修正時の個別解除を確認した
- BOOTHの未発送・発送済み・キャンセルによる在庫連動と、予定削除時の初売り参照・販売記録保護を確認した
- JSONバックアップformatVersion 2、旧formatVersion 1復元互換、不正参照・planned/completed・在庫履歴整合性の検証を確認した
- 既存localStorageキーと各version 1形式を維持し、外部通信、購入者個人情報保存、商品名・イベント名・売上のconsole出力を行わない
- Inventory Foundation Phaseを完了とし、次工程はFull Data Reset Phaseとする

### Full Data Reset Phase（完了）
- 「出力・バックアップ」画面の最下部へ「データ管理」を追加し、テーマ設定以外の保存データを一括初期化できるようにした
- 対象は予定、日記・メモ、健康プロフィール、体重、睡眠、食事、食事定型、運動、体調、毎日のできたこと、月のベスト、商品、在庫履歴、イベント販売、BOOTH販売の15キー
- `hootoDay.theme`は初期化対象外とし、選択中のライト・ダーク・system設定を維持する
- 実行前にformatVersion 2の全データを`HootoDay_before_full_reset_YYYY-MM-DD_HH-mm-ss.json`として自動保存する
- バックアップ作成に失敗した場合は保存データを変更しない
- 確認dialogでは対象件数だけを表示し、正確に「初期化」と入力した場合だけ実行できる
- 初期化は対象キーを削除せず、各データのversion 1空ラッパー形式へ書き換える。健康プロフィールは`profile: null`とする
- 書き込み前に対象キーの値をメモリへ退避し、途中失敗時は元の値へロールバックする
- localStorageへの書き込みがすべて成功した後だけReact stateを空へ置き換え、画面へ即時反映する
- `localStorage.clear()`は使用せず、対象外キーやテーマ設定を変更しない
- 確認dialogはbackdropクリックでは閉じず、キャンセルとEscapeで閉じられる
- 実機確認日：2026年7月18日
- 出力・バックアップ画面最下部のデータ管理セクションと、初期化対象15種類の件数概要を確認した
- テーマが初期化対象外であることと、system設定が初期化前後で維持されることを確認した
- 正確な「初期化」の文字入力確認、キャンセル、Escape、backdropクリック非終了を確認した
- 実行前にformatVersion 2の全正式データが`HootoDay_before_full_reset_YYYY-MM-DD_HH-mm-ss.json`として保存されることを確認した
- テーマ以外の全対象データを正式なversion 1空状態へ戻し、`localStorage.clear()`を使用しないことを確認した
- 初期化後にカレンダー、健康記録、各まとめ、記録を見る、出力、商品・在庫、イベント販売、BOOTH販売、在庫履歴へ空状態が即時反映されることを確認した
- ページ再読み込み後も全対象データの空状態とテーマ設定が維持され、不正データ扱いや白画面にならないことを確認した
- 初期化前のメモリ退避、途中失敗時の全対象キーロールバック、全保存成功後だけReact stateを空へ置換する順序を正式採用した
- 端末内処理とし、外部通信、自動アップロード、本文・健康値・商品名・売上のconsole出力を行わないことを確認した
- 正式運用開始前のテストデータ初期化が完了した
- Full Data Reset Phaseを完了とし、次工程はHootoDay全体の次優先機能を整理する

### 運動記録 正式種目追加（完了）
- 店内を歩いて買い物する日常活動として「イオンでお買い物」を正式種目へ追加した
- 内部値は`aeonShopping`、表示名は「イオンでお買い物」、初期METsは2.8、初期時間は60分とする
- 種目選択時にMETs 2.8と60分を設定し、時間とMETsは既存どおりユーザーが変更できる
- 保存済み記録の編集では保存済みの時間・METsを優先し、dialogを開いただけでは初期値へ戻さない
- 日付別運動カード、運動一覧、運動まとめの種類別集計・グラフへ独立した運動種類として反映する
- TXT・Markdown出力では内部値を出さず、共通表示名変換により「イオンでお買い物」と表示する
- localStorageキー`hootoDay.exerciseSessions`とversion 1形式を維持する
- JSONバックアップformatVersion 2と旧formatVersion 1復元互換を維持し、新種目を正式値として検証する
- METsと推定消費カロリーは本人の振り返り用の概算値であり、医療評価には使用しない
- 実機確認日：2026年7月18日
- 種目選択直後に初期時間60分・初期METs 2.8となり、どちらも保存前に変更できることを確認した
- 保存後の日付別運動カードへ「イオンでお買い物」と表示され、内部値`aeonShopping`を画面へ表示しないことを確認した
- 編集dialogでは保存済みの時間・METsを維持し、自動的に60分・2.8へ戻さないことを確認した
- 運動まとめの種類別集計、合計回数、運動日数、合計時間、推定消費カロリー、グラフ、記録一覧へ独立種目として反映されることを確認した
- TXT・Markdown出力へ正式表示名「イオンでお買い物」を使用することを確認した
- JSONバックアップformatVersion 2へ対応し、`hootoDay.exerciseSessions`のversion 1形式と旧formatVersion 1復元互換を維持する
- 既存種目の内部値・表示名・METs・初期時間は変更していない
- 運動記録の正式種目追加を完了とし、次工程はHootoDay正式運用中の改善事項を整理する

### 運動記録一覧 グループ表示（完了）
- 運動まとめ内の「運動記録一覧」で、同じ日・同じ運動種類の記録を表示上だけグループ化する
- 元の運動記録は個別データのまま維持し、2件以上の場合だけ1枚のまとめカードとして表示する
- まとめカードには運動種類、回数、合計時間、保存済み推定消費カロリーの合計を表示する
- METs、心拍数、メモ、記録時刻はまとめカードへ集約せず、展開した個別記録内へ表示する
- 展開後は元IDの各記録を既存dialogで編集でき、個別削除も可能とする。一括編集・一括削除は追加しない
- 編集・削除後は保存済み配列から即時に再グループ化し、残り1件なら従来の個別表示、0件ならグループを削除する
- 1件だけのグループは従来に近い個別表示を維持する
- 期間条件で絞り込んだ記録だけを対象に、日付降順の既存順序を維持してグループ化する
- 展開状態は画面内だけで管理し、localStorageやJSONバックアップへ保存しない
- TXT・Markdownは従来どおり個別記録単位の出力を維持する
- `hootoDay.exerciseSessions`のversion 1、JSONバックアップformatVersion 2、旧formatVersion 1復元互換を維持する
- 表示専用の改善とし、ExerciseSessionの保存形式や推定消費カロリー計算、全体集計、グラフ元データを変更しない
- 実機確認日：2026年7月18日
- 実機で同日のエアバイク2件・6件がそれぞれ1枚のまとめカードとなり、異なる日付・種類は別表示になることを確認した
- 実機で「イオンでお買い物」1件が不要な回数表示のない従来どおりの個別カードになることを確認した
- まとめカードの回数、合計時間、保存済み推定消費カロリー合計と、一覧上部の元記録総数維持を確認した
- 展開・折りたたみと、展開後の各個別記録、個別編集、個別削除を確認した
- 編集後に合計値が即時更新され、運動種類等の変更時も保存済み配列から適切なグループへ再配置される構造を確認した
- 2件から1件へ減った場合は通常の個別表示へ、1件から0件になった場合はグループ削除へ即時切り替わることを確認した
- 期間で絞り込んだ後の記録だけをグループ化し、期間外記録を件数・合計へ含めないことを確認した
- TXT・Markdownは個別記録単位、localStorage version 1、JSON formatVersion 2、旧formatVersion 1復元互換を維持する
- 表示専用改善として保存形式と元記録のID・内容を変更せず、グループ情報と展開状態を保存しないことを確認した
- 運動記録一覧の同日・同種目グループ表示を完了とし、次工程は正式運用中の改善事項を整理する

### 予定メモ統合 Phase（完了）

- 予定ごとの補足メモ入力欄を廃止し、予定追加・編集dialogへ日付単位の「その日のメモ」を追加
- 「その日のメモ」は`hootoDay.dayMemos`へ保存し、日記画面と共通の`dayMemos`を唯一のデータ源として使用
- 同じ日の複数予定では同じメモを共有し、予定編集から後日追記・修正可能
- 日記画面からの編集内容も予定dialogへ反映
- 予定の日付変更時にメモを自動移動せず、変更先日付の既存メモを表示
- dialog内では日付ごとの未保存下書きをReact stateで保持し、日付を戻した場合に復元。キャンセル時は保存しない
- 旧予定補足フィールドはoptional互換として維持し、自動削除・自動移行を行わない
- 旧補足がある予定だけ「過去の予定補足」として読み取り専用表示
- events version 1、dayMemos version 1、JSON formatVersion 2、旧formatVersion 1復元互換を維持
- 保存形式とlocalStorageキーは変更せず、commitはユーザー実機確認後に行う
- 実機確認日：2026年7月18日
- 実機で新規予定から保存した「その日のメモ」が日記画面へ反映されることを確認
- 実機で同日複数予定が同じ日付メモを共有し、予定ごとの重複保存がないことを確認
- 実機で予定の日付変更時に元日付のメモが移動せず、元の日付へ維持されることを確認
- 変更先日付の既存メモを参照し、日付ごとの未保存下書きをdialog内stateだけで一時保持する仕様を正式採用
- `CalendarEvent.memo`を互換維持し、旧予定補足を自動削除・自動移行せず、値がある場合だけ読み取り専用表示する
- 新しいlocalStorageキーや保存形式変更はなく、次工程は正式運用中の改善事項を整理する

### 健康画面 独立2列レイアウト Phase（完了）

- 通常の行単位2列Gridによって左右カードが行高を共有し、大きな空白が生じる問題を解消
- PC幅では左右を独立した縦列として配置し、左右のカード高さが互いに影響しない構造へ変更
- 左列は体重、食事、体調メモ、右列は睡眠、運動の順で固定
- 食事の直下へ体調メモを詰め、運動カードは右列内で独立して伸びる
- 920px以下では体重、睡眠、食事、運動、体調メモの正式順で1列表示
- カード内容、保存、編集、削除、日付共有機能は変更していない
- 追加ライブラリやJavaScriptによる高さ測定は使用していない
- localStorage、JSONバックアップ、TXT・Markdown出力の保存形式は変更していない
- ユーザーの実機確認完了後、正式完了としてcommitする
- 実機確認日：2026年7月18日
- 実機で食事の直下へ体調メモが表示され、運動カードが長い場合の左列の大きな空白が解消されたことを確認
- 左右カードの高さが互いの次カード位置へ影響せず、左右同幅と自然な列間余白を維持することを確認
- カード高さ固定、`min-height`による統一、JavaScriptによる高さ測定、masonryライブラリは使用していない
- 実機で920px以下の正式順1列表示、日付操作・編集ボタン、横スクロールなしを確認
- ライト・ダーク・system対応と、カード内容・編集・保存・削除機能を維持
- localStorage、JSON、TXT・Markdown、全データ初期化への影響はなく、次工程は正式運用中の改善事項を整理する

### 記録を見る コンパクト表示 Phase（完了）

- 日付カードの上下余白と、日付見出しから最初の記録までの間隔を縮小
- 各記録行の上下paddingと、記録間・日付カード間の余白を縮小
- 右端の遷移ボタンを少し低くし、560px以下では操作しやすい高さと全幅表示を維持
- 日付見出し、本文、ラベルの文字サイズと本文行間は変更していない
- ラベルの色・角丸、カードの背景・枠線・角丸・影、記録間の境界線は維持
- 読みやすさを保ちながら、縦方向を約15〜20％省スペース化することを目安とした
- 320px付近までのレスポンシブと、短文・長文・複数記録の表示を確認する
- 保存、検索、絞り込み、並び順、画面遷移、JSONバックアップ、TXT・Markdown出力、全データ初期化への影響なし
- 追加ライブラリなし
- 追加調整として、PC版の「カレンダーを見る」「健康記録を見る」「日記・メモを見る」等の遷移ボタンを幅156px・高さ32pxへ統一
- PC版遷移ボタンは13pxの文字サイズと1行表示を採用し、文言の長さにかかわらず右端位置が揃うようにした
- 本文領域の折り返しと`min-width: 0`を維持し、固定幅ボタンが本文を押し出さない構造とした
- 560px以下ではPC用固定幅を解除し、全幅・高さ38pxの既存操作領域を維持
- 追加調整も表示専用CSSに限定し、保存・検索・絞り込み・遷移機能への影響なし
- PC版の記録種類ラベルを幅130pxへ統一し、最長の「今日のできたこと」も省略せず1行表示する
- ラベルの右端と本文開始位置を統一し、PCではラベル・可変幅本文・固定幅遷移ボタンの3領域を整列
- 820px以下の1列表示ではラベルの固定幅を解除し、本文や操作ボタンを圧迫しない既存レスポンシブを維持
- ラベル統一も表示専用CSSに限定し、保存・検索・遷移機能への影響なし
- 実機確認日：2026年7月18日
- 日付カードの上下余白、記録行の上下padding、行・カード間隔、日付見出し下の余白を縮小し、文字サイズと本文行間を維持したことを確認
- PC版遷移ボタンを幅156px・高さ32px・文字サイズ13pxへ統一し、「カレンダーを見る」「健康記録を見る」「日記・メモを見る」が同じ大きさになることを確認
- PC版の記録種類ラベルを幅130pxへ統一し、「今日のできたこと」等の最長ラベルも1行で表示されることを確認
- ラベル右端、本文開始位置、遷移ボタン左右端が揃い、ラベル・可変幅本文・遷移ボタンの3領域が整列することを確認
- 長文は中央の本文領域で自然に折り返し、ラベルや遷移ボタンへ重ならないことを確認
- 820px以下ではラベル固定幅を解除し、560px以下では遷移ボタンの全幅・高さ38pxを維持することを確認
- ライト・ダーク・system、検索・絞り込み・並び順・全文表示・各画面への遷移を維持
- localStorage、JSONバックアップ、TXT・Markdown出力、全データ初期化への影響なし
- 実機で省スペース化と整列を確認し、「すごくすっきりした」と評価された
- 記録を見る コンパクト表示 Phaseを完了とし、次工程は正式運用中の改善事項を整理する
### PWA-1 Manifest・正式アイコン Phase（実装済み・実機確認待ち）
- HootoDay正式アイコンとして`HootoDay-icon-source.png`を採用
- 正式デザインは薄いオレンジ背景・緑のフクロウとし、色味・構図・文字なしを維持
- 通常アイコンの180px・192px・512px、maskableアイコンの192px・512px、48pxのfaviconを生成
- maskableアイコンは元デザインを変更せず、重要部分が中央の安全領域へ収まるよう同色背景上で縮小配置
- `manifest.webmanifest`を追加し、name・short_nameを`HootoDay`、displayを`standalone`、orientationを`portrait-primary`に設定
- `theme_color`はHootoDay UIの主要緑`#397552`、`background_color`は正式アイコン背景から抽出した薄いオレンジ`#fbaf85`を採用
- Apple Touch IconとApple向けWeb App metaを追加
- viewportへ`viewport-fit=cover`を追加
- Service Worker・Workbox・`vite-plugin-pwa`は未導入で、オフライン起動・更新通知は未対応
- localStorage・JSONバックアップ・TXT・Markdown・各保存形式への変更なし
- 追加ライブラリなし
- commitはユーザー実機確認後に行う

### 日付別運動カード 折りたたみ Phase（実装済み・実機確認待ち）

- 日付別運動カードで3件以上の個別記録を初期状態では折りたたむ
- 1～2件は従来どおり個別記録をすべて表示する
- 件数・合計時間・推定消費は折りたたみ中も常時表示する
- 展開後にすべての個別記録と編集buttonを表示する
- 削除により3件から2件以下になった場合は通常表示へ戻す
- 追加直後は追加内容を確認できるよう展開し、日付変更・再読み込み後は初期状態へ戻す
- 展開状態はReact stateだけで管理し、保存形式・集計方法は変更しない
- PC・iPhoneへ対応し、commitはユーザー実機確認後に行う

### 食事定型メニュー select表示修正 Phase（実装済み・実機確認待ち）

- iPhoneで定型メニューselectの文字が上下に切れ、矢印表示が崩れる実機報告を確認
- 16px入力指定とselect固有の高さ・line-height・Safari標準appearanceの組み合わせを調査
- native矢印を非表示にし、clickを妨げない独自矢印1つへ統一
- 狭幅では高さ46px、16px文字、左右padding、右側の矢印領域を確保する
- 長い定型メニュー名は1行表示とし、親幅を押し広げない
- PC版の表示と、定型メニューの追加・編集・削除・食事欄への反映を維持する
- `mealTemplates`・`mealRecords`のversion 1とJSON互換を維持する
- commitはユーザー実機確認後に行う
### iPhone上部ナビ 1段表示 Phase（実装済み・実機確認待ち）
- iPhone・狭幅表示で上部ナビゲーション6項目を横1段に表示
- `repeat(6, minmax(0, 1fr))`による6等分の均等配置とし、横スクロールなしを優先
- モバイル専用短縮ラベルとして「予定・健康・記録・販売・出力・設定」を採用
- PC版の正式ラベル、アイコン、画面遷移先は変更なし
- アイコン21px、ラベル約10.6px、上下padding 5pxのコンパクト表示
- 5列指定が原因だった設定項目の2段目折り返しを解消
- active背景・文字色・`aria-current`を維持し、`aria-label`は正式名称のまま維持
- 320px付近まで6項目1段・横スクロールなしで確認
- localStorage・JSONバックアップ・TXT・Markdown・各保存形式への影響なし
- PWA-1の正式アイコン・manifest・Apple向けmeta・viewport-fit対応を維持
- Service Workerは未導入、追加ライブラリなし
- commitはユーザー実機確認後に行う
- 追加調整として、iPhone上部ナビから「出力」を外し、「予定・健康・記録・販売・設定」の5項目へ変更
- 出力・バックアップ機能とPC版の正式ナビは削除せず、設定dialog内の「データ管理」から既存画面を開ける導線を追加
- モバイルの大きな角丸active背景を廃止し、文字色・アイコン色・短い下線を中心としたフラットなactive表現へ変更
- ナビbuttonを46pxへ低くしつつ、5等分セル全体のタップ領域、`aria-current`、設定のdialog属性を維持
- 出力画面表示中は5項目のいずれにも誤ったactive表示を付けない
### iPhoneカレンダー予定 1行コンパクト表示 Phase（実装済み・実機確認待ち）
- iPhone・狭幅表示の予定チップをコンパクト化
- 時間＋予定名の1行表示を維持し、2行表示は採用しない
- 予定文字を9pxへわずかに縮小し、時間表示は省略しない
- 時間を固定領域、予定名を残り幅で縮小する領域とし、予定名側だけ末尾省略
- チップ左右paddingを3pxから2px、時間と予定名のgapを3pxから1pxへ縮小
- チップ角丸を4pxから3pxへ縮小
- 日付セル内余白を上下4px・左右3pxから上下3px・左右1pxへ縮小
- チップをセルの利用可能幅いっぱいにし、複数予定の縦gapも3pxから2pxへ縮小
- PC版の予定表示、カテゴリ色、予定順、最大表示件数、保存形式は変更なし
- 320px付近まで1行表示・横スクロールなしで確認
- PWA-1の正式アイコン・manifest・Apple向けmetaと、6項目1段ナビを維持
- Service Workerは未導入、追加ライブラリなし
- commitはユーザー実機確認後に行う
- iPhone実機では予定名が2文字程度しか見えない場合があったため、カテゴリ左線を1px、左右paddingを1px、時間と予定名のgapを0へ再調整
- 時間＋予定名の1行表示と時間の完全表示を維持し、予定名側だけを末尾省略する
- モバイルでは予定タイトルそのものを表示し、カテゴリ正式名と一致する予定をカテゴリ短縮名へ置換しない
- 選択枠を疑似要素でセル背景側へ描画し、日付・記録アイコン・予定チップを前面へ分離してカテゴリ色との重なりを解消
- ライト・ダーク・system切替後も選択枠、今日表示、予定チップ、カテゴリ左線が安定する構造へ変更
- iPhoneでは前月・月タイトル・翌月・今日・できたことを1行化し、月タイトルと各操作をコンパクト化
- 月操作領域の下余白を縮め、曜日行とカレンダーの開始位置を上へ詰める
- PC版の月操作、月のベスト表示、予定チップは変更していない
- 保存形式、localStorage、JSONバックアップへの影響なし
- 状態は引き続き「実装済み・実機確認待ち」とし、commitはユーザー再確認後に行う
- 選択枠と予定チップの描画順をPC・iPhone共通で再整理し、日付セルへ独立した重なりコンテキストを設定
- 選択枠は2pxの疑似要素として背面に配置し、日付数字・今日丸・記録アイコン・予定チップ・カテゴリ左線を前面へ固定
- テーマ切替直後もレイヤー順をCSS宣言順へ依存させず、カテゴリ色と保存データへ影響しない構造を採用
- z-indexだけでは実機上で選択枠とチップ端の接触が残ったため、選択日の上部内容と各予定チップを左右2pxずつ内側へ移動
- 選択日の予定チップ幅を合計4px縮小し、2pxの選択枠とカテゴリ左線・チップ背景を物理的に分離
- 内側余白は選択日だけへ適用し、PC・iPhone共通で非選択日の表示幅を維持

### iPhoneカレンダー 月操作幅配分 Phase（実装済み・実機確認待ち）

- モバイル年月を`2026/7`形式へ短縮し、PC版の`2026年7月`表記は維持
- 前月・年月・翌月を必要最小限の固定幅グループへまとめ、矢印と年月の余白を縮小
- 今日buttonは必要最小限の内容幅とし、月操作バーの余剰幅をすべて「できたこと」へ割り当て
- 「できたこと」は省略・改行せず、既存の月一覧dialogと月のベスト選択機能を維持
- 月移動グループ・今日・できたことを1行で表示し、320px以上で横スクロールを発生させない
- 予定チップ1行表示、保存済み予定タイトル、選択枠の背面描画、PC版表示を維持
- 保存形式、localStorage、JSONバックアップへの影響なし
- commitはユーザー再確認後に行う

### 今日のできたこと 30文字制限 Phase（実装済み・実機確認待ち）

- PC・iPhone共通で、新規入力・編集時の「今日のできたこと」を最大30文字へ変更
- 共通定数を設け、HTMLの`maxLength`、dialogの保存検証、Hookの保存前検証で同じ上限を使用
- 入力中に`現在文字数 / 30`のカウンターを表示し、上限情報を`aria-live`と入力説明へ関連付け
- 短い達成記録として扱い、長文はその日のメモへ記録する案内を追加
- 既存の30文字超データは削除・自動切り詰め・読込除外せず、旧120文字以内データの読取・JSON復元互換を維持
- 既存30文字超データを編集保存する場合だけ、30文字以内への修正を求める
- カレンダー上部の月のベスト本文は1行省略を維持し、表示文字を少し小さく・軽く調整
- 選択日詳細、記録を見る、月一覧、TXT・Markdown出力では既存内容を確認できる
- localStorageキー、version、JSON formatVersion、型、全データ初期化形式は変更していない
- commitはユーザー実機確認後に行う
- 実機で日付別のできたことを保存・変更してもカレンダー上部が変わらないとの報告を受け、保存から表示までのデータフローを再調査
- 日付別記録は保存直後に共通stateへ反映され、選択日詳細・月の候補一覧・記録を見る・出力・JSON対象へ即時伝播することを確認
- カレンダー上部の本文は日付別記録ではなく、月一覧から手動選択した「月のベスト」だけを表示する既存仕様であり、自動選択されないことが不更新に見える主因
- 月のベスト自動選択は追加せず、モバイル導線を「今月一覧」、選択済みPC表示へ「月のベスト」ラベルを追加して役割を明確化
- 日付別記録の新規・編集・削除後、月候補と月のベスト本文が同じstateから再計算され、月切替・再読み込み後も保存内容を参照する構造を維持
- 30文字制限、保存形式、localStorage、JSON、TXT・Markdownへの影響なし
- 月一覧で月のベストを選択してもdialogが開いたままで、背後の更新済み上部表示を確認できないことが「選んだのに出ない」と感じる主因だった
- 月のベスト選択成功時は選択stateを更新して月一覧dialogを閉じ、カレンダー上部の選択済み表示を即座に見せる
- モバイルの未選択表示は「今月一覧」、選択済み表示は「月のベスト」とし、保存状態と表示ラベルを一致させる
- 月キー・選択日参照・`hootoDay.monthlyAchievementSelections` version 1の保存・読込・編集追従・削除解除は既存構造を維持

### 健康画面 iPhone上部UI省スペース調整（実装済み・実機確認待ち）

- 560px以下では、日付別・体重・睡眠・運動・体調の5画面切り替えをアイコン付きの1行5等分表示へ変更
- モバイルラベルを短縮し、active状態は既存テーマ色を使いながら小さな操作領域で判別可能にする
- PC版は正式名称と従来の見やすさを維持し、同じbutton・`aria-pressed`で操作する
- 560px以下の共通選択日は、前日・日付入力・次日・今日を1行へまとめ、重複していた大きな日付見出しを非表示にする
- 日付入力、共通`selectedDate`、各健康カード・カレンダーとの連動は変更しない
- 320px以上で横スクロールを発生させず、ライト・ダーク・systemへ既存テーマ変数で対応する
- localStorage、JSONバックアップ、TXT・Markdown、初期化、健康記録の保存形式への影響なし
- commitはユーザー実機確認後に行う

### 月のベスト 実機タップ経路再調査（実装済み・実機確認待ち）

- 実機では月一覧の候補を選択しても上部表示が変わらない問題が継続したため、候補buttonからstate保存・dialog終了・上部再描画までを再調査
- 日付は`YYYY-MM-DD`、月キーは`YYYY-MM`で統一され、`monthlyAchievementSelections`のstate・localStorage version 1・表示側の参照条件に不一致がないことを確認
- 従来は候補カード内の小さなbuttonだけが選択操作であり、iPhoneでは候補行をタップしても選択clickが発火しない領域が大きかった
- 未選択候補はカード全体を単一buttonへ変更し、buttonの入れ子を作らず、タッチ操作・focus-visible・`aria-pressed`を維持
- 選択成功時だけstateを更新してdialogを閉じ、上部を「月のベスト」へ即時切り替える
- 候補変更、再読み込み、月移動、元記録編集、元記録削除、PC・iPhone表示を確認する
- 月のベスト自動選択、保存構造、JSON、TXT・Markdownへの変更なし
- 実機では選択・保存・dialog内の選択済みカード・上部ラベルの「月のベスト」切り替えは正常で、上部本文だけが表示されない状態と特定
- `MonthlyAchievementHighlight`は選択本文をDOMへ描画していたが、560px以下のCSSが本文と王冠・ラベルを`display: none`にし、短縮ラベルだけを表示していた
- モバイルの選択済みbuttonを省スペースな2段表示へ変更し、1行目へ「月のベスト」、2行目へ王冠＋選択本文を配置
- 2行目の本文へ残り幅を割り当て、長文だけを1行末尾省略し、日付はモバイルで非表示にする
- 行間と文字サイズを抑え、月操作バーの高さをほぼ増やさない設計とする
- 未選択時の「今月一覧」、PCの王冠・ラベル・本文・日付、dialog内全文表示を維持
- buttonの`aria-label`へ選択本文全文を含め、視覚的に省略されても読み上げで確認可能にする
- 保存、選択、dialog終了、monthKey、localStorage、JSON、TXT・Markdownへの変更なし

### 睡眠途中覚醒 追加時白画面修正 Phase（実装済み・実機確認待ち）

- iPhone実機で「途中覚醒を追加」を押すと白画面になり、操作不能になる問題を確認
- 途中覚醒の追加時だけ`crypto.randomUUID()`をフォールバックなしで直接呼び出しており、未対応環境では実行時例外になる構造を原因として特定
- `crypto.randomUUID`が利用できる環境では従来値を使用し、未対応環境では表示・保存に使える一意な文字列IDを生成するよう修正
- 途中覚醒の正式なデータ構造、初期15分、時刻だけ・時間範囲の2方式、入力検証、計算式は変更しない
- 1件・複数件の追加、各行の編集・削除、キャンセル、保存、再読み込みを確認する
- iPhone狭幅とPCでdialog表示、縦スクロール、保存操作、横スクロールなしを確認する
- 睡眠カード、睡眠まとめ、記録一覧、TXT・Markdown、JSONバックアップ・復元の既存処理を維持
- `hootoDay.sleepRecords`とversion 1、JSON formatVersion 2、旧formatVersion 1復元互換を維持
- commitはユーザー実機確認後に行う

### 睡眠まとめ 表示順整理 Phase（実装済み・実機確認待ち）

- 睡眠まとめの最新の睡眠を最上部に維持し、実睡眠時間グラフをその直下へ移動
- 途中覚醒傾向をグラフの下、期間平均を主要情報の最後へ移動
- メモ付き記録などの既存情報を期間平均の後へ維持
- CSSの`order`に依存せず、DOM順と視覚順・読み上げ順を一致
- PC・iPhoneで同じ基本順を使用し、既存のカード間余白だけを維持
- 睡眠計算、期間集計、グラフデータ、保存形式、localStorage、JSON、TXT・Markdownへの影響なし
- commitはユーザー実機確認後に行う

### iPhone dialog保存操作・運動1回保存修正 Phase（実装済み・実機確認待ち）

- iPhone実機で運動追加時に保存を一度押しても反映されない場合がある報告を確認
- 運動保存は既存の`form onSubmit`と`type="submit"`へ統一済みで、保存入口を追加せず維持
- iPhoneではpanel全体と操作欄が同じスクロール領域にあり、キーボードやブラウザ下部UI表示時に最初のタップがフォーカス解除へ使われやすい構造を原因として整理
- 運動・睡眠dialogを、固定ヘッダー、縦スクロールする本文、下部操作欄の3領域へ整理
- 下部操作欄をdialog内下端へsticky配置し、`100dvh`、Safe Area下余白、本文のoverscroll抑制へ対応
- iPhone幅では運動・睡眠dialog内のinput、select、textareaを16px以上にして自動ズームを防止
- 運動保存中は保存buttonを無効化して「保存中…」を表示し、同期処理への連続タップによる重複保存を防止
- 入力エラー時は該当入力へスクロール・フォーカスし、`aria-invalid`と`aria-describedby`でエラーを関連付ける
- 睡眠等の長い健康dialogへ共通の本文スクロールclassを再利用し、全dialogの大規模変更は行わない
- 運動記録の保存形式、計算式、グループ表示、localStorage version 1、JSON formatVersion 2、旧v1互換を維持
- commitはユーザー実機確認後に行う

### 体重まとめ コンパクトカード・グラフ優先 Phase（実装済み・実機確認待ち）

- 最新体重、BMI、標準体重、美容体重、目標体重を短い数値カードとして整理
- PCは3列を基本とし、iPhoneは320px以上で2列を基本とする
- 最新体重の前回比、BMIの体重範囲、標準・美容・目標体重と最新値との差を維持
- 「体格の参考値です」「BMI 20相当」等の一般向け長文説明を削除し、医療診断ではない旨だけを短く維持
- 集計期間操作をページ見出し側へ移し、体重グラフを期間集計より上へ配置
- グラフ下へ選択期間の平均、最小、最大、期間内増減、記録件数をコンパクトカードで整理
- グラフのデータ、軸、目標線、空状態、期間切り替えを維持
- 体重・BMI・標準体重等の計算式、丸め、localStorage、JSON、TXT・Markdownへの変更なし
- commitはユーザー実機確認後に行う

### 運動まとめ 主要集計2列表示 Phase（実装済み・実機確認待ち）

- 合計回数・運動日数・合計時間・推定消費カロリーをコンパクトな4カードへ整理
- iPhoneでは合計回数・運動日数、合計時間・推定消費の2列×2段で表示する
- PCでは既存の4列表示、920px以下では2列表示を維持する
- 狭幅では推定消費カロリーを「推定消費」と表示し、読み上げ上の正式名称は維持する
- 計算式・丸め・保存形式・期間切り替え・後続セクションの順番は変更しない
- 320px以上へ対応し、commitはユーザー実機確認後に行う

### 記録を見る 検索・絞り込み折りたたみ Phase（実装済み・実機確認待ち）

- 検索・絞り込み領域をPC・iPhone共通で初期折りたたみに変更
- buttonで展開・閉じることができ、閉じても入力条件と検索結果を維持する
- 検索文字・記録種類・期間・並び順の非初期値を適用条件数として表示する
- 全件数と表示件数は折りたたみ外へ残し、常時確認可能にする
- 展開状態はReact stateだけで管理し、再読み込み後は折りたたみへ戻す
- 検索対象・検索ロジック・localStorage・JSON・TXT・Markdown形式は変更しない
- PC・iPhoneへ対応し、commitはユーザー実機確認後に行う

## iPhoneカレンダー クイック追加スライド Phase（実機確認済み）

- iPhone・狭幅のカレンダー下部へ、選択日を対象にする固定「＋」buttonを追加。
- 「＋」から選べる項目は「予定」「メモ」の2つに限定。
- 予定追加は既存のevents保存処理、メモ追加は既存のdayMemos保存処理を再利用。
- 選択中の日付を予定・メモ入力へ引き継ぎ、保存後は既存どおりカレンダーや日付詳細へ即時反映。
- モバイルのクイック追加から開いた入力画面だけ、右からスライドする全画面パネルとして表示。
- 左上の戻るbuttonは既存のキャンセルと同じく、保存せず入力画面を閉じる。
- パネル幅をviewport内へ固定し、入力欄を含めてmin-width: 0とbox-sizingを整理。
- ヘッダー・スクロール可能な本文・下部操作欄の3領域へ整理し、100dvhとSafe Areaへ対応。
- PC版の予定追加・予定編集・日記メモdialogは従来表示を維持。
- localStorageキー、各version 1、JSON formatVersion 2、旧v1復元互換、TXT・Markdown形式は変更なし。
- Service Workerとオフライン対応は未導入のまま維持。
- commitはユーザーのiPhone実機確認後に行う。
- 実機で日付・開始時刻・終了時刻のinputがiOS固有の内部最小幅により横超過する問題を確認。
- iPhoneでは日付を1列全幅、開始・終了時刻を同幅の2列へ整理。
- 時刻列へ`repeat(2, minmax(0, 1fr))`を使用し、親・子へ`min-width: 0`を適用。
- date/time inputのinline-size、max-inline-size、box-sizingを親幅内へ固定。
- iOSのnative date/time pickerは維持し、独自pickerや固定幅は追加していない。
- 430px、393px、375px、360px、320pxで横幅を確認。
- PC版の既存2列配置、保存処理、保存形式は変更なし。
- 前回の開始・終了時刻2列配置でも、iPhone Safari実機ではnative time inputの固有最小幅により重なりと右側超過が継続。
- 実機で確実に重ならないことを優先し、560px以下の予定フォーム全体を`minmax(0, 1fr)`の単一列へ変更。
- モバイルでは日付・開始時刻・終了時刻をすべて全幅の縦1列表示として正式採用。
- 「その日のメモ」はlabel、textarea、補足文、文字数の順へ整理し、textareaの最小高を確保。
- 本文スクロール領域の下余白を増やし、本文末尾と下部操作欄が重ならないよう調整。
- 393×500px、393×600px、320×500pxを含む短いviewport高で確認する。
- PC版、events・dayMemosの保存処理、保存形式は変更なし。
- モバイル単一列化後もiPhone実機で右側超過が継続したため、input単体ではなくdialog・panel・header・form body・field・footerまで再監査。
- EventEditorDialogではfieldset・legendを使用しておらず、fieldset固有のmin-content幅は原因外と確認。
- native dialogの`inset: 0`と`width: 100%`の組み合わせ、およびfooterのflex子buttonへ一律100%幅を与える構造を実機差が出る要因として整理。
- モバイルdialogは`inset-inline: 0`・`inline-size: auto`・`max-inline-size: none`へ変更し、100vw依存を除去。
- panel配下の全要素と疑似要素をborder-boxへ統一し、各wrapperへmax-width・min-width・inline-size制約を追加。
- footerは`repeat(2, minmax(0, 1fr))`の明示的な2列gridとし、buttonを各列内へ固定。
- label・補足文・文字数・textareaを含む全要素をviewport内へ制約し、320pxまで実測する。
- PC版と保存形式は変更せず、iPhone実機で固定「＋」、予定・メモ選択、スライドパネル、通常予定保存、横超過なしを確認済み。
- 320px相当の短い表示高ではdialog配下に超過要素がなくても、`html`・`body`の`min-width: 320px`がスクロールバー分を含む可視幅より広く残ることを確認。
- 560px以下ではroot側の固定最小幅を解除し、横超過をoverflowで隠さずdocument自体を利用可能幅へ追従させる。

## iPhoneカレンダー クイック追加スライド Phase：予定入力配置の正式調整（実機確認済み）

- モバイル予定入力のDOM順と表示順を、予定名、日付・カテゴリ、開始・終了時刻、終日、その日のメモへ統一
- 予定名を最上部の全幅欄へ配置
- 日付とカテゴリを `repeat(2, minmax(0, 1fr))` の同一行へ配置
- 開始時刻と終了時刻を同一行へ配置
- 終日予定とその日のメモは全幅を維持
- iOSのnative date/time inputを維持し、親幅内へ収まるよう論理幅とoverflowを制約
- カテゴリselectの矢印領域を確保し、長い表示名は欄内で省略
- モバイルのラベル、行間、textarea高を省スペース化
- 320pxまで横超過しない構造とし、PC版のdialog動作・保存形式は維持
- iPhone実機で日付・カテゴリ、開始・終了時刻の2列配置と右側への横超過解消を確認済み

### iPhone予定入力 native input直接タップ修正（iPhone実機確認済み）

- 表示buttonから`showPicker()`・`click()`を呼ぶ方式では、iPhone Safari実機でnative pickerが開かないことを確認
- iOS Safariではプログラム経由のpicker起動が安定しないため、JavaScriptによる起動処理を廃止
- native date/time inputを表示field全体へ透明overlayし、native input自身がユーザーの直接タップを受ける方式へ変更
- 見た目用要素は非操作divとし、`aria-hidden`・`pointer-events: none`で操作要素をnative input一本へ統一
- labelの`htmlFor`をnative inputのidへ関連付け、labelタップ、キーボード、スクリーンリーダー操作をnative input側へ集約
- native inputへ`aria-label`・`aria-invalid`・`aria-describedby`を設定し、エラー時のscroll・focusを維持
- `focus-within`で透明inputのfocusを見た目用fieldへ表示
- 終日予定ではnative inputをdisabledにし、見た目もdisabled表示する
- 日付`YYYY/MM/DD`・時刻`HH:mm`・未設定表示と、従来の保存値・stateを維持
- PC版native input、モバイル2列配置、320pxまでの横超過なしを維持
- iPhone実機で日付・開始時刻・終了時刻pickerと通常予定保存を確認済み

### iPhone予定入力 native picker表示分離（実機確認済み・直接タップ方式へ更新済み）

- iOS Safariのnative date/time input内部描画が2列の列幅を超える実機問題を確認
- width・min-width・overflow等のCSS制約だけではWebKit内部描画を制御できないため、モバイルのみ表示用buttonと裏側のnative pickerを分離
- 表示用buttonから同期的に`showPicker()`を呼び、未対応・例外時はnative inputの`click()`へフォールバック
- 日付は表示だけ`YYYY/MM/DD`へ整形し、保存値は従来どおり`YYYY-MM-DD`を維持
- 時刻は`HH:mm`表示、未設定時は「未設定」とし、保存値も従来どおり`HH:mm`を維持
- native inputはDOMと既存state連携を維持しつつ、WebKit依存の内部文字・picker iconを表面へ出さずレイアウト幅へ影響させない
- 必須・時刻前後関係エラーは表示用buttonへ関連付け、最初の不正項目のスクロール・focusを維持
- 終日予定では表示用buttonとnative inputの双方をdisabledにし、pickerを開かない
- PC版は従来のnative date/time input、dialog幅、配置を維持
- 320pxまで2列と横超過なしを確認し、PWA・将来のWebViewアプリ化でも同じ表面表示を使用する
- localStorage、JSONバックアップ、TXT・Markdown、全データ初期化の形式変更なし
- 日付・時刻の表示崩れ解消を実機確認済み。picker操作はnative input直接タップ方式へ更新済み

### Sync Design Phase（設計完了・実装未着手）

- 今後のHootoDay同期実装の正式仕様として`SYNC_DESIGN.md`を作成
- localStorageを各端末の正式保存先として維持し、SupabaseはPC・iPhone間の同期媒体として使用する方針
- Supabase Authのanonymous sign-inと、`workspace`・`member`・`device`による端末接続を設計
- 最初のPCをowner、ペアリングしたiPhoneをmemberとし、短時間・1回限りのpairing codeをsecurity definer RPCで処理する方針
- owner/member限定RLSとし、service roleをフロントエンドで使用しない
- 最初の同期パイロットはDayMemoだけに限定し、手動push/pull、初回同期保護、入力検証、エラー時のlocalStorage非変更を設計
- 空のiPhoneがデータ入りPCを空で上書きしないことを初回同期の最重要規則として確定
- 共通同期レコード、revisionとbase revision、tombstone、再送の冪等性、競合時の自動上書き禁止を設計
- DayMemoには独立IDがないため、日付を同期上の`entity_id`として使用する
- 一般データは共通同期テーブル方式を採用し、在庫・販売同期は専用トランザクションと冪等性が必要な最後のPhaseに分離
- `hootoDay.theme`と画面状態は同期対象外
- Service Worker、自動同期、バックグラウンド同期は後続Phaseで検討
- 既存localStorageの各version 1形式は変更しない
- JSONバックアップformatVersion 2と旧formatVersion 1復元互換は変更しない
- 次工程は既存hooto-platformのテーブル・RPC・RLS確認と、非破壊のSQL設計
- commitはユーザー確認後に行う
